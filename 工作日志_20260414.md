# 北交所新股估值项目工作日志

日期：2026-04-14  
记录人：Codex  
目的：保存当前分析结论、阶段决策与下一窗口可直接续做的上下文

## 一、本轮工作目标

本轮的实际工作重点不是立即写完整业务代码，而是先完成三件事：

- 阅读并理解项目方案文档
- 识别当前仓库的真实状态
- 拆出一条可执行的、去 Wind 化的实施路径

## 二、已阅读文档

本轮重点阅读并整理了以下文档：

- `方案文档_北交所新股上市首日估值框架.md`
- `实施计划_北交所新股估值框架.md`
- `minimal_project_template/README.md`
- `minimal_project_template/python/config_loader.py`

### 文档结论

两份主文档已经把项目目标、数据源、目录结构、公式、参数模板、报告模板、模块职责写得比较完整。

当前缺的不是“方案设计”，而是“把方案正式编码成模块化工程”。

## 三、当前仓库状态梳理

仓库当前并不是正式工程结构，而是“验证脚本 + 文档 + 样本数据”的状态。

### 当前主要文件

- `calc_indicators.py`
- `calc_scores.py`
- `collect_ipo_data.py`
- `download_wsi.py`
- `find_candidates.py`
- `get_comparable_pe.py`
- `pick_two.py`
- `query_industry.py`
- `query_industry2.py`
- `query_industry3.py`
- `test_wss.py`
- `valuation_verification.py`
- `valuation_verification_v2.py`
- `wsi_analysis.py`
- `wsi_analysis2.py`
- `首日分时走势/*.csv`
- 两份主方案文档

### 当前缺失的正式工程文件

根据实施计划，以下正式文件还没有建立：

- `bse_ipo_valuation.py`
- `config_loader.py`
- `data_fetcher.py`
- `wind_helper.py`
- `valuation_engine.py`
- `report_generator.py`
- `pdf_parser.py`
- `industry_mapping.py`
- `trend_scorer.py`
- `策略参数.txt`
- `运行.bat`
- `说明文档.md`

### 当前缺失的正式目录

- `公告文件`
- `输出`
- `data`

## 四、对现有脚本的理解

现有 Python 文件大致可以分为四类：

### 1. 东方财富 / IPO 信息探索

- `collect_ipo_data.py`

这类脚本用于验证或探索 IPO 数据获取方式，但还没有抽象成稳定接口。

### 2. Wind 相关验证

- `get_comparable_pe.py`
- `find_candidates.py`
- `pick_two.py`
- `query_industry.py`
- `query_industry2.py`
- `query_industry3.py`
- `test_wss.py`

这类脚本主要用于探索可比公司估值、行业字段与 Wind 返回特征，暂时不应直接当正式模块使用。

### 3. 分时走势与打分验证

- `download_wsi.py`
- `calc_indicators.py`
- `calc_scores.py`
- `wsi_analysis.py`
- `wsi_analysis2.py`

这类脚本已经沉淀出一部分可迁移逻辑，尤其是：

- CSV 读取方式
- 分时基础指标计算
- 四类走势分类思路
- 多维评分思路

后续适合重构进 `trend_scorer.py`。

### 4. 估值验证脚本

- `valuation_verification.py`
- `valuation_verification_v2.py`

这类脚本已经沉淀出一部分核心公式，尤其是：

- 方法一的 EPS 与可比 PE 逻辑
- 方法二的基础涨幅与调节因子逻辑
- 综合定价逻辑

后续适合重构进 `valuation_engine.py`。

## 五、从文档提炼出的正式目标结构

本项目最终目标是一个模块化脚本工具，主流程如下：

1. 输入新股代码
2. 读取 `策略参数.txt`
3. 获取 IPO 基础信息
4. 获取老股转让与可比公司信息
5. 计算方法一与方法二
6. 生成关注提示
7. 输出 Markdown 报告

其中正式模块边界已经比较明确：

- `config_loader.py`
- `data_fetcher.py`
- `industry_mapping.py`
- `pdf_parser.py`
- `trend_scorer.py`
- `wind_helper.py`
- `valuation_engine.py`
- `report_generator.py`
- `bse_ipo_valuation.py`

## 六、环境检查结果

本轮做过基础环境核对，结果如下。

### Python

- 当前命令行 `python` 版本：`Python 3.13.12`
- `py -V` 返回：`Python 3.13.12`
- 可执行路径包含：`C:\Users\ai\AppData\Local\Programs\Python\Python313\python.exe`

### 依赖导入情况

- `openpyxl`：可导入
- `numpy`：可导入
- `WindPy`：可导入
- `pdfplumber`：当前未安装

### 环境结论

- 不依赖 Wind 的主干开发可以立即开始
- PDF 解析模块后续如果要落地，需要补装 `pdfplumber`
- 当前不建议让项目初版依赖 `pdfplumber` 才能运行，应做成可选依赖

## 七、Wind 相关决策

这一条非常重要，供下一窗口直接继承。

### 已发生事项

在用户叫停之前，本轮曾做过一次 Wind 可用性试探性检查，用于确认环境中存在 `WindPy`。

### 当前有效决策

自 2026-04-14 用户明确提出“Wind 接口先关掉”后，后续施工应遵循以下约束：

- 暂不进行 Wind 实际接入
- 暂不进行 Wind 接口验证
- 暂不让主流程依赖 Wind
- 可以保留 `wind_helper.py` 文件名和参数位，但只做占位或禁用模式

### 下一窗口执行原则

除非用户重新明确开启 Wind，否则下一窗口不要：

- 调 Wind API
- 做 Wind 通道测试
- 设计 Wind Excel 回退交互

## 八、关键认识

本轮最重要的判断有三条。

### 1. 项目当前最缺的是“工程化”

方案文档已经足够细，不缺设计说明。真正的缺口在于：

- 正式文件没有落地
- 模块边界还没有固化
- 主流程还没有统一入口

### 2. 现有验证脚本可以复用，但不能直接当成正式产物

尤其是：

- `valuation_verification_v2.py`
- `calc_indicators.py`
- `calc_scores.py`

这些脚本更适合作为“公式来源”和“逻辑参考”，需要重构后进入正式模块。

### 3. 先做无 Wind 版本是正确路径

原因很明确：

- 能更快得到可运行版本
- 能降低依赖和不确定性
- 能把复杂问题拆开
- 便于后续单独恢复 Wind，不污染主流程

## 九、最终确定的施工方案

施工方案已另存为：

- `施工方案_无Wind优先.md`

其核心路线是：

1. 先整理项目骨架
2. 先实现配置、行业映射、东方财富抓取
3. 先跑通无 Wind 主流程
4. 再补 PDF 与本地分时 CSV
5. 最后单独恢复 Wind

## 十、建议的下一窗口开工顺序

建议下一窗口不要再重新做大范围分析，直接按下面顺序落代码：

1. 创建正式目录和参数模板
2. 编写 `config_loader.py`
3. 编写 `industry_mapping.py`
4. 编写 `data_fetcher.py`
5. 编写 `valuation_engine.py`
6. 编写 `report_generator.py`
7. 编写 `bse_ipo_valuation.py`
8. 运行一次最小主流程测试

### 最小主流程测试标准

只要满足下面几项，就说明第一阶段成功：

- 输入新股代码可运行
- 能读取参数文件
- 能请求东方财富数据
- 能使用方法二计算结果
- 能输出一份 Markdown 报告到 `输出` 目录

## 十一、下一窗口的实现边界

为避免任务再次变大，建议下一窗口只做以下内容：

- 正式工程骨架
- 配置加载
- 行业映射
- 东方财富接口封装
- 方法二估值
- Markdown 报告输出

建议下一窗口暂不做以下内容：

- Wind
- PDF 解析细节
- 自动下载公告
- 复杂走势评分
- 运行批处理优化

## 十二、备注

当前仓库不是 Git 仓库目录，本轮没有进行分支、提交或版本控制操作。

如果下一窗口直接开始编码，最推荐的起手动作是：

- 先创建目录与模板文件
- 再从 `config_loader.py` 开始
- 保持所有 Wind 逻辑处于禁用状态

---

## 十三、夜间续做记录（2026-04-14）

在本日志形成后，项目主干已继续向前推进，当前新增完成情况如下：

- `pdf_parser.py` 已接入真实 PDF 解析，可从招股说明书 / 招股意向书中提取公司简介与可比公司代码
- `trend_scorer.py` 已接入本地 `首日分时走势` CSV，走势因子已正式进入方法二
- 方法二与走势模块都已加上“仅使用上市日早于标的股票上市日的历史样本”约束，避免前视偏差
- 方法二基础涨幅统计与走势分数汇总，已切换为“时间衰减中位数”口径，越接近标的上市日的样本权重越高

### 已记录待继续增强的问题

当前“老股转让”识别已能处理明确负例，例如：

- `本次发行全部为新股发行`
- `不涉及原股东公开发售股份`

但上市公告书文本抽取存在样本差异，部分文件虽然能成功读取正文，却未必能稳定抓到老股转让字段。因此当前策略为：

1. 优先读取上市公告书
2. 若上市公告书未提取到明确结果，则回退到招股说明书 / 招股意向书
3. 若两者都未识别，再按 `0` 并在报告中标注“待确认”

### 明天继续补样本时的验证重点

- 多补几份“明确有老股转让”的上市公告书
- 多补几份“明确无老股转让”的上市公告书
- 对比同一家公司招股书与上市公告书中的字段表达差异
- 继续收紧 `pdf_parser.py` 中老股转让字段的正例 / 负例规则

---

## 十四、今日实际完成内容汇总（收尾版）

截至 2026-04-14 收工前，项目已经从“方案文档 + 验证脚本”推进到“可运行的正式工程主干”，今天实际完成的工作可归纳为以下几部分。

### 1. 正式工程骨架已基本落地

已建立并接通以下主模块：

- `bse_ipo_valuation.py`
- `config_loader.py`
- `data_fetcher.py`
- `industry_mapping.py`
- `valuation_engine.py`
- `report_generator.py`
- `pdf_parser.py`
- `trend_scorer.py`
- `wind_helper.py`
- `策略参数.txt`
- `说明文档.md`
- `运行.bat`

同时已建立正式目录：

- `公告文件`
- `输出`
- `data`

### 2. 无 Wind 主流程已经可以稳定出报告

当前主流程已经支持：

1. 输入北交所新股代码
2. 读取 `策略参数.txt`
3. 从东方财富获取 IPO 基础数据与近期样本
4. 计算方法二估值
5. 输出 Markdown 报告到 `输出` 目录

在 `wind_channel = disabled` 的前提下，主流程可以独立运行，不依赖 Wind。

### 3. 走势评分模块已正式接入方法二

今天已完成 `trend_scorer.py` 的正式接入，当前能力包括：

- 从本地 `首日分时走势\*.csv` 读取首日分时数据
- 按四类走势形态进行分类
- 按五维评分体系计算综合分，第六维换手率保留为可选权重
- 将走势因子正式纳入方法二调节因子

同时，已明确并落地以下约束：

- 走势计算不能把标的股票自己的首日表现计入历史样本
- 走势样本只能使用上市日早于标的股票上市日的历史新股

### 4. 方法二已升级为“时间衰减加权”口径

今天新增完成的增强点：

- 方法二基础涨幅统计已由简单中位数切换为“时间衰减中位数”
- 走势分数汇总同样切换为“时间衰减中位数”
- 越接近标的上市日的历史样本权重越高

这样做的目的，是让估值结果更灵敏地反映最近市场温度，同时继续避免前视偏差。

### 5. PDF 提取模块已进入可实测状态

今天已用真实 PDF 样本接通并验证以下能力：

- 从招股说明书 / 招股意向书中提取公司简介
- 从招股说明书 / 招股意向书中提取可比公司代码
- 从上市公告书或招股文件中提取老股转让信息
- 主流程已支持更宽松的 PDF 文件命名识别

目前已支持识别的文件类型包括：

- `招股说明书`
- `招股说明书摘要`
- `招股书`
- `招股意向书`
- `上市公告书`
- `上市公告`

### 6. 老股转让识别已修正一类关键误判

今天在 `920177` 样本中定位并修复了一类关键问题：

- 过去会把“本次发行全部为新股发行，不涉及原股东公开发售股份”误判成存在老股转让
- 当前已改为先识别负例，再做正例提取

也就是说，以下表述已可稳定识别为 `0`：

- `本次发行全部为新股发行`
- `不涉及原股东公开发售股份`
- `不涉及老股转让`
- `无老股转让`

### 7. 已完成的真实回归验证

今天已完成的真实报告验证包括：

- `920012`
- `920156`
- `920177`
- `920186`

其中：

- `920156` 已验证招股说明书里的公司简介和可比公司代码能进入最终报告
- `920177` 已验证老股转让误判被修正，报告中显示为 `0.00 万股（PDF 提取：招股文件）`
- `920186` 已验证 `招股意向书` 可以被主流程识别并提取公司简介

## 十五、下一步需要做的事情

下一步建议继续按“样本增强优先”的思路推进，优先级如下。

### 第一优先级：继续补公告样本，增强老股转让识别

- 补充更多上市公告书 PDF，尤其是“明确有老股转让”的样本
- 补充更多上市公告书 PDF，尤其是“明确无老股转让”的样本
- 继续对比同一家公司招股书与上市公告书中的字段表达差异
- 继续收紧 `pdf_parser.py` 中老股转让字段的正例 / 负例规则

### 第二优先级：继续增强报告展示细节

- 考虑在报告中展示方法二实际采用的历史样本代码
- 考虑在报告中更明确展示时间衰减加权口径与样本范围
- 视样本情况决定是否在报告中单独提示“上市公告书未识别，已回退招股文件”

### 第三优先级：继续补强 PDF 解析边界

- 观察 `招股意向书` 与 `招股说明书` 在可比公司章节上的文本差异
- 评估是否需要补充“仅有公司名称、没有标准证券代码”的可比公司处理策略
- 继续提高上市公告书文本抽取稳定性

### 第四优先级：后续再恢复 Wind

在用户重新明确开启 Wind 之前，继续保持以下原则：

- 不把 Wind 接入主流程
- 不把 Wind 当作当前阶段阻塞项
- `wind_helper.py` 继续保持隔离与可插拔设计
