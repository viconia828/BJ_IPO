# 北交所新股估值工具说明

当前版本已完成第一阶段无 Wind 主干，可实现：

- 输入北交所新股代码
- 读取 `策略参数.txt`
- 从东方财富接口获取 IPO 基础信息与近期样本
- 基于方法二生成首日估值
- 从 `公告文件` 中提取招股说明书 / 招股意向书里的公司简介、可比公司代码
- 从本地 `首日分时走势` CSV 计算走势评分并纳入方法二
- 输出 Markdown 报告到 `输出` 目录

## 使用方式

命令行运行：

```powershell
python bse_ipo_valuation.py 920012
```

或双击 `运行.bat` 后输入代码。

## 当前边界

- `wind_channel` 默认是 `disabled`，当前不接入 Wind
- `trend_scorer.py` 默认已启用本地 CSV 评分，并按“仅使用上市日早于标的的样本”计算走势因子
- 方法二与走势评分默认已切换到时间衰减中位数，越接近标的上市日的历史样本权重越高
- `pdf_parser.py` 提供可选解析，未安装 PDF 依赖或无 PDF 时会自动降级
- 老股转让当前优先读取上市公告书，若未识别到明确表述会回退到招股文件；上市公告书识别规则仍会继续随样本补充而增强
- 如果行业无法自动识别，建议在 `策略参数.txt` 中填写 `stock_industry = 一级行业 / 二级行业`

## 输出结果

报告输出目录：

```text
输出\{代码}_{简称}_估值_{YYYYMMDD}.md
```

## 下一阶段

- 继续补充更多招股说明书与上市公告书样本，增强老股转让识别
- 继续完善报告细节与样本统计展示
- 在用户重新开启后恢复 Wind 可比公司估值
