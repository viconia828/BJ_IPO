# 北交所新股上市首日估值框架 — 实施计划

> 交付物：可运行的 Python 工具，输入新股代码 → 输出一页纸 Markdown 估值报告
> 配套文档：`方案文档_北交所新股上市首日估值框架.md`（同目录，包含完整数据源字段、公式、模板）

---

## 一、项目背景概要

用户输入一只北交所新股代码（如 920012），脚本自动完成三件事：

1. **Part 1** — 展示新股主要信息（发行价、PE、上市日期、流通盘等）
2. **Part 2** — 生成公司简介、列出可比公司、展示估值对比表
3. **Part 3** — 用两种方法计算首日合理价格，加权得出综合估值区间

数据来源优先级：**东方财富 API（主） → 北交所公告 PDF（补） → Wind 双通道（可比公司估值专用）**

---

## 二、目录结构

```
桌面\北交所新股估值\
│
├── bse_ipo_valuation.py          # 主脚本（入口）
├── config_loader.py              # 策略参数.txt 加载
├── data_fetcher.py               # 东方财富 API 封装
├── wind_helper.py                # Wind 双通道（API + Excel 回退）
├── valuation_engine.py           # 估值计算（方法一 + 方法二 + 综合）
├── report_generator.py           # Markdown 报告生成
├── pdf_parser.py                 # 公告 PDF 解析（老股转让 / 可比公司 / 公司简介）
├── industry_mapping.py           # 开源行业分类映射
├── trend_scorer.py              # WSI 分时走势评分（六维评分 + 四类形态 + 双因子走势因子）
│
├── 策略参数.txt                   # 用户可编辑的全部参数
│
├── 首日分时走势\                  # WSI 分时数据 CSV 缓存（Wind 获取后自动保存，或手动放入）
├── 公告文件\                      # 用户手动放入 PDF（可选）
├── 输出\                          # 生成的估值报告
│
├── 运行.bat                       # 一键运行
├── 说明文档.md                    # 使用说明
└── 方案文档_北交所新股上市首日估值框架.md  # 本项目详细设计文档（已有）
```

---

## 三、实施步骤（按顺序执行）

### 步骤 1：创建 `策略参数.txt`

直接从方案文档第七章复制完整模板即可。要点：

- 格式为 `key = value`，`#` 开头为注释
- 底部有 `[industry_mapping]` 段，存放申万二级行业 → 开源一级行业映射
- 所有数值参数带注释说明含义和单位

### 步骤 2：实现 `config_loader.py`

**职责**：读取 `策略参数.txt`，返回参数字典。

**要点**：
- 解析 `key = value` 格式，自动做类型转换（字符串/浮点/整数）
- 识别 `[industry_mapping]` 段，单独解析为字典
- `comparable_companies` 为空时返回空列表，非空时按逗号分割为列表
- `old_shares_transfer` 支持三种值：`auto` / 具体数字 / `0`
- 提供 `load_params(filepath)` 函数，返回一个 dict

### 步骤 3：实现 `data_fetcher.py`

**职责**：封装东方财富 API 调用，获取新股信息和近期新股列表。

**核心接口信息**：

```
URL:  https://datacenter-web.eastmoney.com/api/data/v1/get
方法: GET, 返回 JSON
无需鉴权，但需带 headers:
    User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64)
    Referer: https://data.eastmoney.com/

固定参数:
    reportName = RPTA_APP_IPOAPPLY
    columns    = ALL
    source     = WEB
    client     = WEB
```

**需要实现的函数**：

#### `fetch_ipo_info(code: str) -> dict`
- 查询指定新股：`filter=(SECURITY_CODE="{code}")`
- 返回该新股的全部字段（一条记录的 dict）
- 字段名和含义见方案文档附录 A

#### `fetch_recent_ipos(months: int = 3) -> list[dict]`
- 查询近 N 月已上市北交所新股：`filter=(MARKET_TYPE="北交所")(LISTING_DATE>"{date}")`
- `date` = 当前日期往前推 `months` 个月
- `sortColumns=LISTING_DATE`，`sortTypes=-1`
- 需要注意分页：`pageSize=50` 一般足够，如不够需翻页
- 只返回已上市（`LISTING_DATE` 非空且 `CLOSE_PRICE` 非空）的记录

**注意事项**：
- filter 中的中文和引号需要 URL 编码（用 `urllib.parse.quote`）
- 实测 API 稳定可用，无频率限制，但建议加 1 秒延迟
- `pageNumber` 从 1 开始

### 步骤 4：实现 `industry_mapping.py`

**职责**：将新股映射到开源证券北交所行业分类（一级 + 二级）。

**开源证券分类体系**：5 大一级行业、21 条二级产业链（来源：开源证券北交所策略周报）

| 一级行业 | 二级行业 |
|----------|----------|
| 高端装备 | 机械设备、汽车零部件、金属制品业、国防军工、电力设备、仪器仪表制造业、电池 |
| 信息技术 | 电子、技术服务、通信、半导体制造 |
| 化工新材 | 化学制品、金属新材料、非金属材料、橡胶和塑料制品业、电池材料、纺织制造、专业技术服务业 |
| 消费服务 | （周报未逐一披露，按实际标的归类） |
| 医药生物 | 中药、医疗器械、生物制品 |

**实现方式**：
- 读取 `策略参数.txt` 中的 `[industry_mapping]` 段
- 映射格式为 `申万行业名 = 开源一级 / 开源二级`（如 `半导体 = 信息技术 / 半导体制造`）
- 解析时按 `/` 拆分，左侧为一级，右侧为二级
- 提供 `get_industry(sw_industry_name: str) -> tuple[str, str]` 函数，返回 `(一级, 二级)`
- 映射不到时返回 `("未分类", "未分类")`
- 方法二行业统计时，按**一级行业**分组（5 大行业：高端装备/信息技术/化工新材/消费服务/医药生物）；如果同一级样本 < `min_industry_samples`，退回全市场

**行业名称获取**：

东方财富 API 没有直接给出开源证券分类。处理方式（三级优先级）：
1. 优先让用户在 `策略参数.txt` 中填写 `stock_industry = 信息技术 / 半导体制造`
2. 如果为 `auto`，使用 Wind GICS L2 + CSRC L2 自动映射至开源一级行业（已验证映射表见下方）
3. 实在无法判断时，用全市场数据替代行业数据

**已验证的 Wind GICS/CSRC → 开源一级行业映射（19 只样本）**：

| GICS L2 / CSRC L2 | 映射至开源一级行业 | 样本 |
|---|---|---|
| 资本货物 / 机械设备仪表 | 高端装备 | 920011 晨光电机、920119 美德乐、920188 悦龙科技 |
| 汽车与汽车零部件 | 高端装备 | 920055 隆源股份、920168 通宝光电、920086 科马材料、920187 通领科技 |
| 技术硬件与设备 / 半导体 | 信息技术 | 920012 创达新材、920181 赛英电子 |
| 材料（改性塑料/铝合金等） | 化工新材 | 920028 新恒泰、920078 族兴新材、920076 国亮新材 |
| 耐用消费品与服装 / 农业节水 | 消费服务 | 920183 海菲曼、920159 农大科技、920036 觅睿科技 |
| 医疗保健设备与服务 | 医药生物 | 920166 海圣医疗、920069 普昂医疗、920050 爱舍伦、920180 爱得科技 |

> 映射依据：Wind GICS L2 + CSRC L2 + 开源证券附录 C 二级行业列表。Wind 对北交所股票不返回申万行业，但 GICS 和 CSRC 可用。

### 步骤 4.5：实现 `trend_scorer.py`

**职责**：获取分时数据（CSV 缓存优先 → Wind WSI），对近期新股做六维走势评分 + 四类形态分类，输出双因子加权走势因子供方法二使用。

**核心设计**（详见方案文档第九章）：

#### 数据获取（三级优先级）

| 优先级 | 来源 | 说明 |
|--------|------|------|
| 1 | 本地 CSV 缓存 | `首日分时走势\{code}.csv`，GBK 编码，含 DateTime/open/high/low/close/volume/amount |
| 2 | Wind WSI API | `w.wsi("{code}.BJ", "open,high,low,close,volume,amt", "{date} 09:30:00", "{date} 15:00:00")` |
| 3 | 跳过 | 两者均不可用时跳过该股票 |

- Wind WSI 成功获取后，自动保存为 CSV 到 `首日分时走势\` 目录（下次直接走缓存）
- **关键：WSI 接口必须使用 `.BJ` 后缀**（如 `920012.BJ`），与 `wsd` 的 `.NQ` 不同
- 正常应返回 242 根 bar

#### 四类走势形态分类

按以下优先级判定（满足即停止）：

```
1. D-贴线震荡：振幅 < 15% AND |C/VWAP - 1| < 3%
2. A-高开回落：HiPos < 0.10（开盘即巅峰）
3. B-冲高维持：HiPos >= 0.15 AND C/VWAP >= 0 AND C/High >= 75%
4. C-冲高回落：HiPos >= 0.15 AND (C/VWAP < 0 OR C/High < 75%)
5. 以上均不满足 → 按 C/VWAP 正负归入 B 或 C
```

走势强度排序：**B > D > C > A**（已通过 19 只样本验证）

#### 评分架构：`TrendDimension` 基类 + 6 个子类

```
TrendDimension（基类）
  ├── CloseVWAPDimension        # 维度一：收盘强度（权重 25%）— C/VWAP 偏离度
  ├── PriceRetentionDimension   # 维度二：价格保持度（权重 20%）— C/High
  ├── HighTimingDimension       # 维度三：高点时序（权重 20%）— HiPos
  ├── ClosingMomentumDimension  # 维度四：尾盘动量（权重 15%）— Last 30m 涨幅
  ├── VolumeRhythmDimension     # 维度五：成交节奏（权重 10%）— 前半量占比
  └── TurnoverDimension         # 维度六：分时换手率（权重 10%）— 全天换手 × 开盘集中度
```

每个子类实现 `score(bars: dict) -> float`，返回 0-100 分。评分映射详见方案文档 9.3 节。

#### 评分入口：`TrendScorer` 类

- `score_single(code_bj: str, date: str) -> float`：对单只股票评分，返回 0-100 综合分
- `score_batch(stocks: list[tuple[str, str]]) -> dict`：批量评分，返回 `{code: score}` 字典

#### 对外接口：`get_trend_factor(industry: str, recent_ipos: list[dict], params: dict) -> float`

**双因子加权模型**：

```
走势因子 = industry_trend_weight(0.60) × 行业走势因子
         + market_sentiment_weight(0.40) × 市场情绪因子
```

- **行业走势因子**：取近期 `trend_score_stocks` 只同行业新股 → 逐只六维评分 → 取中位数 → 映射（≥70 → 1.05，40-70 → 1.0，<40 → 0.95）
- **市场情绪因子**：取近期 `trend_score_stocks` 只全市场新股（不限行业）→ 同样六维评分 → 取中位数 → 映射
- 同行业样本不足时，行业走势因子退回全市场

**后续改进（待实施）**：当前取中位数为等权，后续改为按上市日期距今远近做时间衰减加权（越近权重越大），使行业走势因子和基础涨幅更灵敏地反映最新市场温度。同理适用于 VWAP 涨幅中位数的计算。

**容错处理**：
- Wind 未登录且无本地 CSV 缓存 → 返回 1.0（中性因子），不影响主流程
- 个别股票 WSI 返回错误码（如 -40520007，已知 920086、920050 存在此问题）→ 先查 CSV 缓存，无则跳过
- `recent_ipos` 为空 → 直接返回 1.0

**注意事项**：
- WSI 用 `.BJ` 后缀，不是 `.NQ`（这是已验证的结论，与 wsd 不同）
- Wind API 的 `w.wsi()` 调用需要 Wind 终端已登录
- CSV 文件为 GBK 编码（Wind 导出默认），读取时需 `encoding='gbk'`
- 评分权重和阈值从 `params` 读取（来自策略参数.txt），支持用户自定义
- 换手率维度需从东方财富 API 的 `TURNOVERRATE` 字段获取全天换手率

### 步骤 5：实现 `pdf_parser.py`

**职责**：从北交所公告 PDF 中提取三类信息。

**依赖库**：`pdfplumber`（优先）或 `PyPDF2`

**需要实现的函数**：

#### `extract_old_shares(pdf_path: str) -> float`
- 从上市公告书 PDF 提取老股转让数量（万股）
- 搜索关键词："老股转让"、"存量股份发售"、"原股东公开发售"、"存量发行"
- 提取关键词附近的数值（注意单位：万股 / 股）
- 未找到返回 0.0

#### `extract_comparable_companies(pdf_path: str) -> list[dict]`
- 从招股说明书摘要 PDF 提取可比公司
- 搜索"可比公司"、"同行业上市公司"、"对标公司"段落
- 正则匹配股票代码：`\d{6}\.(SH|SZ|BJ)` 或上下文中的纯 6 位数字 + 公司名称
- 返回 `[{"code": "600519.SH", "name": "贵州茅台"}, ...]`

#### `extract_business_desc(pdf_path: str) -> str`
- 从招股说明书摘要 PDF 提取公司业务描述
- 搜索"主营业务"、"发行人基本情况"、"公司简介"章节
- 提取 500-1000 字的原始文本，后续由 `report_generator` 压缩为 150-200 字

**注意事项**：
- 公告文件目录可能为空（用户没放 PDF），所有函数需要优雅处理文件不存在的情况
- PDF 命名约定：`{代码}_招股说明书摘要.pdf`、`{代码}_上市公告书.pdf`
- 北交所 PDF 多为扫描件，如果 pdfplumber 提取不到文字，提示用户

### 步骤 6：实现 `wind_helper.py`

**职责**：获取沪深可比公司的估值数据（PE/PB/收盘价/市值），提供 API 和 Excel 双通道。

**关键前提**：
- Wind Python API（WindPy）安装路径：`C:\Wind\Wind.NET.Client\WindNET\x64`
- 运行时需要 `sys.path` 加入该路径，或通过 bat 设置 `PYTHONPATH`
- **Wind API 仅对沪深股票（.SH / .SZ）有效**，北交所股票（.NQ 后缀）大部分字段返回 None
- 北交所股票在 Wind 中的后缀是 `.NQ`，不是 `.BJ`

**需要实现的函数**：

#### `get_comparable_valuations(codes: list[str], channel: str = "auto") -> list[dict]`

参数：
- `codes`：可比公司代码列表，如 `["600519.SH", "000858.SZ"]`
- `channel`：`auto` / `api_only` / `excel_only`（来自策略参数 `wind_channel`）

返回：
```python
[
    {"code": "600519.SH", "name": "贵州茅台", "close": 1453.96, "pe_ttm": 20.22, "pb_lf": 7.85, "mkt_cap": 18267.5},
    ...
]
```

**API 通道逻辑**：
```python
import sys
sys.path.insert(0, r"C:\Wind\Wind.NET.Client\WindNET\x64")
from WindPy import w
w.start()

# wsd 获取最近交易日数据
data = w.wsd(",".join(codes), "close,pe_ttm,pb_lf,mkt_cap_ard", end_date, end_date)
# mkt_cap_ard 单位是元，需 / 100000000 转为亿

# 逐字段检查是否有 None，有则该公司标记为需要 Excel 回退
w.stop()
```

**Excel 回退通道逻辑**：
- 用 `openpyxl` 生成 `可比公司估值.xlsx`
- A 列写入代码，B-G 列写入 Wind Excel 函数公式字符串（注意：openpyxl 写公式时直接写 `=W.WSS(A1,"pe_ttm")` 即可）
- 打印提示："请用 Excel 打开 可比公司估值.xlsx，等待 Wind 公式计算完成后保存关闭"
- 等待用户操作后（input("按回车继续...")），重新读取 xlsx 获取数值
- Excel 函数格式见方案文档 2.3 节

**注意事项**：
- Wind API 连接可能失败（未登录客户端），需要 try-except 捕获
- `w.start()` 返回值检查：`.ErrorCode == 0` 才算成功
- Wind API 超时设置建议 30 秒

### 步骤 7：实现 `valuation_engine.py`

**职责**：两种估值方法 + 综合定价，是核心计算模块。

**需要实现的函数**：

#### `method1_comparable(issue_price, issue_pe, comparable_data, params) -> dict`

可比公司对比估值法：

```
新股 EPS = issue_price / issue_pe
可比 PE 中位数/均值 = 取 comparable_data 中所有公司的 pe_ttm（剔除负值）
目标 PE = 可比 PE 统计值 × bse_discount_factor
目标价 = EPS × 目标 PE
涨幅 = (目标价 - issue_price) / issue_price × 100
```

参数来源：`bse_discount_factor`、`comparable_pe_stat`（median/mean）

返回：`{"target_price": float, "change_pct": float, "eps": float, "comp_pe": float, "target_pe": float}`

#### `method2_industry_momentum(issue_price, issue_pe, industry_pe, float_shares, industry, recent_ipos, params) -> dict`

行业新股综合折溢价法：

```
步骤1：从 recent_ipos 中筛选同行业新股，取首日涨幅中位数作为基础涨幅
       优先按二级行业匹配（如"化学制品"）；同二级样本不足则退回一级行业（如"化工新材"）
       如果同一级行业样本仍 < min_industry_samples，用全部 recent_ipos
步骤2：计算三个调节因子
       - 流通盘因子：float_shares < float_size_threshold → 1 + small_cap_premium，否则 1.0
       - PE 因子：pe_ratio = issue_pe / industry_pe
                  < pe_low_threshold → 1 + pe_discount_boost
                  > pe_high_threshold → 1 + pe_premium_drag
                  否则 1.0
       - 走势因子（双因子加权）：调用 trend_scorer.get_trend_factor(industry, recent_ipos, params)
                  行业走势因子(60%): 近期同行业新股 → 六维评分 → 中位数 → 映射
                  市场情绪因子(40%): 近期全市场新股 → 六维评分 → 中位数 → 映射
                  走势因子 = 0.6 × 行业走势因子 + 0.4 × 市场情绪因子
                  Wind 不可用且无 CSV 缓存时退回 1.0
步骤3：预期涨幅 = 基础涨幅 × 流通盘因子 × PE因子 × 走势因子
       目标价 = issue_price × (1 + 预期涨幅 / 100)
```

返回：`{"target_price": float, "change_pct": float, "base_chg": float, "adj_factor": float, "sample_count": int, ...各因子明细}`

#### `composite_valuation(target1, target2, params) -> dict`

```
综合目标价 = target1 × weight_comparable + target2 × weight_industry_momentum
区间下限 = 综合目标价 × (1 - price_range_width)
区间上限 = 综合目标价 × (1 + price_range_width)
```

返回：`{"target_price": float, "range_low": float, "range_high": float, ...}`

#### 走势因子说明

走势因子由 `trend_scorer.py` 提供，`valuation_engine.py` 通过调用 `trend_scorer.get_trend_factor()` 获取。
详见步骤 4.5 和方案文档第九章。

#### `generate_notes(data_dict, params) -> list[str]`

根据数据特征自动生成关注提示（规则见方案文档 5.4 节），返回提示文案列表。

### 步骤 8：实现 `report_generator.py`

**职责**：将所有数据和计算结果填入 Markdown 模板，输出报告文件。

**Markdown 模板**：见方案文档第六章，完整模板可直接复制使用。

**需要实现的函数**：

#### `generate_report(all_data: dict, output_dir: str) -> str`

- `all_data` 包含：新股基本信息、可比公司数据、估值计算结果、关注提示、近期新股表现
- 填充模板中的 `{变量}` 占位符
- 可比公司表格和近期新股表格动态生成行
- 数值格式化：价格保留 2 位小数，百分比保留 1-2 位小数，大数字加逗号
- 公司简介：优先用 PDF 提取的长描述压缩到 150-200 字，否则用 API 的 `MAIN_BUSINESS`
- 输出文件命名：`{代码}_{简称}_估值_{YYYYMMDD}.md`
- 返回输出文件路径

### 步骤 9：实现 `bse_ipo_valuation.py`（主入口）

**职责**：串联所有模块，实现完整流程。

**运行流程**（对应方案文档 8.2 节的 12 步）：

```python
def main():
    # 0. 获取新股代码（命令行参数 或 input 提示输入）
    code = sys.argv[1] if len(sys.argv) > 1 else input("请输入新股代码: ").strip()

    # 1. 加载策略参数
    params = config_loader.load_params("策略参数.txt")

    # 2. 东方财富 API 获取新股信息
    ipo_info = data_fetcher.fetch_ipo_info(code)
    #    如果查不到 → 报错退出

    # 3. 获取老股转让信息
    #    old_shares_transfer == "auto" → 查 公告文件\{code}_上市公告书.pdf
    #    否则用策略参数中的数值
    #    计算首日流通盘 = TOTAL_ISSUE_NUM + old_shares

    # 4. 获取可比公司列表
    #    comparable_companies 非空 → 用策略参数指定的
    #    为空 → 查 公告文件\{code}_招股说明书摘要.pdf
    #    都没有 → 提示用户手动填入策略参数后重新运行

    # 5. Wind 获取可比公司估值
    comp_data = wind_helper.get_comparable_valuations(comp_codes, params["wind_channel"])

    # 6. 生成公司简介
    #    有招股书 → pdf_parser 提取 → 截取/压缩为 150-200 字
    #    无招股书 → 用 API 的 MAIN_BUSINESS

    # 7. 方法一计算
    result1 = valuation_engine.method1_comparable(...)

    # 8. 方法二计算（需先获取近期新股数据）
    recent_ipos = data_fetcher.fetch_recent_ipos(params["recent_months"])
    result2 = valuation_engine.method2_industry_momentum(...)

    # 9. 综合定价
    final = valuation_engine.composite_valuation(...)

    # 10. 生成关注提示
    notes = valuation_engine.generate_notes(...)

    # 11. 输出报告
    output_path = report_generator.generate_report(all_data, "输出")

    # 12. 完成
    print(f"报告已生成: {output_path}")
```

**错误处理**：
- API 请求失败 → 打印友好提示，不显示 traceback
- Wind 连接失败 → 自动切换 Excel 通道，打印提示
- 可比公司为空 → 跳过方法一，方法二权重设为 1.0
- PDF 不存在 → 跳过对应功能，使用备选数据

### 步骤 10：创建 `运行.bat` 和 `说明文档.md`

**运行.bat**：

```bat
@echo off
chcp 65001 >nul
cd /d "%~dp0"
set PYTHONPATH=C:\Wind\Wind.NET.Client\WindNET\x64;%PYTHONPATH%

echo ====================================
echo  北交所新股上市首日估值框架
echo ====================================
echo.

set /p CODE="请输入新股代码(如920012): "

"C:\Users\Administrator\AppData\Local\Programs\AlphaEngine\resources\python\python\python.exe" bse_ipo_valuation.py %CODE%

echo.
pause
```

**说明文档.md**：包含使用方法、参数说明、常见问题。

---

## 四、依赖库

| 库 | 用途 | 安装命令 |
|-----|------|---------|
| `pdfplumber` | PDF 解析 | `pip install pdfplumber` |
| `openpyxl` | Excel 读写（Wind Excel 通道） | `pip install openpyxl` |
| `numpy` | 中位数/均值计算 | 一般已预装 |
| `WindPy` | Wind API | 无需安装，已在 `C:\Wind\...\x64` |

Python 路径：`C:\Users\Administrator\AppData\Local\Programs\AlphaEngine\resources\python\python\python.exe`

安装命令前缀：`"C:\Users\Administrator\AppData\Local\Programs\AlphaEngine\resources\python\python\python.exe" -m pip install --user <package>`

---

## 五、已验证的技术细节（踩坑记录）

这些是前期调研中已经验证的结论，实现时请直接采纳，不要重新探索：

### 5.1 东方财富 API

- **已验证可用**，无需鉴权，返回 JSON
- `filter` 参数中的中文引号需要 URL 编码
- 用 `urllib.request` 或 `requests` 均可，建议带 `User-Agent` 和 `Referer`
- 返回结构：`{"result": {"data": [...], "count": N, "pages": N}}`，数据在 `result.data` 中
- 单页 50 条足够覆盖近 6 个月（约 30 只）
- 已上市新股的首日表现字段（`OPEN_PRICE`、`CLOSE_PRICE`、`LD_CLOSE_CHANGE` 等）只有上市后才有值

### 5.2 Wind API

- **北交所股票用 `.NQ` 后缀**（wsd 场景），不是 `.BJ`（.BJ 会报 invalid indicators）
- 北交所股票的 `pe_ttm`、`pb_lf`、`mkt_cap_ard` 全部返回 `None`，**不可用**
- **北交所股票的 `industry_sw`（申万行业）返回 `None`**，但 **`industry_gics`（GICS）和 `industry_csrc`（证监会行业）可正常返回**
- 行业分类获取：用 `w.wss(code, 'industry_gics', 'industryType=2')` 获取 GICS L2，再映射至开源 5 大行业（映射表见步骤 4）
- 沪深股票（`.SH` / `.SZ`）的上述字段正常返回，**可用**
- **结论：Wind API 只用于获取沪深可比公司的估值数据，不用于北交所股票本身**；但可用于获取北交所股票的 GICS/CSRC 行业分类
- **Wind wss 查北交所日频数据**需用 `.BJ` 后缀 + `tradeDate=YYYY-MM-DD` 参数（wsd 对北交所股票不可用）
- `w.start()` 需要 Wind 终端已登录
- WindPy 路径：`C:\Wind\Wind.NET.Client\WindNET\x64`
- **Wind WSI（分钟线）用 `.BJ` 后缀**，与 wsd 的 `.NQ` 不同。已验证 open/high/low/close/volume/amt 全部正常返回，每天 242 根 bar
- WSI 部分历史数据可能返回错误码 -40520007（如 920086、920050），需先查 `首日分时走势\{code}.csv` 缓存
- **CSV 缓存文件为 GBK 编码**（Wind 导出默认），读取时必须 `encoding='gbk'`，否则 UnicodeDecodeError
- CSV 格式：`DateTime,open,high,low,close,volume,amount`，末尾可能有空行需跳过

### 5.3 北交所官网

- 北交所网站是 SPA 架构，直接请求 URL 会得到空壳 HTML
- 公告搜索需要走 AJAX 接口，逆向较复杂
- **本项目暂不自动下载公告**，由用户手动放入 `公告文件\` 目录

### 5.4 北交所新股特征（截至 2026-04）

- 近 6 个月 30 只新股，**全部**采用"直接定价"方式
- 发行 PE 高度集中在 **14.99 倍**附近（监管窗口指导上限）
- 首日涨幅中位数 **170%**，均值 **270%**，破发率 **0%**
- 这些数据可用于设定参数默认值和合理性校验

### 5.5 其他

- Windows 的 `cmd.exe` 中运行 Python 时，f-string 的 `{` 会与 bat 变量冲突，需要写入 .py 文件再执行
- 输出到终端的中文需要 `chcp 65001`（UTF-8）
- 运行体验要简洁，不要显示 traceback，用 try-except 包住主流程，友好提示错误

---

## 六、测试验证

完成编码后，用以下用例验证：

### 测试用例 1：已上市新股（完整流程）

- 代码：`920012`（创达新材，2026-04-13 上市）
- 预期：能从 API 获取全部信息，报告完整生成
- 校验点：
  - 发行价 19.58，发行 PE 14.99，行业 PE 63.36
  - 首日实际收盘 51.67，涨幅 163.89%（可与模型预测对比）
  - 方法一和方法二都应产出合理目标价

### 测试用例 2：无 PDF、无手动参数（最小配置）

- 代码：`920012`
- `策略参数.txt` 中 `comparable_companies` 和 `old_shares_transfer` 均为空/auto
- `公告文件\` 目录为空
- 预期：
  - 老股转让标注"待确认"
  - 可比公司列表为空 → 方法一跳过 → 方法二权重自动设为 1.0
  - 报告仍能正常生成（仅方法二）

### 测试用例 3：手动指定可比公司

- 代码：`920012`
- `策略参数.txt` 中填入 `comparable_companies = 300829.SZ,603186.SH`
- `wind_channel = api_only`
- 预期：Wind API 获取这两家公司 PE/PB，方法一正常计算

### 测试用例 4：Wind 不可用时的 Excel 回退

- `wind_channel = excel_only`
- 预期：生成 `可比公司估值.xlsx`，提示用户打开 Excel 计算后保存，然后读取结果

### 测试用例 5：WSI 走势因子（六维 + 四类形态 + 双因子）

- 代码：`920012`（创达新材，**信息技术 / 半导体**）
- 前提：Wind 终端已登录，或 `首日分时走势\` 目录有 CSV 缓存
- 预期：
  - 走势因子非 1.0（即 WSI 模块生效，非容错退回值）
  - 走势因子在 0.95 ~ 1.05 之间
  - 六维评分正常输出（含分时换手率维度）
  - 四类走势形态分类正确（920012 应为 B-冲高维持）
  - 双因子加权：行业走势因子(60%) + 市场情绪因子(40%)
  - **基础涨幅应取信息技术组中位数**（约 130.9%），而非全市场中位数（136.8%）
- 对照：920012 走势评分 73.3（B-冲高维持），信息技术组仅 2 只（920012 + 920181），走势分中位数 63.6 → 中性因子 1.00
- 验证基准（分行业版 v2 结果）：
  - 方法二目标价 ≈ 50.59 元（预期涨幅 +158.4%），vs 实际首日收盘 51.67 元（偏差 -2.1%）
- 校验点：
  - 报告 Part 3 方法二中走势因子列应显示具体值而非 1.0
  - 终端日志应打印参与评分的股票数量、行业分组、中位数得分、行业/市场两个子因子值
  - CSV 缓存优先：若 `首日分时走势\920012.csv` 存在，应直接读取而不调用 Wind WSI

### 测试用例 6：CSV 缓存回退

- 代码：涉及 920086（科马材料）或 920050（爱舍伦）
- 这两只已知 Wind WSI 返回错误码 -40520007
- `首日分时走势\` 目录已有对应 CSV 文件
- 预期：
  - 从 CSV 缓存读取分时数据，正常计算六维评分
  - 920086 应分类为 C-冲高回落（评分约 34.2）
  - 920050 应分类为 A-高开回落（评分约 35.0）
  - 不因 Wind 错误而跳过这两只股票

---

## 七、交付检查清单

- [ ] `策略参数.txt` 已创建，参数完整
- [ ] 输入 `920012` 能正常运行，报告生成到 `输出\` 目录
- [ ] API 请求失败时不抛异常，有友好提示
- [ ] Wind 连接失败时自动切换 Excel 通道
- [ ] 无 PDF、无可比公司时报告仍能生成（降级模式）
- [ ] 终端输出简洁，无 traceback
- [ ] `运行.bat` 双击可运行
- [ ] `说明文档.md` 包含使用方法和参数说明
- [ ] WSI 走势因子正常计算（Wind 已登录时）
- [ ] Wind 未登录时走势因子退回 1.0，报告正常生成

---

---

## 八、端到端验证结果（分行业修正版 v2）

方案文档附录中对 920012 创达新材（强/B-冲高维持）和 920055 隆源股份（弱/C-冲高回落）做了完整验证，以下为核心结论：

| 指标 | 920012 创达新材 | 920055 隆源股份 |
|------|----------------|----------------|
| 行业组 | 信息技术（2只） | 高端装备（7只） |
| 基础涨幅（分行业） | 130.9% | 91.4% |
| 走势因子 | 1.00（中性） | 0.97（弱势 -5%×60%） |
| 方法二目标价 | 50.59 元 | 51.20 元 |
| 方法二偏差 | **-2.1%**（精准） | +35.4%（高估） |
| 综合偏差 | +33.1% | +60.1% |

**核心发现**：
1. 分行业修正使隆源方法二偏差从 +73.5% 降至 +35.4%（改善 38pp）
2. 创达方法二偏差始终在 ±3% 以内
3. 方法一是综合偏差的主要来源（PE 离散度大）
4. 后续优先改进：方法一异常值剔除、走势因子连续化、行业基础涨幅和走势因子引入时间衰减加权

> 完整验证过程和分组明细见方案文档附录 A。

---

*文档结束 | 2026-04-14（v2 分行业修正）*
