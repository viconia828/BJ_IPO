# Agent 工作规则

## 进程处理

- 不要随便停止不是自己发起的进程。即使看到高 CPU、长时间运行或名字类似 `python.exe`，也不能仅凭进程名判断可以结束。
- 停止进程前，必须确认该进程是当前任务中由自己明确启动的，并且已经不再需要继续运行。优先核对命令行、启动时间、工作目录和本轮操作记录。
- 如果不能确认进程归属，先向用户说明情况并询问，不要擅自 `Stop-Process`、`taskkill` 或关闭外部服务。

## 工作日志

- 每天工作要一边做一边记录工作日志，不要等到最后凭记忆补写。
- 日志应记录关键操作、重要命令、测试/验证结果、数据或参数文件变更、遇到的风险和未完成事项。
- 如果项目没有另行指定路径，默认按日期维护在 `docs/工作日志/YYYYMMDD_工作日志.md`；已有当天日志时追加更新。

## PowerShell 参数处理

- PowerShell 会把花括号、尖括号和引号作为语法的一部分处理；传递给 Git、Python 或其他命令的字面量参数时，要先判断是否需要转义或引用。
- 包含花括号的参数要用单引号保护，例如 Git stash 引用写成 `'stash@{0}'`，避免 `@{}` 或 `{}` 被 PowerShell 当成特殊语法。
- 尖括号 `<` 和 `>` 是重定向符号。不要把 Markdown 里的 `<path>`、链接或带尖括号的说明直接粘进命令；处理真实路径时优先使用引号和 PowerShell 的 `-LiteralPath`。
- 单引号表示字面量，双引号会展开变量和表达式。参数里不需要展开变量时优先使用单引号；确实需要双引号时，内部双引号用反引号转义或改用更稳妥的参数形式。
- 遇到大量 PowerShell 元字符的原生命令参数，可以谨慎使用 `--%` 停止解析；但文件操作仍优先用 PowerShell 原生命令和 `-LiteralPath`，避免拼接字符串命令。
