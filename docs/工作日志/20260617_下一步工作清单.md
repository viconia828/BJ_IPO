# 2026-06-17 下一步工作清单

## 一、第一优先级：把申购金额梯度拟合从“约束”推进到“可调模型”

当前 `allocation_fit_json` 已经提供第一版约束，但仍是粗拟合：

- 使用网上获配户数、网上发行手数、有效申购股数/冻结资金、顶格金额构造可行分布。
- 已识别 3 个“顶格仍不足正股、全员抢碎股”样本。
- 绝大多数普通样本目前是 `rough_lot_account_fit`，需要继续从粗分桶推进到更稳定的梯度反推模型。

下一步建议：

1. 明确拟合目标函数：同时约束获配户数、获配手数、冻结资金、未获配户均上限。
2. 对 1 手、2 手、3 手及以上获配户数做整数规划或启发式搜索。
3. 给每个样本输出拟合残差和置信度，低置信样本不得直接用于调参。
4. 单独处理 `top_apply_below_guaranteed=true` 场景，把它作为强标签而不是异常值。

## 二、第二优先级：修补公告解析缺口

当前样本表仍有几个可改善点：

- `920186` 缺 `ONLINE_VA_NUM`，导致 `allocation_fit_ready=false`。
- 少数样本的 `ONLINE_ISSUE_NUM` 会误命中 `100`，生成器已自动回退总发行量并写入 `parse_errors`，但解析器本身还可继续修。
- 冻结资金目前可由有效申购股数和发行价推导，但若公告直接披露金额，仍应优先解析公告原值。

建议顺序：

1. 针对 `920186` 打开发行结果公告文本，补有效申购户数正则。
2. 收窄 `ONLINE_ISSUE_NUM` 的上下文，避免命中比例/表格里的 `100`。
3. 增加一到两个真实公告文本 golden case，避免后续回归。

## 三、第三优先级：接入离线调参

样本表现在已经具备正股门槛调参基础：

- 33 条 `model_ready`
- 30 条 `guaranteed_label_ready`
- 3 条 `fractional_label_ready`
- 32 条 `allocation_fit_ready`

下一步可以新增调参脚本：

- `tools/tune_subscription_prediction.py`

首批建议调的参数：

- 有效申购倍数估计的时间衰减半衰期
- 顶格金额因子指数
- 发行规模因子指数
- 冻结天数因子指数
- 拟合分布残差惩罚

评估指标建议：

- 正股门槛金额 MAE/MAPE
- 是否进入全员抢碎股场景的分类准确率
- 碎股门槛方向性误差
- 拟合分布残差

## 四、第四优先级：报告展示继续收口

当前报告已经展示正股/碎股门槛和是否抢时间。后续可补：

- 顶格不足正股时，明确写成“顶格仍不足正股，顶格账户按时间优先获配 100 股”。
- 展示 `allocation_fit_quality`，让用户知道碎股门槛是公告直接标签、梯度表、全员抢时间标签，还是粗拟合估计。
- `_一览.txt` 可增加 `时间优先场景` 或 `拟合质量` 列，方便横向比较。

## 五、第五优先级：公告下载稳定性

本轮证明官网 `403` 多数可通过慢速重试解决。建议保留以下操作规范：

- 批量补公告使用 `--download-retries 3 --download-delay-seconds 5`。
- 若后续再遇到 403，不要马上判断代码搜索错误；先慢速重试。
- 若慢速重试仍失败，再检查上市后代码、挂牌前代码和 issue-detail 项目 ID 映射。

## 六、收工备注

下一轮开工建议先跑：

- `python tools\build_subscription_history.py`
- `python tests\validate_subscription_history_builder.py`

确认样本表状态稳定后，再进入梯度拟合调参。
