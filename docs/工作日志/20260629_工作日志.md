# 2026-06-29 工作日志

## 一、A/B 机交接口径确认

本轮确认跨机器交接策略：

- A 机当前估值结果更准，后续估值侧参数和估值样本以 A 机为主做选择性合并。
- B 机（本机）当前申购资金估算规模更准，先把 B 机的申购规模相关参数、历史样本、手工分档标签和 replay 数据带到远端，供 A 机明天择优合并。
- 公告 PDF 不作为跨机器迁移资产。两台机器都可以按需从北交所公告源重新下载。
- 当前已经从本机删除的公告 PDF 按删除处理，不再随仓库携带。
- `data/offline_tuning/` 下的离线调参样本、单样本 replay cache、上市首日均价缓存、申购历史样本和手工分档标签均作为可提交数据集跟踪。
- `首日分时走势/`、`输出/`、`data/tushare_db/`、`data/wind_db/`、`data/pdf_parse_cache/` 仍然视为本机运行缓存，不作为本轮交接内容。

## 二、本轮准备提交内容

- 更新 `.gitignore`：取消忽略 `data/offline_tuning/listing_average_prices.json`、`data/offline_tuning/replay_dataset.json` 和 `data/offline_tuning/replay_items/`，避免新增 replay 样本缓存继续漏跟踪。
- 提交 B 机当前 `策略参数.txt` 和 `自动调参记录.txt`，保留本机最近一次估值/申购调参写回记录，供 A 机选择性合并。
- 提交 B 机当前申购资金样本：
  - `data/offline_tuning/subscription_history_sample.csv`
  - `data/offline_tuning/subscription_ladder_labels.csv`
- 提交 B 机当前估值 replay 数据：
  - `data/offline_tuning/replay_dataset.json`
  - `data/offline_tuning/replay_items/*.json`
  - `data/offline_tuning/listing_average_prices.json`
- 删除仓库中仍被跟踪的公告 PDF，后续由各机器按需重新下载。

## 三、A 机合并注意事项

明天在 A 机合并时建议先按文件类别做选择：

- 申购规模侧优先参考 B 机：`subscription_history_sample.csv`、`subscription_ladder_labels.csv`、申购相关 `subscription_prediction_*` 参数和对应自动调参记录。
- 估值侧优先参考 A 机：综合估值权重、方法二 PE 修正、趋势权重、估值 replay 结果需要结合 A 机最新估值表现再决定是否采纳。
- 公告 PDF 不要从本提交恢复；缺哪个公告，由 A 机运行主程序或下载工具重新拉取。
- 合并完成后建议先跑一次 `git status --short`，确认除本机缓存外无意外未提交状态，再分别复核一只估值样本和一只申购规模样本。
