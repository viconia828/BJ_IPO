# 2026-07-11 工作日志

## 雪球首日估值语料可访问性探测

用户提供四个雪球专栏作者，希望读取其“上市估值、上市前瞻、首日价格分析、首日股价”类文章，抽象成可复用的首日估值 skill，并用本地数据验证。

本轮先做可访问性探测：

- 直接 `curl -I` 四个作者页均返回 HTTP 200。
- 普通 `curl -L` 返回阿里云 WAF 挑战页，不是正文。
- 雪球公开时间线接口在普通 HTTP 会话下同样被 WAF 挡住。
- 尝试 Codex 内置浏览器时，当前浏览器控件无法创建空白标签页，报 webview attach 超时，未继续依赖该通道。
- 使用本机 Chrome + `playwright-core` 渲染，在提权联网后成功读取雪球主页、时间线接口和详情页正文。

新增探测脚本：

- `tools/xueqiu_access_probe.cjs`

新增探测输出：

- `outputs/xueqiu_access_probe.json`

首轮探测结果：

- `8889879564`：识别为“条条道路通罗马Lee”，前 5 页命中 22 篇，详情页可读。
- `8692639756`：识别为“无房户小侯”，前 3 页命中 13 篇，详情页可读。
- `9833039947`：识别为“月半928”，前 2 页命中 6 篇，详情页可读。
- `8851207271`：识别为“兔子兔888”，前 5 页命中 94 篇，详情页可读。

关键结论：

- 后续采集可以使用本机 Chrome + Playwright 渲染，不需要登录。
- 页面仍显示“登录”按钮，但正文不是登录墙。
- 应将网页采集逻辑保持为独立工具，不并入正式估值主流程。

## 设计文档

新增设计文档：

- `docs/设计文档/09_雪球首日估值语料采集与Skill方案.md`

设计要点：

- 先建立可复现的雪球语料采集器，再抽象首日估值 skill。
- 语料保存到 `data/xueqiu_corpus/`，包括单篇 JSON、JSONL 汇总和索引文件。
- 后续 repo-local skill 计划放在 `skills/bj-ipo-first-day-valuation/`。
- 回测必须防止信息泄漏：上市后评论和收评不得作为上市前预测规则。

## 语料采集器

新增采集脚本：

- `tools/xueqiu_corpus_collect.cjs`

采集器实现要点：

- 使用本机 Chrome + `playwright-core` 访问雪球作者页、时间线接口和文章详情页。
- 支持 `--users`、`--keywords`、`--max-pages`、`--max-articles-per-author`、`--stop-empty-pages`、`--delay-ms`、`--refresh`、`--refresh-unreadable`、`--headful`。
- 输出单篇 JSON、`articles.jsonl`、`index.json` 和运行报告。
- 正文清洗会去掉登录、分享、评论、作者签名等页脚噪声。
- 字段抽取覆盖发行价、目标 PE、流通市值/总市值、首日价格区间、可比公司、首日判断、风险短语和作者规则短语。
- 遇到雪球滑块验证或 WAF 文案时，停止当前作者采集并标记 `blocked_by_verification`，不尝试绕过验证。
- 汇总时合并已缓存的可读文章，避免一次验证页导致历史可读语料丢失。

## 首轮四作者语料结果

最终四作者汇总命令：

```powershell
C:\Users\Ai\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe tools\xueqiu_corpus_collect.cjs --max-pages 20 --max-articles-per-author 200 --stop-empty-pages 3 --delay-ms 200
```

最终输出：

- `data/xueqiu_corpus/index.json`
- `data/xueqiu_corpus/articles.jsonl`
- `outputs/xueqiu_corpus_collect_20260711_152828.json`

最终统计：

- 作者数：4
- 候选文章：77
- 可读文章：75
- 被验证页挡住：2
- 抽到发行价：54
- 抽到首日价格区间：35
- 抽到目标 PE：21
- 文章类型：`first_day_price_analysis` 13 篇，`listing_valuation` 56 篇，`listing_preview` 6 篇，`other` 2 篇。
- 作者分布：无房户小侯 13 篇，兔子兔888 36 篇，条条道路通罗马Lee 22 篇，月半928 6 篇。
- 本轮从可读缓存合并：27 篇。

说明：

- `data/xueqiu_corpus/articles/` 保留单篇缓存，包含历史采集时留下的不可读或未进入本轮索引的文件。
- 后续规则抽象和验证以 `data/xueqiu_corpus/index.json` 与 `articles.jsonl` 为准。

## Repo-local Skill

新增首日估值 skill 骨架：

- `skills/bj-ipo-first-day-valuation/SKILL.md`
- `skills/bj-ipo-first-day-valuation/references/corpus_schema.md`
- `skills/bj-ipo-first-day-valuation/references/heuristic_taxonomy.md`
- `skills/bj-ipo-first-day-valuation/references/local_field_mapping.md`
- `skills/bj-ipo-first-day-valuation/agents/openai.yaml`

当前 skill 定位：

- 指导雪球首日估值文章采集。
- 约束作者经验规则抽象方式。
- 要求每条规则映射到本地字段并通过上市前可得性检查。
- 后续用于把作者规则与本地 replay/offline tuning 数据做对照验证。

## 验证

已执行：

```powershell
C:\Users\Ai\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --check tools\xueqiu_corpus_collect.cjs
```

结果：采集脚本语法检查通过。

已执行 skill 结构手工检查，确认：

- `SKILL.md` frontmatter 存在。
- `agents/openai.yaml` 存在。
- 三个 `references/*.md` 存在。

补充校验：

- 用户授权后执行 `python -X utf8 -m pip install PyYAML`，当前用户级 Python 环境已存在 `PyYAML 6.0.3`。
- 已执行 `python -X utf8 C:\Users\Ai\.codex\skills\.system\skill-creator\scripts\quick_validate.py skills\bj-ipo-first-day-valuation`。
- 结果：`Skill is valid!`

## 首轮本地验证

新增验证脚本：

- `tools/validate_xueqiu_author_ranges.py`

脚本验证口径：

- 只验证文章中可抽出的显式首日价格区间。
- 按标题标的和 `BJ920xxx` 股票标签映射到本地 replay 样本。
- 多标的前瞻文章按标的分段抽取，避免把一个标的的价格区间挂到另一个标的上。
- 排除发布时间不早于上市日的文章。
- 首日均价优先使用 replay `AVERAGE_PRICE`，缺失时只读本地首日分时 CSV 计算。

验证命令：

```powershell
python -X utf8 tools\validate_xueqiu_author_ranges.py
```

最终报告：

- `outputs/xueqiu_author_range_validation_20260711_154243.md`
- `outputs/xueqiu_author_range_validation_20260711_154243.json`

结果：

- 预测行数：53。
- 可评估预测：51。
- 行级区间命中：15/51，命中率 29.4%。
- 覆盖本地代码：23 只。
- 至少一次作者区间命中的代码：10 只，代码级命中率 43.5%。
- 同代码 baseline 对照：3/22，命中率 13.6%。
- 同代码 2026-07-10 扫描最优候选对照：7/22，命中率 31.8%。

分作者：

- 兔子兔888：6/12，50.0%。
- 条条道路通罗马Lee：6/15，40.0%。
- 无房户小侯：2/6，33.3%。
- 月半928：1/18，5.6%。

初步结论：

- 作者显式价格区间包含可用信号，但分作者差异很大。
- 月半928多标的前瞻偏基本面合理估值，未充分反映首日情绪溢价，不能直接当首日均价区间。
- 后续应将作者规则拆成估值锚、情绪溢价、流通供给、题材热度和风险过滤，再与现有模型组合验证。

## 2025-12 起扩展采集与覆盖审计

用户确认本地样本均为 2026 年样本，可按发文日期从 2025-12 开始检查。为避免标题关键词漏召回，本轮扩展采集关闭关键词过滤，并从 2025-12-01 起翻取四位作者时间线。

扩展采集命令：

```powershell
C:\Users\Ai\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe tools\xueqiu_corpus_collect.cjs --since-date 2025-12-01 --no-keyword-filter --max-pages 80 --max-articles-per-author 1000 --stop-empty-pages 999 --delay-ms 500 --refresh-unreadable
```

新增采集器能力：

- 支持 `--since-date <YYYY-MM-DD>`，按发文日期停止或过滤时间线文章。
- 支持 `--no-keyword-filter`，用于按日期全量召回作者文章。
- `--refresh-unreadable` 同时刷新不可读缓存和已识别为验证/WAF 的缓存。
- 增强验证页识别，覆盖“安全威胁”“访问被阻断”“请求ID”“当前网址”等文案。

扩展采集报告：

- `outputs/xueqiu_corpus_collect_20260711_155804.json`

扩展采集统计：

- 作者数：4。
- 汇总文章：136 篇。
- 可读文章：134 篇。
- 识别到验证/阻断页：17 篇。
- 抽到发行价：87 篇。
- 抽到首日价格区间：50 篇。
- 抽到目标 PE：37 篇。
- 文章类型：`listing_valuation` 73 篇，`subscription_strategy` 21 篇，`first_day_price_analysis` 13 篇，`listing_preview` 5 篇，`other` 24 篇。
- 作者分布：无房户小侯 48 篇，兔子兔888 52 篇，条条道路通罗马Lee 23 篇，月半928 13 篇。
- 本轮合并历史可读缓存：37 篇。

新增覆盖审计脚本：

- `tools/audit_xueqiu_local_sample_coverage.py`

覆盖审计报告：

- `outputs/xueqiu_local_sample_coverage_20260711_155832.md`
- `outputs/xueqiu_local_sample_coverage_20260711_155832.json`

覆盖审计结论：

- 本地样本数：42。
- 当前语料/缓存文章记录：180。
- 已发现四作者上市前预测的本地样本：39。
- 其中已抓到上市前可读正文：30。
- 只有候选/验证页但正文缺失：9。
- 有显式价格区间的本地样本：30。
- 当前没有做到“四位作者预测过的本地样本全部抓到正文”。仍缺正文的 9 只为：`920086 科马材料`、`920076 国亮新材`、`920159 农大科技`、`920180 爱得科技`、`920166 海圣医疗`、`920168 通宝光电`、`920187 通领科技`、`920078 族兴新材`、`920028 新恒泰`。
- 当前四作者语料/缓存未发现 `920050 爱伦医疗` 的预测证据。
- `920117 龙鑫智能` 只找到非上市前或非预测类文章，本轮不纳入 author-rule score。

分作者覆盖：

- 兔子兔888：已发现预测代码 38，可读正文代码 19，显式区间代码 19；早期样本仍有较多详情页被验证/阻断。
- 条条道路通罗马Lee：已发现预测代码 21，可读正文代码 21，显式区间代码 15。
- 无房户小侯：已发现预测代码 12，可读正文代码 12，显式区间代码 7。
- 月半928：已发现预测代码 26，可读正文代码 26，显式区间代码 26。

扩展采集后的显式区间验证报告：

- `outputs/xueqiu_author_range_validation_20260711_155833.md`
- `outputs/xueqiu_author_range_validation_20260711_155833.json`

验证结果：

- 预测行数：68。
- 可评估预测：66。
- 行级区间命中：17/66，命中率 25.8%。
- 覆盖本地代码：29 只。
- 至少一次作者区间命中的代码：12 只，代码级命中率 41.4%。
- 同代码 baseline 对照：3/27，命中率 11.1%。
- 同代码 2026-07-10 扫描最优候选对照：8/27，命中率 29.6%。

## Author-Rule Score 首版

新增评分脚本：

- `tools/evaluate_xueqiu_author_rule_score.py`

评分口径：

- 只使用上市前、预测类、正文可读且能抽到显式价格区间的作者证据。
- 当前不把验证页/阻断页样本纳入评分。
- 作者权重：兔子兔888 1.0，条条道路通罗马Lee 0.9，无房户小侯 0.7，月半928 0.35。
- `作者原始加权区间` 使用作者 low/high 的加权平均；`作者中枢 ±10%` 用于与现有固定宽度估值口径对比。

评分报告：

- `outputs/xueqiu_author_rule_score_20260711_160221.md`
- `outputs/xueqiu_author_rule_score_20260711_160221.json`

评分结果：

- 可评分本地代码：30。
- 作者原始加权区间命中：11/29，命中率 37.9%。
- 作者中枢 ±10% 命中：10/29，命中率 34.5%。
- 与当前模型同代码交集 27 只中，作者中枢 ±10% 命中 10/27，命中率 37.0%。
- 同代码 baseline 命中 3/27，命中率 11.1%。
- 同代码 2026-07-10 扫描最优命中 8/27，命中率 29.6%。

排序观察：

- author score 与实际首日均价涨幅 Spearman：0.873。
- 低分组平均/中位实际涨幅：104.6% / 94.9%。
- 高分组平均/中位实际涨幅：465.3% / 450.1%。
- 这说明当前 author-rule score 的更强价值可能在“首日强弱排序/情绪强度分层”，不只是固定区间命中。

## Author-Rule 与本地估值组合验证

新增组合验证脚本：

- `tools/evaluate_xueqiu_author_model_blend.py`

脚本口径：

- 只读本地 replay 数据、调参扫描报告和已生成的 author-rule score，不写回 `策略参数.txt`。
- 使用当前参数模型、2026-07-10 扫描最优参数、作者显式区间作为基准。
- 扫描本地模型目标价与作者中枢的线性融合，`alpha` 表示作者权重。
- 扫描区间宽度 `±8%/±10%/±12%/±15%/±20%`。
- 分别测试模型不可用时是否允许用作者区间兜底。
- 输出全样本命中率、可用样本命中率、MAE、Spearman 和逐样本明细。

生成报告：

- `outputs/xueqiu_author_model_blend_scan_sample_20260711_161509_496929.md`
- `outputs/xueqiu_author_model_blend_scan_sample_20260711_161509_496929.json`
- `outputs/xueqiu_author_model_blend_author_scored_20260711_161515_071510.md`
- `outputs/xueqiu_author_model_blend_author_scored_20260711_161515_071510.json`
- `outputs/xueqiu_author_model_blend_all_actual_20260711_161521_340317.md`
- `outputs/xueqiu_author_model_blend_all_actual_20260711_161521_340317.json`

2026-03 起扫描样本结果（31 只）：

- 当前参数模型：3/31，全样本命中率 9.7%，可用样本命中率 12.5%，MAE(涨幅) 143.57，Spearman 0.375。
- 2026-07-10 扫描最优：8/31，全样本命中率 25.8%，可用样本命中率 33.3%，MAE(涨幅) 139.85，Spearman 0.477。
- 作者固定 ±10%：10/31，全样本命中率 32.3%，可用样本命中率 37.0%，MAE(涨幅) 65.76，Spearman 0.868。
- 作者原始加权区间：11/31，全样本命中率 35.5%，可用样本命中率 40.7%，MAE(涨幅) 65.76，Spearman 0.868。
- 最优组合为 `current_params_author_blend_a0.8_w0.20_fallback`，命中 17/31，全样本命中率 54.8%，可用样本命中率 56.7%，可用 30/31，MAE(涨幅) 72.74，Spearman 0.827。
- 固定 ±10% 约束下，当前最优仍为 `author_fixed10`，命中 10/31。
- 不启用作者兜底时，最优为 `current_params_author_blend_a0.8_w0.20`，命中 12/31，可用 24/31。

作者覆盖样本结果（27 只）：

- 当前参数模型：3/27。
- 2026-07-10 扫描最优：8/27。
- 作者固定 ±10%：10/27。
- 作者原始加权区间：11/27。
- 最优组合 `current_params_author_blend_a0.8_w0.20_fallback`：17/27，全样本命中率 63.0%，可用 27/27。

全量有实际均价样本结果（39 只）：

- 当前参数模型：4/39。
- 2026-07-10 扫描最优：9/39。
- 作者固定 ±10%：10/39。
- 作者原始加权区间：11/39。
- 最优组合 `current_params_author_blend_a0.8_w0.20_fallback`：18/39，全样本命中率 46.2%，可用样本命中率 48.6%，可用 37/39。
- 固定 ±10% 约束下，最优为 `scan_best_author_blend_a0.3_w0.10_fallback`，命中 12/39。

阶段性判断：

- 作者信号确实明显改善中枢和排序；融合模型的最佳上限来自较高作者权重和较宽区间。
- 若保持正式估值当前 ±10% 区间宽度，作者单独固定区间或低权重融合更稳，但命中率提升幅度较小。
- 作者兜底贡献显著，尤其覆盖本地模型因行业未分类/方法一不可用而无估值的样本；后续正式化时需要把兜底逻辑做成显式开关。
- 下一步不宜直接把 `±20%` 最优组合写入正式模型；更合适的是先实现“作者评分修正项 + 可配置兜底 + 宽度分层”的候选参数集，再做留出样本或新样本滚动验证。

## 手工 MHTML 补采与复核

用户将人工抓取文件放入根目录 `新建文件夹/`，共 9 个雪球 MHTML，并补充 `920050 爱伦医疗` 上市时名称为 `爱舍伦`。

新增/调整：

- 新增 `tools/import_manual_xueqiu_mhtml.py`，支持从手工保存的雪球 MHTML 中解析正文、标题、作者、发布时间、URL、发行价和首日价格区间，并写回 `data/xueqiu_corpus/`。
- 修正导入器的 `stock_mentions` 逻辑，只保留正文显式 `$名称(代码)$`，过滤页面行情组件里的匿名股票链接。该修正避免了 `科马材料/农大科技` 等文章被无关 `920081` 链接污染，从而导致价格区间窗口切分失败。
- 在 `tools/validate_xueqiu_author_ranges.py` 增加别名 `爱伦医疗 -> 爱舍伦`。

导入命令：

```powershell
python -X utf8 tools\import_manual_xueqiu_mhtml.py
```

最终导入报告：

- `outputs/xueqiu_manual_mhtml_import_20260711_163150.json`

导入结果：

- MHTML 文件：9。
- 成功导入：9。
- 错误：0。
- 补齐正文：`920086 科马材料`、`920076 国亮新材`、`920159 农大科技`、`920180 爱得科技`、`920166 海圣医疗`、`920168 通宝光电`、`920187 通领科技`、`920078 族兴新材`、`920028 新恒泰`。
- 9 篇均抽到显式 `对应价格区间`。
- 语料索引更新为 145 篇文章、143 篇可读、2 篇验证/阻断页、59 篇有价格区间。

覆盖复核报告：

- `outputs/xueqiu_local_sample_coverage_20260711_163201.md`
- `outputs/xueqiu_local_sample_coverage_20260711_163201.json`

覆盖复核结果：

- 本地样本数：42。
- 已发现四作者上市前预测样本：40。
- 已抓到上市前可读正文：39。
- 有显式价格区间：39。
- 仍缺正文：`920050 爱伦医疗`。别名生效后已定位到兔子兔888 `爱舍伦上市估值`，状态为候选但详情页仍是验证页。
- 另有 `920183 海菲曼` 只有阻断/薄缓存，尚未形成可用上市前预测证据。
- `920117 龙鑫智能` 仍为只找到非上市前或非预测类文章。

显式区间验证报告：

- `outputs/xueqiu_author_range_validation_20260711_163201.md`
- `outputs/xueqiu_author_range_validation_20260711_163201.json`

显式区间验证结果：

- 预测行数：77。
- 可评估预测：75。
- 行级区间命中：22/75，命中率 29.3%。
- 覆盖本地代码：38 只。
- 至少一位作者命中的代码：17 只，代码级命中率 44.7%。
- 同代码 baseline：3/30，命中率 10.0%。
- 同代码扫描最优：8/30，命中率 26.7%。

Author-rule score 复跑报告：

- `outputs/xueqiu_author_rule_score_20260711_163211.md`
- `outputs/xueqiu_author_rule_score_20260711_163211.json`

Author-rule score 复跑结果：

- 可评分代码：39。
- 作者原始加权区间：16/38，命中率 42.1%。
- 作者中枢 ±10%：14/38，命中率 36.8%。
- 同代码交集 30 只中，作者原始加权区间 12/30，baseline 3/30，扫描最优 8/30。
- Spearman(score, 实际首日均价涨幅)：0.893。

组合验证复跑报告：

- `outputs/xueqiu_author_model_blend_scan_sample_20260711_163212_001691.md`
- `outputs/xueqiu_author_model_blend_scan_sample_20260711_163212_001691.json`
- `outputs/xueqiu_author_model_blend_author_scored_20260711_163211_983588.md`
- `outputs/xueqiu_author_model_blend_author_scored_20260711_163211_983588.json`
- `outputs/xueqiu_author_model_blend_all_actual_20260711_163212_003480.md`
- `outputs/xueqiu_author_model_blend_all_actual_20260711_163212_003480.json`

组合验证复跑结果：

- 2026-03 起扫描样本：最佳 `current_params_author_blend_a0.8_w0.20_fallback`，19/31，全样本命中率 61.3%，可用样本命中率 63.3%。
- 作者覆盖样本：同一最佳组合，19/30，全样本命中率 63.3%。
- 全量有实际均价样本：最佳 `current_params_author_blend_a0.9_w0.20_fallback`，22/39，全样本命中率 56.4%，可用样本命中率 57.9%。
- 全量样本参考项：当前参数模型 4/39，扫描最优 9/39，作者固定 ±10% 14/39，作者原始加权区间 16/39。

阶段判断：

- 这次人工补采把原 9 个正文缺失样本全部转成可用显式区间；已知预测缺正文的问题只剩 `920050 爱伦医疗/爱舍伦`，另需留意 `920183 海菲曼` 的薄缓存状态。
- 新增早期样本后，author-rule 与本地模型融合的提升更明显；但最优组合仍依赖 `±20%` 宽区间，正式参数化前仍需做宽度分层或留出验证。

## 下一步

- 实现可复用的“作者评分修正项 + 可配置兜底 + 宽度分层”候选参数集，先作为离线观察项，不写入正式估值默认参数。
- 继续补抓 `920050 爱伦医疗/爱舍伦` 的兔子兔888正文，并检查 `920183 海菲曼` 是否存在可读上市前预测；补齐后重跑覆盖审计、author-rule score 和组合验证。
- 将显式区间之外的语义规则继续拆成估值锚、流通供给、行业热度、基本面质量、市场情绪、交易结构和风险过滤。

## 作者逻辑蒸馏启动

用户希望在不继续接入雪球外部信息的情况下，抽象作者提高准确率的逻辑，并把这部分逻辑内化到本地估值模型。

已先更新设计文档：

- `docs/设计文档/09_雪球首日估值语料采集与Skill方案.md`

本轮目标：

- 分析作者提升来自哪些样本和哪些本地可得特征。
- 将作者显式目标价作为 teacher label 做归因，但不把作者目标价或正文作为 proxy 预测输入。
- 构造第一版 `author_proxy_score`，只使用本地 replay、分时、公告解析缓存、行业映射和已有调参结果。
- 输出归因报告和 proxy 回测报告，与当前模型、扫描最优、作者融合上限并列比较。

计划新增脚本：

- `tools/analyze_xueqiu_author_logic_distillation.py`

输入基线：

- 最新 author-rule score：`outputs/xueqiu_author_rule_score_20260711_163211.json`
- 最新 blend 结果：`outputs/xueqiu_author_model_blend_scan_sample_20260711_163212_001691.json`
- 本地 replay 数据：`data/offline_tuning/replay_dataset.json`
- 当前参数：`策略参数.txt`
- 扫描最优报告：`调参/valuation_hit_rate_scan_202603plus_20260710_001437.json`

预期产物：

- `outputs/xueqiu_author_logic_distillation_*.json`
- `outputs/xueqiu_author_logic_distillation_*.md`

## 作者逻辑蒸馏脚本与结果

新增脚本：

- `tools/analyze_xueqiu_author_logic_distillation.py`

脚本能力：

- 合并本地 replay 数据、当前模型预测、扫描最优预测、author-rule score 和实际首日均价。
- 按作者增量类别拆分样本：`model_miss_author_hit`、`model_hit_author_hit`、`model_hit_author_miss`、`model_unavailable_author_hit`、`model_miss_author_miss`、`no_author`。
- 构造本地-only `author_proxy_score`，只使用本地字段：发行价、流通市值、老股比例、发行 PE/行业 PE、近端新股情绪、同业近端表现、顶格资金、模型不确定性。
- 扫描 proxy 候选：固定步长中枢修正、滚动历史校准、动态宽度分层、本地情绪兜底。

执行命令：

```powershell
python -X utf8 tools\analyze_xueqiu_author_logic_distillation.py --target all_actual
python -X utf8 tools\analyze_xueqiu_author_logic_distillation.py --target scan_sample
python -X utf8 tools\analyze_xueqiu_author_logic_distillation.py --target author_scored
```

输出报告：

- `outputs/xueqiu_author_logic_distillation_all_actual_20260711_165001_537058.md`
- `outputs/xueqiu_author_logic_distillation_all_actual_20260711_165001_537058.json`
- `outputs/xueqiu_author_logic_distillation_scan_sample_20260711_165020_731086.md`
- `outputs/xueqiu_author_logic_distillation_scan_sample_20260711_165020_731086.json`
- `outputs/xueqiu_author_logic_distillation_author_scored_20260711_165032_647799.md`
- `outputs/xueqiu_author_logic_distillation_author_scored_20260711_165032_647799.json`

全量有实际均价样本（39 只）：

- 最佳 proxy：`current_params_proxy_step0_layered_fallback`。
- 命中：16/39，全样本命中率 41.0%，可用样本命中率 41.0%。
- proxy score 与实际涨幅 Spearman：0.462。
- 对照：当前模型 4/39，扫描最优 9/39，作者固定 ±10% 14/39，作者原始加权区间 16/39。

2026-03 起扫描样本（31 只）：

- 最佳 proxy：`current_params_proxy_step0_layered_fallback`。
- 命中：13/31，全样本命中率 41.9%，可用样本命中率 41.9%。
- proxy score 与实际涨幅 Spearman：0.520。
- 对照：当前模型 3/31，扫描最优 8/31，作者固定 ±10% 11/31，作者原始加权区间 12/31，雪球融合上限 19/31。

作者覆盖样本（30 只）：

- 最佳 proxy：`scan_best_rolling_proxy_a0.75_w0.20`。
- 命中：12/30，全样本命中率 40.0%，可用样本命中率 50.0%。
- proxy score 与实际涨幅 Spearman：0.550。

归因观察：

- scan sample 中，proxy 低分组平均/中位实际涨幅为 163.2% / 125.0%，高分组为 376.6% / 317.7%，排序信号存在。
- 相关性较强的本地字段：模型不确定性、同业近端表现、近 5 只新股情绪、发行价、顶格资金。
- `model_miss_author_hit` 样本并不是简单“作者大幅上调中枢”；更多体现为近端情绪强、供给弹性和模型不确定性下的宽度/兜底价值。
- 直接用 `proxy_score * 固定步长` 修正中枢不稳定，容易推过头；滚动历史校准 MAE 更好，但命中率仍未超过动态宽度/兜底方案。

阶段判断：

- 本地-only proxy 已经能内化一部分作者收益：scan sample 从扫描最优 8/31 提升到 13/31，全量样本从扫描最优 9/39 提升到 16/39。
- 当前可正式继续推进的是“proxy 排序 + 动态宽度 + 本地情绪兜底”，不是强行学习作者中枢。
- 作者融合仍有更高上限，说明还存在本地结构化字段未覆盖的信息，需要继续从公告语义和题材标签中抽象。

## 四作者覆盖表导出

用户需要按本地样本查看四位雪球作者分别覆盖了哪些股票，用于人工查漏补缺。

新增脚本：

- `tools/export_xueqiu_author_coverage_table.py`

导出命令：

```powershell
python -X utf8 tools\export_xueqiu_author_coverage_table.py
```

输出：

- `outputs/xueqiu_author_coverage_table_20260711_165935.md`
- `outputs/xueqiu_author_coverage_table_20260711_165935.csv`
- `outputs/xueqiu_author_coverage_table_20260711_165935.json`

表格口径：

- `R`：可读上市前预测且有显式区间。
- `T`：可读上市前预测但无显式区间。
- `B`：有上市前预测候选但正文缺失/验证页。
- `N`：仅找到非上市前或非预测可读文章。
- `b`：仅阻断/薄缓存。
- `-`：未发现。

导出结果：

- 本地样本 42 行。
- 已发现上市前预测样本 40 个。
- 已抓到上市前可读正文 39 个。
- 有显式区间样本 39 个。
- 重点查漏：
  - `920050 爱伦医疗`：兔子兔888 `B`，候选为 `爱舍伦上市估值`，URL `https://xueqiu.com/8851207271/372060732`。
  - `920183 海菲曼`：兔子兔888 `b`，当前只有上市日阻断/薄缓存，需检查是否存在上市前预测。
  - `920117 龙鑫智能`：无房户小侯、月半928 为 `N`，当前仅非上市前/非预测证据，需检查是否存在上市前估值/前瞻。

## 二轮手工样本导入与隐式区间抽取

用户完成一轮人工查漏补缺，确认除龙鑫外其他标的应基本覆盖，并提示可读预测中可能存在非标准区间表达，例如“不低于 XX 元”“可能达到 XX 元”“XX-XX 倍 PE”。

本轮新增/修改：

- `tools/import_manual_xueqiu_mhtml.py` 从只导入 MHTML 扩展为同时导入 MHTML/TXT。
- TXT 导入会识别作者、用户 ID、标的代码、发文日期，并写成标准 `data/xueqiu_corpus/articles/<user>_<status>.json`。
- `tools/validate_xueqiu_author_ranges.py` 增加隐式区间抽取：
  - `不低于 XX 元` -> `[XX, 1.2 * XX]`。
  - `可能达到/目标价/中枢价 XX 元` -> `XX ±10%`。
  - `XX-XX 倍 PE` -> 使用本地 `ISSUE_PRICE / AFTER_ISSUE_PE` 推 EPS 后折算价格，`forecast_kind` 标记为 `*_implied_price`。
- 多标的文章的区间归属从“前缀出现其他标的一律拒绝”改为“区间前最近出现的标的优先”，并让显式价格跨窗口优先于 PE/单点价折算。
- 覆盖表图例改为“可评分区间（显式或折算）”，折算项在逐作者明细中标记 `折算`。

验证过的关键句式：

- `中科仪市首日价格区间为43-49元` 可抽为 43-49。
- `对应首日股价：49.3-65.1元` 可抽为 49.3-65.1。
- `首日股价区间大概率在18.8-28.2元之间` 可抽为 18.8-28.2。
- `不低于45元` 可折算为 45-54。
- `可能达到60元` 可折算为 54-66。
- `给予爱舍伦上市首日35-43倍25年动态PE估值` 可折算为 PE implied price，并保留折算标记。

导入命令：

```powershell
python -X utf8 tools\import_manual_xueqiu_mhtml.py
```

导入报告：

- `outputs/xueqiu_manual_article_import_20260711_174431.json`

导入结果：

- 输入文件：59 个，其中 MHTML 49 个、TXT 10 个。
- 成功导入：59 个。
- 错误：0。
- 语料索引更新为 184 篇文章、182 篇可读、2 篇验证/阻断页。
- 抽到价格区间文章：85 篇。
- 抽到 target PE 文章：94 篇。

最新覆盖审计：

- `outputs/xueqiu_local_sample_coverage_20260711_174806.md`
- `outputs/xueqiu_local_sample_coverage_20260711_174806.json`
- `outputs/xueqiu_author_coverage_table_20260711_174807.md`
- `outputs/xueqiu_author_coverage_table_20260711_174807.csv`
- `outputs/xueqiu_author_coverage_table_20260711_174807.json`

覆盖结果：

- 本地样本：42。
- 已发现上市前预测样本：41。
- 已抓到上市前可读正文：41。
- 有可评分区间样本：41。
- 唯一仍未形成上市前预测覆盖的是 `920117 龙鑫智能`，当前状态为仅非上市前/非预测证据。
- `920050 爱伦医疗/爱舍伦` 已补齐兔子兔888和月半928证据；条条道路通罗马Lee为可读预测但仍未抽到可评分区间。
- `920183 海菲曼` 已补齐条条道路通罗马Lee和月半928上市前预测；兔子兔888为上市日文章，不作为上市前预测。

最新区间验证：

- `outputs/xueqiu_author_range_validation_20260711_174806.md`
- `outputs/xueqiu_author_range_validation_20260711_174806.json`

结果：

- 预测行数：121。
- 可评估预测：114。
- 行级命中：38/114，命中率 33.3%。
- 有实际均价的覆盖代码：40。
- 至少一位作者命中的代码：25，代码级命中率 62.5%。
- 同代码 baseline：3/31。
- 同代码扫描最优：8/31。

最新 author-rule score：

- `outputs/xueqiu_author_rule_score_20260711_174807.md`
- `outputs/xueqiu_author_rule_score_20260711_174807.json`

结果：

- 可评分代码：41。
- 有实际均价代码：40。
- 作者原始加权区间命中：18/40，命中率 45.0%。
- 作者中枢 ±10% 命中：16/40，命中率 40.0%。
- 调参样本交集 31 只中，作者原始加权区间命中 16/31，作者中枢 ±10% 命中 14/31，baseline 3/31，扫描最优 8/31。
- Spearman(score, 实际首日均价涨幅)：0.886。

最新组合验证：

- `outputs/xueqiu_author_model_blend_scan_sample_20260711_174819_193571.md`
- `outputs/xueqiu_author_model_blend_scan_sample_20260711_174819_193571.json`
- `outputs/xueqiu_author_model_blend_author_scored_20260711_174831_516029.md`
- `outputs/xueqiu_author_model_blend_author_scored_20260711_174831_516029.json`
- `outputs/xueqiu_author_model_blend_all_actual_20260711_174819_275061.md`
- `outputs/xueqiu_author_model_blend_all_actual_20260711_174819_275061.json`

结果：

- Scan sample：最佳 `current_params_author_blend_a0.9_w0.20_fallback`，20/31，命中率 64.5%，Spearman 0.890。
- Author scored：同 scan sample，20/31，命中率 64.5%。
- All actual：最佳 `current_params_author_blend_a0.9_w0.20_fallback`，23/39，命中率 59.0%，Spearman 0.864。
- 参考项：scan sample 当前参数 3/31，扫描最优 8/31，作者固定 ±10% 14/31，作者原始加权区间 16/31。

最新本地-only 蒸馏复跑：

- `outputs/xueqiu_author_logic_distillation_scan_sample_20260711_174819_250956.md`
- `outputs/xueqiu_author_logic_distillation_scan_sample_20260711_174819_250956.json`
- `outputs/xueqiu_author_logic_distillation_author_scored_20260711_174831_630675.md`
- `outputs/xueqiu_author_logic_distillation_author_scored_20260711_174831_630675.json`
- `outputs/xueqiu_author_logic_distillation_all_actual_20260711_174819_278577.md`
- `outputs/xueqiu_author_logic_distillation_all_actual_20260711_174819_278577.json`

结果：

- Scan sample 本地-only proxy：13/31，命中率 41.9%，proxy Spearman 0.520。
- All actual 本地-only proxy：16/39，命中率 41.0%，proxy Spearman 0.462。
- 结论未变：本地-only proxy 能内化一部分排序、动态宽度和兜底逻辑，但不能复刻作者目标价中枢。

阶段判断：

- 第二轮导入后，除龙鑫外，本地样本的四作者上市前预测覆盖已基本补齐。
- 新增样本进一步稳定了 author-rule 排序信号和融合上界；scan sample 最佳融合从上一轮 19/31 提升到 20/31，全量从 22/39 提升到 23/39。
- 隐式区间折算是必要补充，但 PE 折算存在 EPS 口径风险，后续报告和模型必须保留 `forecast_kind`，不能把折算区间当作原文显式报价。

## Proxy 排序 + 动态宽度 + 本地情绪兜底推进

用户要求继续推进本地-only 的“proxy 排序 + 动态宽度 + 本地情绪兜底”。本轮先更新设计文档，再实现独立策略评估脚本，不改正式 `valuation_engine.py`。

设计文档已更新：

- `docs/设计文档/09_雪球首日估值语料采集与Skill方案.md`

新增脚本：

- `tools/evaluate_local_proxy_strategy.py`

脚本能力：

- 复用 distillation 中的本地 proxy 特征：发行价、流通市值、老股比例、发行 PE/行业 PE、近端新股情绪、同业表现、顶格资金、模型不确定性。
- 生成 `proxy_rank_pct` 与 `proxy_tier`，用于排序和低/中/高分层诊断。
- 扫描三类动态宽度：
  - `conservative`：低/中/高约 `±8%/±12%/±15%`。
  - `balanced`：低/中/高 `±10%/±15%/±20%`。
  - `observe_wide`：研究项，允许 `±12%/±20%/±25%`。
  - `layered_v1`：按绝对 proxy 分、模型不确定性、模型不可用和近端情绪触发 `±10%/±15%/±20%`。
- 扫描本地情绪兜底：
  - `recent_mood`：本地模型不可用时用上市日前近端新股情绪。
  - `rolling_mood`：用滚动 proxy/实际涨幅关系，不足时回退近端情绪。
  - `guarded_rolling_mood`：低 proxy 且弱情绪时不兜底。
- 扫描受控中枢混合：
  - 默认 `model` 不改模型中枢。
  - 非研究项允许仅在高 proxy 或高不确定性时用滚动本地情绪做 50% 混合。
  - `all_rolling50/75` 标记为研究项，不作为默认推荐。

同时修正：

- `tools/analyze_xueqiu_author_logic_distillation.py` 中 `current_params` 字段映射 bug。
- 旧版读取 `current_params_available/current_params_predicted_change_pct`，但 teacher rows 实际字段是 `current_available/current_predicted_change_pct`。
- 因此旧报告中 `current_params_proxy_step0_layered_fallback` 的 13/31，更接近“全样本本地情绪兜底”，不是“当前模型中枢 + 不可用时兜底”。

另外将新策略脚本的滚动历史口径改为：

- 先在全量有实际均价的本地样本上按上市日滚动估计本地情绪。
- 再切片到 `scan_sample`、`author_scored` 或 `all_actual`。
- 这样 3 月以后的样本可以使用 1-2 月已经上市的本地样本作为历史，而不是只在目标集合内部滚动。

执行命令：

```powershell
python -X utf8 tools\analyze_xueqiu_author_logic_distillation.py --target scan_sample
python -X utf8 tools\analyze_xueqiu_author_logic_distillation.py --target all_actual
python -X utf8 tools\analyze_xueqiu_author_logic_distillation.py --target author_scored
python -X utf8 tools\evaluate_local_proxy_strategy.py --target scan_sample
python -X utf8 tools\evaluate_local_proxy_strategy.py --target all_actual
python -X utf8 tools\evaluate_local_proxy_strategy.py --target author_scored
```

修正后的 distillation 输出：

- `outputs/xueqiu_author_logic_distillation_scan_sample_20260711_180213_160179.md`
- `outputs/xueqiu_author_logic_distillation_scan_sample_20260711_180213_160179.json`
- `outputs/xueqiu_author_logic_distillation_all_actual_20260711_180213_190750.md`
- `outputs/xueqiu_author_logic_distillation_all_actual_20260711_180213_190750.json`
- `outputs/xueqiu_author_logic_distillation_author_scored_20260711_180331_854893.md`
- `outputs/xueqiu_author_logic_distillation_author_scored_20260711_180331_854893.json`

修正后的 distillation 结果：

- Scan sample：最佳 `scan_best_rolling_proxy_a0.75_w0.20_fallback`，13/31，命中率 41.9%，proxy Spearman 0.520。
- All actual：最佳 `scan_best_rolling_proxy_a0.50_layered_fallback`，16/39，命中率 41.0%，proxy Spearman 0.462。
- Author scored：最佳 `scan_best_rolling_proxy_a0.75_w0.20_fallback`，13/31。

新策略评估输出：

- `outputs/local_proxy_strategy_scan_sample_20260711_180454_136649.md`
- `outputs/local_proxy_strategy_scan_sample_20260711_180454_136649.json`
- `outputs/local_proxy_strategy_author_scored_20260711_180454_203828.md`
- `outputs/local_proxy_strategy_author_scored_20260711_180454_203828.json`
- `outputs/local_proxy_strategy_all_actual_20260711_180454_227200.md`
- `outputs/local_proxy_strategy_all_actual_20260711_180454_227200.json`

严格三件套结果（不改可用模型中枢）：

- Scan sample：推荐 `scan_best_proxy_model_balanced_recent_mood`，12/31，命中率 38.7%，平均宽度 16.1%，兜底 3/7。
- Author scored：同上，12/31，命中率 38.7%，平均宽度 16.1%，兜底 3/7。
- All actual：推荐 `scan_best_proxy_model_balanced_recent_mood`，14/39，命中率 35.9%，平均宽度 16.7%，兜底 3/8。
- 对照：scan sample 当前参数 3/31、扫描最优 8/31；all actual 当前参数 4/39、扫描最优 9/39。

研究上限：

- Scan sample：`scan_best_proxy_all_rolling75_layered_v1_recent_mood`，14/31，命中率 45.2%，平均宽度 19.2%。
- All actual：`scan_best_proxy_all_rolling50_layered_v1_recent_mood`，18/39，命中率 46.2%，平均宽度 19.4%。
- 这些策略会对所有可用模型样本混合滚动本地情绪中枢，暂列研究项，不作为默认候选。

阶段判断：

- 严格三件套证明“排序/宽度/兜底”能稳定超过当前参数和扫描最优，但提升不如作者融合。
- 纯相对分位宽度不如绝对 proxy 分和模型不确定性触发；proxy 排序更适合用于解释、分层和人工关注优先级。
- 若要进正式候选，优先把 `scan_best + balanced dynamic width + recent_mood fallback` 作为离线观察项。
- 若要追求命中率，需要研究 `rolling` 中枢混合，但它已经不只是“兜底”，必须单独标记和留出验证。

## 估值区间与上市当日卖点联动启动

用户要求把当前保守候选和滚动中枢两条估值线接入此前的首日盘中量价观察，形成“盘前估值区间 -> 开盘位置 -> 分时确认 -> 卖出动作”的指导模型。

本轮先新增设计文档：

- `docs/设计文档/10_估值区间与首日盘中卖点联动模型.md`

设计口径：

- 保守候选和滚动中枢分别保留，不提前平均成一条线。
- 开盘位置拆为低于下限、靠近下限、区间中部、靠近上限、高于上限。
- 引入上一只已完成首日交易的新股反馈，但严格按上市日期向前连接，禁止使用同日或未来样本。
- 复用 7 月 2 日形成的 9:35、9:45、10:00、10:30、13:30、14:00、14:30 量价观察节点。
- 回测同时评价卖出价改善、相对全天高点损失和尾部风险，不只追求平均卖价。
- 920081 欧伦电气作为最新案例单列，不用它反向选择历史阈值。

下一步实现独立离线评估脚本，暂不修改正式 `code/valuation_engine.py`。

### 联动回测实现与结果

新增脚本：

- `tools/evaluate_intraday_valuation_guidance.py`

新增/更新入口：

- 设计文档：`docs/设计文档/10_估值区间与首日盘中卖点联动模型.md`
- Skill：`skills/bj-ipo-first-day-valuation/SKILL.md`

脚本能力：

- 读取 40 只本地分钟线并生成 9:35、9:45、10:00、10:30、13:30、14:00、14:30 节点快照。
- 在内存中按分钟线日期和成交额/成交量修正 replay 日期与首日均价，生成保守候选和滚动中枢两条本地估值区间，不写回正式 replay。
- 将开盘位置分为双线高于上限、双线低于下限、单线冲突、区间内、靠近上下限等状态。
- 严格按更早交易日连接前一只新股的首日反馈，同日样本不互相引用。
- 回放原盘中量价状态机、保守区间联动、滚动区间联动、双线共识、有/无前序反馈和 50% 极保守开盘分支。
- 输出全量、2026 年 3 月以后、最近 10 只时间切片，以及逐样本动作轨迹。

最新输出：

- `outputs/intraday_valuation_guidance_20260711_183242_257694.md`
- `outputs/intraday_valuation_guidance_20260711_183242_257694.json`

验证范围：

- 本地分钟线：40 只。
- 成功解析：40 只。
- 两条估值线可用：40 只。
- 解析错误：0。

全量核心结果（卖出价相对开盘价）：

- 原盘中量价状态机：平均 +1.9%，中位 +1.5%，P10 -7.8%，最差 -12.8%。
- 保守区间联动：平均 +1.5%，中位 +1.1%，P10 -5.7%，最差 -9.0%。
- 滚动区间联动：平均 +1.4%，中位 +1.1%，P10 -5.7%，最差 -9.0%。
- 双线共识联动：平均 +1.4%，中位 +1.3%，P10 -5.7%，最差 -9.0%。

前序弱反馈：

- “前序弱 + 当前靠近/低于下限”触发 14 只，相对不使用前序反馈平均增量 +0.3%，中位 0.0%。
- “前序弱 + 当前双线低于下限”共 6 只；开盘卖 30%、剩余观察至 9:35，相对开盘平均 +2.6%、中位 +1.6%。
- 将开盘比例提高到 50% 后，相对 30% 方案平均 -0.6%、中位 -0.4%，仅 2/6 改善。

920081 欧伦电气案例：

- 前序样本 920136 的首日均价低于估值下限，前序反馈标记为弱。
- 开盘 55 元，保守与滚动两条线均为 129.30-193.95 元，属于双线严重低于预期。
- 默认动作：开盘卖 30%，9:35 价格 53 元且未收复开盘与累计均价，处理剩余 70%；回放组合卖价 53.60 元。
- 极保守 50% 分支回放卖价 54.00 元；收盘为 47.02 元。
- 两条本地线在该样本上都来自近端情绪兜底，明显受此前极强新股情绪污染。该区间适合作为“预期破裂”信号，不宜解释为可信基本面中枢。
- 指导输出将该情形标记为 `low_regime_break`，动作层仍可使用，区间中枢不可作为可信估值引用。

阶段结论：

- 估值区间联动主要改善尾部风险，平均卖价仍略低于原盘中量价状态机。
- 默认指导采用“双线低于下限 -> 开盘 30% -> 9:35 修复确认”；前序弱用于降低心理预期和缩短观察，不自动把开盘比例提高到 50%。
- 下一步应给本地情绪兜底增加 `regime-break` 降权，避免连续极强样本后单只转弱时仍生成失真的高区间。

## Regime-break 情绪兜底降权启动

用户同意继续测试情绪兜底的 regime-break 降权。本轮先更新：

- `docs/设计文档/10_估值区间与首日盘中卖点联动模型.md`

设计原则：

- 只修改模型不可用时的本地情绪兜底，不修改已有模型中枢。
- 只读取上一只更早交易日已完成首日交易的本地样本，禁止同日或未来数据。
- 用“上一只实际首日均价涨幅 / 上一只上市前 recent5 情绪预期”识别情绪断档。
- 第一版触发阈值为：上一只预期至少 80%，实际低于预期的 60%，且绝对落差不少于 50 个百分点。
- 测试 50% 混合、75% 混合和直接 cap 到上一只实际涨幅三档。
- 重点检查 920081 的高情绪兜底区间是否恢复到可解释范围，同时确认历史 fallback 命中和盘中尾部不恶化。

下一步在独立离线策略脚本中实现候选并复跑，不修改正式 `valuation_engine.py`。

### Regime-break 实现与回测

更新：

- `tools/evaluate_local_proxy_strategy.py`
- `tools/evaluate_intraday_valuation_guidance.py`

新增：

- `tools/evaluate_regime_break_fallback.py`

实现内容：

- 为每只样本记录上一只代码、上一只上市前情绪预期、实际首日均价涨幅、实际/预期比和绝对落差。
- 同日样本不互相引用；只在更早交易日样本完成后更新 regime 状态。
- 新增 `recent_mood_regime_blend50`、`recent_mood_regime_blend75`、`recent_mood_regime_cap` 三类兜底候选。
- 降权只在本地模型不可用、原本需要情绪兜底且 regime break 触发时生效。

输出：

- `outputs/local_proxy_strategy_scan_sample_20260711_185957_616218.md/json`
- `outputs/local_proxy_strategy_all_actual_20260711_185957_665140.md/json`
- `outputs/regime_break_fallback_20260711_190247_560086.md/json`

结果：

- 39 只 all_actual：原保守线 14/39、MAE 152.08；blend50 14/39、MAE 153.37；blend75 14/39、MAE 155.13；cap 13/39、MAE 156.90。
- 31 只 scan sample：原保守线 12/31；blend50/75 11/31；cap 10/31。
- 纳入 920081 的 40 只：保守线原兜底 14/40、MAE 155.39；blend50 14/40、MAE 153.74。滚动线原兜底 18/40、MAE 127.21；blend50 18/40、MAE 125.57。
- 920081：原区间 129.30-193.95；blend50 93.37-140.06；blend75 75.41-113.12；cap 57.45-86.17；实际均价 51.61，仍未命中。

结论：

- 简单直接 cap 会错杀后续反弹并降低历史命中，不升级默认。
- blend50 在含最新样本的 40 只目标域改善 MAE且不降命中，但在原 scan sample 少命中一只，增益依赖 920081，暂列影子候选。
- 后续 regime break 触发时同时输出原兜底和 blend50 降权区间，积累真正前向样本后再决定是否替换默认。
- 正式 `code/valuation_engine.py` 未修改。

## 估值学习流水线与 BAT 入口集成启动

用户要求：

- 将本轮新增的本地 proxy、滚动中枢、regime-break 和盘中指导接入估值自动调参。
- 在 `添加新股首日走势.bat` 增加第 3 项“刷新雪球参考”。
- 根目录新增 `xueqiu/`，用户手工保存雪球 MHTML/TXT 后由第 3 项自动导入和学习。
- 第 3 项禁止自动下载雪球网页。

已新增设计文档：

- `docs/设计文档/11_估值学习流水线与BAT入口集成.md`

实施方案：

- 自动调参结束时生成兼容 scan-report 的影子上下文，解决新增逻辑固定依赖旧扫描报告的问题。
- 接受参数后基于新参数刷新；不接受时基于候选 overrides 做影子复核。
- 新增本地影子估值统一编排脚本。
- 新增雪球手工参考统一编排脚本，只读取 `xueqiu/`，不调用自动采集器。
- BAT 只增加菜单和编排入口，业务逻辑保留在 Python 工具中。

### 估值学习流水线与 BAT 入口实施完成

新增：

- `tools/run_valuation_shadow_pipeline.py`
- `tools/refresh_xueqiu_reference.py`
- `docs/设计文档/11_估值学习流水线与BAT入口集成.md`
- `xueqiu/.gitkeep`

更新：

- `tools/tune_params.py`：自动调参结束时生成 `调参/valuation_auto_shadow_context_latest.json` 及时间戳归档，并自动刷新 proxy、滚动中枢、regime-break 和盘中指导影子报告。
- `tools/manual_tune_prompt.py`：补充自动调参后的影子刷新说明。
- `添加新股首日走势.bat`：新增第 3 项“刷新雪球参考”，支持 `3 --no-pause` 参数直达。
- `README.md` 与 `skills/bj-ipo-first-day-valuation/SKILL.md`：改为根目录 `xueqiu/` 手工文件入口优先，明确不自动下载雪球文章。

第 3 项编排链：

1. 导入 `xueqiu/*.mhtml`、`xueqiu/*.txt`。
2. 抽取/折算作者估值区间并验证。
3. 更新本地样本覆盖审计和作者覆盖表。
4. 更新 author-rule score。
5. 对 `scan_sample`、`author_scored`、`all_actual` 刷新作者/模型融合和本地规则蒸馏。
6. 更新本地 proxy、regime-break 与盘中指导影子报告。

验证：

- Python 编排空目录测试通过，状态为 `waiting_for_files`；未触发网络。
- `添加新股首日走势.bat 3 --no-pause` 空目录测试通过。
- 使用现有 `新建文件夹/` 59 个手工文件完整回放通过，其中 MHTML 49 个、TXT 10 个，13 个步骤全部完成。
- 完整回放汇总：`outputs/xueqiu_reference_refresh_20260711_192959_793114.md/json`。
- 最新 author-rule score：41 个代码有评分，40 个可评估，作者加权区间命中 18/40；扫描交集命中 16/31。
- 最新覆盖：42 个本地样本中 41 个有可读上市前预测；只剩 `920117 龙鑫智能`。
- 最新本地保守 proxy：扫描样本 12/31，平均宽度 16.1%；研究用滚动中枢候选 14/31。
- 自动调参上下文协议 smoke 通过：当前 90 天窗口 21 个样本，`sample_codes`、`top_candidates[0].overrides`、`auto_tune_status` 均正确。
- 统一影子流水线 smoke 通过：`outputs/valuation_shadow_pipeline_20260711_192756_795437.md/json`。

边界：

- BAT 第 3 项和 `tools/refresh_xueqiu_reference.py` 均不调用 `tools/xueqiu_corpus_collect.cjs`，不访问雪球网络。
- 本轮新增逻辑接入的是自动调参后的影子学习/评价阶段；proxy、滚动中枢和 regime-break 尚未成为正式搜索参数，也不会自动写入 `策略参数.txt`。

### BAT 交互菜单换行兼容修复

用户实际双击 BAT 后，菜单第 2 项之后出现 `'/'`、`'esh'` 等“不是内部或外部命令”错误。

定位结果：

- `添加新股首日走势.bat` 在新增第 3 项后混用了 CRLF 和 LF。
- 55 个换行中只有 29 个为 Windows CRLF；`cmd.exe` 在 UTF-8 中文菜单附近错误拆分了命令。
- 之前的 `3 --no-pause` 测试直接跳转到标签，绕过了交互菜单，因此未覆盖该问题。

修复与验证：

- 将 BAT 统一为 UTF-8 无 BOM、55/55 个 CRLF。
- 使用 PTY 实际启动交互菜单，四个选项均完整显示。
- 实际按下 `3`，成功进入本地雪球刷新；空 `xueqiu/` 返回 `waiting_for_files`，退出码为 0，未出现命令拆分错误。

### BAT 空目录输出精简

- 用户确认第 3 项菜单已可正常进入，并要求空目录提示后不再打印机器 JSON 摘要。
- `tools/refresh_xueqiu_reference.py` 在 `waiting_for_files` 状态下改为提示后直接以 0 退出。
- 复测 BAT 第 3 项：仅显示本地文件夹提示和“不访问或下载雪球网页”，不再输出 JSON 代码段。

## 自动调参本地学习重排接入

用户运行自动估值调参后指出，界面中的候选排序和建议参数仍完全来自旧评分，新逻辑只在调参结束后生成影子报告，未真正影响参数选择。

确认现状：

- 第一版仅接入自动调参后处理。
- proxy、动态宽度、情绪兜底和 regime-break 不参与 `best` 与 `changed_overrides` 选择。
- 因此此前“接入自动调参”的表述不完整。

已补充设计文档第八节，准备实现每轮两级重排：

- 第一层保留现有核心搜索，控制候选规模。
- 第二层仅用本地字段评价保守动态区间、regime-break 和滚动中枢。
- 综合权重暂定为核心 45%、保守线 35%、regime 线 15%、滚动线 5%。
- 两级胜者将替换本轮最优、成为下一轮中心，并在用户接受后写回正式可调参数。
- 作者价格和文章内容只作为规则形成阶段的教师信息，不进入候选重排输入。

### 两级重排实现与验证

新增：

- `tools/local_learning_auto_rerank.py`

更新：

- `tools/tune_params.py`：每轮核心搜索后执行本地学习重排；重排胜者替换 `best` 和 `changed_overrides`，并成为下一轮中心。
- `code/param_tuning.py`：自动调参接受记录增加综合学习分及保守/regime/滚动三线摘要。
- `tests/validate_param_tuning.py`：增加本地重排、无作者输入、walk-forward 和最终 overrides 来源断言。
- `tools/manual_tune_prompt.py`、`README.md`、Skill：同步新的两级调参流程。

实现口径：

- 候选池：核心 Top 20，加 baseline、阶段起点和核心最优，去重后通常为 21-22 组。
- 综合权重：核心评分 45%、保守动态区间 35%、regime-break 15%、滚动中枢 5%。
- 保守/regime 线采用 `conservative` 动态宽度；平均宽度预算 17%，超出部分扣分。
- 滚动线采用 50% 滚动 proxy 中枢混合和 `layered_v1` 宽度，只保留 5% 研究权重。
- 作者预测字典固定为空；结果中写入 `author_inputs_used=false`。
- proxy 阈值、分位层级和滚动中枢改为逐上市日 walk-forward；同日样本不读取彼此结果，早期样本不读取后来样本特征分布。

真实三轮 CLI 回放：

- 第 1 轮核心最优与两级重排最优一致。
- 第 2 轮一致，累计建议包含 `bse_discount_factor=0.625`。
- 第 3 轮核心评分最优为 `bse_discount_factor=0.60`、`sentiment_decay_half_life_days=12`；两级重排选择第 3 轮起点，保留 `bse_discount_factor=0.625`，不新增半衰期修改。
- 第 3 轮界面正确显示“是否改变核心最优：是”。
- 保守线：21 个近期样本，加权命中率 27.46%，加权 MAE 211.33，平均宽度 14.72%，可用率 100%。
- regime-break：加权命中率 27.46%，加权 MAE 212.68，平均宽度 14.72%。
- 滚动中枢：加权命中率 36.20%，加权 MAE 186.78，平均宽度 19.80%。
- 退出后生成的自动调参上下文确认：最终 `top_candidates[0].overrides` 等于两级重排胜者；核心最优另行保留；`author_inputs_used=false`、`walk_forward_proxy=true`。

验证：

- `python -X utf8 tests\validate_param_tuning.py`：通过。
- Python 编译检查：通过。
- 真实 CLI 三轮输入 `2 -> 2 -> 3`：通过；未写入 `策略参数.txt`，仅写临时影子上下文。
- 补充同日防泄漏测试：同日样本的 `rolling_proxy_history_count` 相同，不能读取彼此实际结果；下一交易日才纳入这些已完成样本。
- 修正自动调参 CLI 测试路径：影子上下文写入测试临时目录并关闭完整影子刷新，不再污染正式 `调参/`。
- 清理本轮测试误写入 `调参/` 的 7 个 `valuation_auto_shadow_context` 文件；复跑测试后正式目录未再生成测试上下文。

### 影子流水线控制台输出精简

用户接受第 4 轮参数后，自动影子刷新把子脚本完整 JSON 明细打印到控制台，信息量过大。

调整：

- `tools/run_valuation_shadow_pipeline.py` 成功步骤不再转发子脚本 JSON，只显示“开始 / 完成 / Markdown 报告路径”。
- `tools/refresh_xueqiu_reference.py` 同步采用相同规则，避免 BAT 第 3 项有文件时打印多段机器 JSON。
- 子脚本失败时仍打印 stdout/stderr，保留排障信息。
- 流水线结束时只显示完成提示和统一 Markdown 汇总报告路径。

复测：

- 四步影子流水线控制台输出由完整 JSON 明细缩减为 10 余行进度信息。
- 最新汇总：`outputs/valuation_shadow_pipeline_20260711_201557_818149.md/json`。
## 申购账户分布接入缺陷确认与修复启动

用户复核 920176 申购门槛后发现，乔路铭样本使 597 万元以上账户数从约 8 万户附近突降到
31343 户。排查确认：

- 人工梯度表已正确保存 `1+0=293;2+0=586;3+0=879;3+1=949.91;4+1=1172;5+1=1465`。
- `build_account_pool_history.py` 只使用了人工梯度的金额，账户数仍按相同 `lot_level` 读取
  `allocation_fit_json` 的 `compressed_extra_lots`。
- 乔路铭的 31343 户来自公告汇总量的三档代数压缩解，不是公告账户分布，也不是雪球作者账户分布。
- 该压缩解被错误写入 586 万元以上切点，并通过最新快照覆盖机制影响 920176。

已新增设计文档：

- `docs/设计文档/12_申购账户分布公告约束反推方案.md`

修复边界确定为：公告获配户数和获配手数为硬约束，人工核验梯度为金额切点，历史账户曲线为形状先验；
雪球作者推测只作印证参考，不进入主账户池。

### 修复实现与验证

更新 `tools/build_account_pool_history.py`：

- 移除人工门槛金额与 `compressed_extra_lots` 账户数按 `lot_level` 直接拼接的逻辑。
- 使用等价方程 `A1=获配户数、sum(Ak)=获配手数` 反推累计账户层级。
- 在每个新样本的门槛上读取上一期账户曲线作为形状先验。
- 公告总量需要收缩时优先保留低获配层历史锚，把修正逐步放到高手数尾部。
- 只把人工核验梯度写成账户池切点；压缩桶金额可辅助补齐隐含层级，但其账户数永不进入主账户池。
- 无人工梯度的普通压缩拟合样本不再产生主账户池观测点。

乔路铭最终重建切点：

- 293 万元：105233 户。
- 586 万元：约 84303 户。
- 879 万元：约 63490 户。
- 949.91 万元：约 57895 户。
- 1172 万元：约 41761 户。
- 1465 万元：约 26433 户。

以上结果由公告 105233 个获配账户、450000 手总获配量和历史账户曲线共同求解；雪球文章约 900 万元
6.3 万户仅用于结果印证，未进入计算。

账户曲线最终按逐样本累计快照实现：

- 新样本两个门槛之间的旧切点按新两端插值更新，不再残留过时的中间低点。
- 第一个本地样本生成稀疏快照，之后每个样本在上一张完整快照上增加或修正切点。
- 构建新快照时读取上一张累计快照的中心和逐点偏差带；正式运行读取目标日期之前的最新累计快照。
- 每个历史样本都保留当时版本，调参按日期读取对应快照，杜绝未来函数。

乔路铭后的最新累计曲线：500 万元约 90446 户、586 万元约 84303 户、700 万元约 76205 户、
800 万元约 69102 户、879 万元约 63490 户。31,343 户断崖已消失。

920176 最终端到端复核：

- 旧账户池版本：0+1 门槛 638.87 万元。
- 错误接入乔路铭压缩桶后：578.38 万元。
- 修复公告约束、累计快照和逐点偏差后：654.16 万元。
- 最新报告：`输出/920176_维琪科技_估值_20260711_213002.pdf`。

测试：

- `validate_account_pool_history_builder.py`：通过；新增两条公告方程、单调性和 31343 户断崖隔离断言。
- `validate_subscription_predictor.py`：通过。
- `validate_subscription_history_builder.py`：通过。
- `validate_subscription_prediction_tuning.py`：通过。
- `validate_param_tuning.py`：通过。
- Python 编译检查：通过。
- `validate_main_flow_regression.py`：既存的行业映射英文提示和报告章节断言失败；与本次申购账户池链路无关。

最终真实 `--mode account-pool` 回放通过，口径显示为 `announcement_constrained_account_pool`：

- 800 万元以上近期平滑约 67470 户，中位数约 67572 户。
- 乔路铭后的最新累计快照约 69102 户。
- 诊断页保留近期平滑值用于观察稳定性，正式门槛和历史回放仍按日期读取最新累计快照。

## 估值三方法职责与折溢价重构启动

用户复核 920176 报告后提出三项估值问题：可比公司缺失、方法一未纳入 PE/流通盘折溢价、方法二行业线与方法三市场情绪线衰减职责混淆。

排查结论：

- 维琪科技招股说明书并非没有可比公司。正文披露锦波生物、芭薇股份、湃肽生物、珈凯生物、创健医疗五家公司。
- 本地公告解析已取得两家上市可比公司，但仍使用 `832982.BJ`、`837023.BJ` 旧北交所代码；当前证券代码分别为 `920982.BJ`、`920123.BJ`，导致行情 PE 查询为空，方法一被误判为不可用。
- 当前方法一只计算“可比 PE × 北交所折扣”，未读取行业 PE 和流通盘。
- 当前方法二和方法三都使用 10 天半衰期，行业结构性差异与短期全市场情绪发生重复计价。
- 雪球作者在无法直接比较时，通常参考宽口径同行或行业平均估值，再结合成长、规模和质地修正，并降低判断置信度；该规则可抽象为本地行业 PE 低置信度兜底。

新增设计文档：`docs/设计文档/13_估值三方法职责与折溢价重构.md`。

本轮实施边界：修复北交所旧代码映射；方法一接入 PE 与流通盘连续因子及行业 PE 低置信度兜底；方法二改用独立静态/长周期参数；方法三自动调参半衰期范围下移，强化短期衰减；随后完成本地历史回放和 920176 端到端复核。

### 实现与回放结果

- `tushare_helper.py` 新增北交所旧代码按公司名精确迁移，旧代码记录缓存 `current_ts_code`，查询摘要保留原代码、现行代码、公司名和匹配来源。
- 920176 两个上市可比代码已从 `832982.BJ`、`837023.BJ` 迁移为 `920982.BJ`、`920123.BJ`，本地取得有效 PE 21.79 倍、27.98 倍，中位数 24.88 倍。
- 方法一新增发行 PE / 行业 PE 连续修正、首日流通盘连续修正、行业 PE 低置信度兜底及置信度权重。
- 行业 PE 兜底不能单独支撑最终估值，只能与方法二共同作为参照；直接上市可比公司不受此限制。
- 方法二新增独立参数 `method2_weight_mode` / `method2_decay_half_life_days`，正式值使用当年静态同行业稳健中位数。
- 方法三不再回退方法二半衰期，正式半衰期由 10 天缩短为 3 天；自动调参搜索范围调整为 2-15 天。
- 正式方法一参数经本地回放收敛为：低 PE 阈值 0.40、最大上修 5%、高阈值 0.80、高 PE 额外惩罚 0、流通盘阈值 1000 万股、最大小盘溢价 10%。高 PE 不重复惩罚的原因是 `EPS=发行价/发行PE` 已经体现一次发行 PE 影响。

同一批原可估样本对比：

- 旧口径：33 个可用、31 个有真实均价，区间命中率 22.58%，近期加权命中率 17.35%，加权 MAE 181.15，排序分 -0.18884。
- 新口径：33 个可用、31 个有真实均价，区间命中率 22.58%，近期加权命中率 18.72%，加权 MAE 185.54，排序分 -0.18389。
- 新口径近期命中与综合排序略有改善，原始 MAE 有所上升；因此 PE/流通盘修正保持较小幅度，后续继续交由自动调参而不是扩大手工系数。

920176 最终复核：

- 方法一恢复可用：目标价约 24.18 元，PE 修正约 1.04，流通盘修正约 1.01。
- 方法二静态行业线：目标价约 125.10 元。
- 方法三 3 天半衰期：近期首日情绪约 404.42%，触及 50% 情绪溢价上限，增加 11.08 元。
- 综合估值约 116.00 元，区间 104.40-127.60 元；相比方法一缺失时约 136 元的中枢明显回落。
- 最新报告：`输出/920176_维琪科技_估值_20260711_220207.pdf`，四页渲染检查通过，无截断、重叠或表格溢出；方法三统计口径和关注提示已统一为中文。
- 回放摘要：`outputs/valuation_method_roles_replay_20260711.md`。

验证：

- `validate_valuation_method_roles.py`：4 个职责隔离用例通过。
- 方法一两个迁移开关兼容配置文件的字符串 `True/False`；新增断言确认字符串 `False` 能真正关闭因子，避免开关误判。
- `validate_tushare_data_pipeline.py`：6 个数据管线用例通过，含旧北交所代码迁移。
- `validate_param_tuning.py`：通过。
- Python 编译检查：通过。
- `validate_main_flow_regression.py`：920180、920119 两只端到端报告均成功生成；仍有既存的旧提示文本与 Markdown 章节断言未随当前报告口径更新，本轮未按旧语义回退实现。

## 本地可比公司解析完整性审计与意向书抓取

启动全本地样本招股文件审计：回放数据集 42 只，公告目录现有 43 份招股说明书。当前零可比代码样本为 920076、920183、920177、920186、920136，另有 920072 因询价发行应读取招股意向书，但本地仅有发行公告和发行结果公告。

用户确认 920072 应按招股意向书口径处理。官网项目详情复核发现同时披露：

- 2026-06-04《科莱瑞迪：招股意向书》；
- 2026-06-17《科莱瑞迪：招股说明书》。

现有官网抓取器能识别“招股意向书”标题，但同一 `NEW` 桶内默认只选择一份较新文件，且下载 CLI/BAT 没有同时同步两个阶段文件的入口。用户进一步明确询价项目的时间顺序为“先意向书、询价、再说明书”。方案据此修正为不设优先级：BAT 第 2 项每轮同时扫描招股意向书和招股说明书，各自同步官网最新一份，本地已有文件直接复用；直接定价项目若只有说明书则只同步说明书。

### 意向书/说明书同步与可比公司解析修复完成

下载链路：

- `bse_official_helper.py` 按标题区分 `intent` 与 `prospectus`，同一项目可同时返回两个阶段的最新文件；原单文件接口保留兼容。
- `download_bse_official_pdf.py --document prospectus` 改为同步下载意向书与正式说明书，不设置优先顺序。
- `sync_offline_tuning_dataset.py --sync-prospectus-documents` 在离线样本刷新前逐代码扫描两个阶段；BAT 第 2 项已接入该开关。
- 920072 已从北交所官网补齐 `920072_科莱瑞迪_招股意向书.pdf` 与 `920072_科莱瑞迪_招股说明书.pdf`，两份文件完整性校验通过。

解析审计：

- 新增 `tools/audit_comparable_parser_coverage.py`，输出全量 JSON/Markdown，检查零结果、无效代码、明确选择句、章节代码差异及意向书/说明书阶段差异。
- 全量扫描 45 份招股阶段文件、44 个代码样本；最终仅 920177、920186 保持空结果。920177 为既有合理空样本，920186 的可比对象为境外/台湾企业，国内证券代码为空符合预期。
- 无效代码数为 0；920072 的意向书与正式说明书均稳定识别南微医学、春立医疗、康拓医疗、惠泰医疗四家公司。

解析器修复：

- 明确选择句优先识别“选取/选择/选为”“作为发行人/公司/同行业可比公司”等写法；句内有名称或证券代码时不再被较早的宽泛章节覆盖。
- “选择如下公司”及无明确选择句的版式改用后续可比表格兜底，并要求至少三家已核实名称共同出现，避免把客户或竞争对手误并入。
- 最终出口过滤裸数字和不支持的代码格式；已核实的北交所旧代码统一映射到现行 `920xxx.BJ`。
- 对同一名称在选择句和后续表格出现代码冲突的情况，以已核实名称映射纠正；康美特的德邦科技恢复为 `688035.SH`，同时保留其他独立可比代码。
- 可比公司解析缓存版本提升至 22，隔离旧误抓结果和一次并行审计产生的空缓存。

本轮修复覆盖的主要样本包括：920028、920029、920050、920072、920076、920078、920086、920117、920125、920126、920136、920156、920161、920166、920168、920176、920183、920188、920189、920193、920211、920218、920220。

验证：

- `validate_pdf_content_goldens.py`：30 个可比公司样本、5 个业务描述样本全部通过。
- `validate_bse_official_helper.py`：9 个用例通过，包含意向书和说明书双阶段选择。
- `validate_bse_newshare_fallback.py`：6 个用例通过。
- `validate_prospectus_autodownload.py`：5 个用例通过。
- `validate_valuation_method_roles.py`：4 个用例通过。
- 下载、同步、审计和解析模块 Python 编译检查通过。
- 最终审计：`outputs/comparable_parser_coverage_audit_latest.json`、`outputs/comparable_parser_coverage_audit_latest.md`。
