# 2026-09-14 工作日志

## 估值报告 BAT 下载失败修复（完成）

- 用户反馈报告等待约 420 秒后失败，北交所 newshare/project 两条链路均出现 HTTP 302 循环重定向；目标代码为 920202。
- 已检查 `运行.bat`、`code/bse_official_helper.py`、`code/bse_ipo_valuation.py` 和现有下载回归脚本。BAT 正常调用 Python，错误来自官网 HTTP 请求。
- 官网网页在线访问复现 Redirect loop detected。系统 curl 在当前环境报 schannel 凭据错误，后续使用 Python 检查实际响应。
- 发现主流程在必需招股文件失败后仍尝试发行/上市公告，待修复避免无效等待。
- 工作区已有数据、策略参数及 9 月 10 日日志等未提交改动；本任务保留这些既有改动。
- Python 禁止跟随重定向后确认官网首页、newshare、公司详情均返回 Location 等于请求原地址的 HTTP 302；HTTP/HTTPS 及直连均复现。
- 用户确认公司为安达股份。旧版新三板公告接口对 920202 返回空列表，但新版 `np-anotice-stock.eastmoney.com/api/security/ann` 已收录 2026-09-14 发布的正式招股说明书 `AN202609141829374202` 与发行公告 `AN202609141829374210`，代码和公司名已核对。东方财富 IPO 数据也确认 920202 为安达股份。
- 正在接入按证券代码严格校验的新版公告备用源、同客户端公告列表复用、循环重定向快速失败，以及招股文件缺失时停止可选公告探测。
- 首轮回归通过：新增重定向/备用源测试 9 项；原官网 helper 9 项；newshare 6 项；自动下载 6 项；主流程 2 项，共 32 项。覆盖普通跳转与 Cookie 握手、循环跳转有界、公告代码匹配/摘要排除/最新正式稿、备用源列表复用、PDF 完整性和必需文件失败快速终止。
- 实际命令 `python -u tools/download_bse_official_pdf.py 920202 --document prospectus` 已在官网失败后正确定位东方财富当天正式招股书，下载验证进行中。
- 实测发现第二层故障：东方财富附件 HTTPS 响应虽标为 application/pdf，实际为约 985 字节的网页验证脚本；同一公开附件的 HTTP 地址返回正常 `%PDF-1.5`，Content-Length=6169864。加入仅限该公开 PDF 域名/路径的 HTTP 备用下载，不执行服务器返回脚本，继续进行 PDF 完整性校验。
- 公告详情提供带版本查询串的真实附件地址；改为验证公告编号与证券代码后保留完整 URL。顺便修复系统 curl 中文错误输出引发的 UnicodeDecodeError（subprocess errors=replace）。
- 新增附件身份校验与 HTTPS 验证页切换测试，目前新测试 11 项通过；正在重新实测下载。
- 第二次真实下载已成功：`公告文件/920202_安达股份_招股说明书.pdf`，6169864 字节，已通过 PDF 文件头/EOF/响应长度校验并注册到本地公告索引。
- 检查 BAT 的默认解释器为 `C:/Program Files/Python312/python.exe`，其中 pdfplumber/pypdf/PyPDF2 均不可用。创建项目 `.venv` 补齐 pypdf，并让 `运行.bat` 优先选择项目环境、保留系统 Python 兜底和退出码。安装时发现证书环境变量指向已不存在的 certifi；仅在安装子进程指定 pip 自带有效证书路径，未改系统环境。
- `.venv` 安装 pypdf 6.18.1 成功，新增 requirements.txt 固定该版本。读取新招股书确认共 331 页，首页证券代码 920202、证券简称安达股份、公司全称湖州安达汽车配件股份有限公司均匹配。
- 在项目 `.venv` 内运行最终 5 组回归脚本，34 项全部通过；输出保存为 `tmp/report_download_tests_20260914.log`。`git diff --check` 针对本次代码/文档改动通过。
- 已通过真实 BAT 入口 `运行.bat 920202` 启动端到端验证，报告生成进行中。
- 使用项目 pypdf 解析实测招股书成功：发行价 7.55、发行 PE 13.5、申购日期 2026-09-16、发行后总股本 11200 万股；可比公司识别为 600933.SH / 603211.SH / 603305.SH / 605133.SH，主营业务识别为汽车铝合金精密压铸件。该步骤验证依赖补齐后确实能读取新 PDF。
- 发行公告实测下载成功：`公告文件/920202_安达股份_发行公告.pdf`，760456 字节、21 页，公司名称与文件类型匹配。
- 真实 BAT 已完成下载、解析、行情取数及估值计算，生成 `输出/920202_安达股份_估值_20260914_202309.html`。当前 Codex 受限沙箱中 Edge GPU 进程启动失败；随后仅对已生成的本地 HTML 使用批准的沙箱外导出，约 4 秒成功，未重新取数。此为执行环境限制，未关闭浏览器沙箱或修改系统安全设置。
- 最终 PDF：`输出/920202_安达股份_估值_20260914_202309.pdf`，380680 字节、4 页；Poppler 全页渲染与逐页视觉检查通过。报告保留上市日期/流通老股待确认信息。
- 从同一份最终 HTML 的表格恢复同名 `_一览.txt`，字段与报告一致，未重新估算。补充后续可比公司行情取数/估值计算阶段提示，并再次通过自动下载回归 6 项。
- 本次新增公告 PDF 导致本地公告索引和运行缓存正常更新；没有改动估值策略参数或其他既有数据改动。最终代码差异检查通过。

## 安达股份冻资临界点测算

- 按发行价 7.55 元、网上发行 2380 万股、顶格申购 119 万股（898.45 万元）及当前账户规模模型测算：当前冻资 11213.38 亿元对应配售率约 0.0160246%，顶格理论获配约 190.69 股，因此为 1 手正股加碎股。
- 顶格理论获配达到 200 股的冻资临界值为 10691.555 亿元，即冻资需较当前下降约 521.83 亿元（4.65%）。临界点及以下进入顶格 2 手正股区间。
- 在当前账户分布模型下，跨过该临界点后额外碎股成为“2+1”，静态门槛升至顶格 898.45 万元；估算剩余碎股槽位约 57942 个、顶格账户约 74033 户，覆盖率约 78.27%，因此顶格申购也需竞争时间顺序。该结果会随实际账户金额分布变化。

## 安达股份正股 2 手情景报告（完成）

- 按冻结资金 10691.50 亿元生成独立情景版，略低于 2 手精确临界值 10691.555 亿元；模型输出申购倍数 5949.97 倍、配售比例 0.0168%、顶格理论获配 200.001 股。
- 情景版申购档位：1+0 为 449.23 万元，1 手保护金额 499.23-549.23 万元，2+0 为顶格 898.45 万元；2+1 同样需要顶格，碎股边界覆盖率约 78.27%，必须抢时间。
- 估值部分沿用同日正式版：综合中枢 12.69 元，正式综合区间 11.42-13.95 元；报告中明确标注为模拟情景，避免与正式冻资预测混淆。
- 输出 `输出/920202_安达股份_正股2手临界情景_20260914.pdf`、配套 HTML 和 `输出/920202_安达股份_正股2手临界情景_20260914_摘要.txt`。
- PDF 为 A4 共 4 页、398675 字节；已通过 pypdf 关键字段抽取校验、Poppler 全页渲染和逐页视觉检查，字体、表格、分页均正常。
