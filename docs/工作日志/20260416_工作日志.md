# 北交所新股估值项目工作日志

日期：2026-04-16  
记录人：Codex  
目的：记录当日全样本扫描入口落地、PDF 三条提取链路集中修正、复核结果与项目整体未完成项，便于下一轮直接续做

## 一、本日工作重点

今天主要完成了三件事：

- 补齐了可复用的 PDF 全样本扫描脚本 `scan_pdf_samples.py`，并完成了对 `公告文件` 的全量扫描
- 围绕 `old_shares`、`comparables`、`business_desc` 三条链路做了一轮按失败样本驱动的最小修正
- 对 `business_desc` 做了招股类文件复核，并单点修正了 `920180` 的特殊版式问题

本轮仍坚持“小范围修正 + 本地验证优先”，没有直接扩大到整条主流程的大范围回归。

## 二、全样本扫描入口与扫描结论

### 1. 扫描入口已正式沉淀

今天新增：

- `scan_pdf_samples.py`

当前能力：

- 批量扫描 `公告文件` 下 PDF
- 输出以下三条链路结果：
  - `extract_old_shares_result()`
  - `extract_comparable_companies()`
  - `extract_business_desc()`
- 支持小样本 smoke test
- 支持全量 JSON 落盘，便于后续复扫和分类

相关输出：

- `输出/pdf_scan_smoke_test.json`
- `输出/pdf_scan_full_20260416.json`
- `输出/pdf_scan_full_20260416_summary.md`

### 2. 首轮全量扫描结论

全量扫描覆盖：

- 总文件数：`44`
- 上市公告书：`20`
- 招股说明书：`23`
- 招股意向书：`1`

首轮全量扫描识别出的核心问题包括：

- `old_shares`
  - `920036`、`920069` 存在大值误提
  - `920086`、`920180` 存在漏提
- `comparables`
  - `920055`、`920119`、`920186` 为零命中
- `business_desc`
  - `920029`、`920119` 为明显误提
  - `920055`、`920076` 存在长尾噪声
  - `920180` 存在“行业比较段挤掉真正主营描述”的特殊问题

## 三、当日已完成的规则修正

本轮继续只改 `pdf_parser.py`，按失败样本最小修正。

### 1. `old_shares`

已完成：

- 修正上市公告书股本结构表块状写法中 `小计` 场景的 payload 起点偏移
- 修正“整数股数被误判为万股”的单位推断问题
- 兼容 `----` 这类多连字符空值写法

修正结果：

- `920036觅睿科技上市公告书.pdf`
  - 由 `816327.0` 万股修正为 `81.6327` 万股
- `920069普昂医疗上市公告书.pdf`
  - 由 `52532151.0` 万股修正为 `525.3215` 万股
- `920086科马材料上市公告书.pdf`
  - 由漏提修正为 `0.0`
- `920180爱得科技上市公告书.pdf`
  - 由漏提修正为 `0.0`

补充复核：

- 20 份上市公告书 `old_shares` 重扫后已达到 `20/20` 命中

### 2. `comparables`

已完成：

- 支持全文中 `证券代码：301029` 这类无后缀 6 位代码补配
- 补充一层很窄的本地名称兜底，仅覆盖当前已确认失败样本中的公司名
- 补充“选取国内上市公司 ... 作为同行业可比公司”这类句式

修正结果：

- `920055隆源股份招股说明书.pdf`
  - 由 `[]` 修正为 `['603305.SH', '600933.SH', '603211.SH', '605133.SH']`
- `920119美德乐招股说明书.pdf`
  - 由 `[]` 修正为 `['301029.SZ', '688097.SH', '300450.SZ', '301662.SZ', '300173.SZ']`
- `920186中科仪招股意向书.pdf`
  - 继续保留为空结果，原因是其可比公司主要为海外/非当前函数契约中的 6 位本地市场代码

### 3. `business_desc`

已完成：

- 增加业务描述候选句有效性过滤
- 屏蔽以下典型误命中：
  - `公司是否...`
  - `主要责任人`
  - `关联关系`
  - `劳务派遣`
  - `发行条件 / 上市条件`
  - `股份回购`
- 增加 `发行人基本情况` fallback 重试
- 增加尾噪声裁剪：
  - `具体情况如下`
  - `主要产品与服务项目`
  - `行业内其他主要企业情况如下`
  - `二、发行人挂牌期间的基本情况`
- 增加“公司名直接起句”的支持，例如：
  - `爱得科技主要从事...`
  - `XX 公司是一家...`

修正结果：

- `920029新恒泰招股说明书.pdf`
  - 由劳务派遣/关联关系段误提修正为主营描述
- `920119美德乐招股说明书.pdf`
  - 由“公司是主要责任人...”误提修正为主营描述
- `920055隆源股份招股说明书.pdf`
  - 长尾技术创新噪声被裁掉
- `920076国亮新材招股说明书.pdf`
  - 由资质标签句修正为主营描述
- `920180爱得科技招股说明书.pdf`
  - 识别出其版式特殊点后，单点修正为主营描述：
    - `公司主要从事以骨科耗材为主的医疗器械的研发、生产与销售，主要产品包括脊柱类、创伤类、运动医学等骨科医用耗材以及用于伤口疗愈的创面修复产品。`

## 四、本地验证与复核状态

本轮已完成的验证包括：

- `validate_pdf_old_shares_goldens.py`
  - `Golden sample validation passed: 6 cases`
- `validate_pdf_content_goldens.py`
  - `PDF content golden validation passed: 2 comparable cases, 2 business cases`
- 上市公告书 `old_shares` 定向重扫
  - `20/20` 命中
- 招股类文件 `business_desc` 复核
  - `24` 份招股类文件中仅剩 `1` 个明显可疑样本

### 当前复核结论

截至今天收尾，招股类文件 `business_desc` 已基本收敛，之前的重点误提样本均已修正。

当前剩余最值得继续关注的点包括：

- `920180` 虽已修正到可接受结果，但本类“行业比较段和主营句重复共现”的版式仍值得在后续复扫中再确认是否影响其他样本
- 招股类文件里仍可能存在少量“结果看似可用、但不是最优简介句”的长尾样本

## 五、今天的停点

今天已经完成了“扫描入口 -> 全量扫描 -> 失败样本最小修正 -> 局部复核”的闭环，但以下事项尚未开始或尚未完成：

- 还未基于今天的新规则重新跑一轮完整全量复扫并更新分类结论
- 还未把今天修复过的典型样本补进新的黄金样本或回归脚本
- 还未做 1 到 2 个样本的最小主流程回归
- 还未回头检查报告文案是否与当前真实数据链路完全一致
- 还未对方法一数据源链路做新一轮小样本交叉验证

## 六、项目整体层面仍未完成的大块

从整个项目而不是仅今天修规则的角度看，目前仍未完成的大块主要有：

### 1. PDF 提取链路尚未真正“收口确认”

虽然 `old_shares`、`comparables`、`business_desc` 三条链路本轮都已完成一轮定向修正，但仍缺：

- 基于最新规则的完整全量复扫
- 复扫后的新版成功 / 失败 / 误提 / 漏提分类
- 新稳定样本沉淀为黄金样本
- 复扫后的残余问题优先级重排

### 2. 主流程最小回归尚未完成

目前已经确认各个局部提取函数可用，但还没有重新验证“提取结果 -> 方法一 -> 报告输出”在最新规则下的整体一致性。

仍缺：

- 选 1 到 2 个样本做主流程最小回归
- 重点核对：
  - 首日流通老股
  - 可比公司估值表
  - 公司简介
  - 方法一目标价
  - 综合估值
  - 提示文案

### 3. 方法一数据源链路还未做最新一轮收口

目前方法一已能运行，但整体层面还缺：

- `Wind 原料字段 -> 本地计算` 的再次稳定性确认
- 东方财富快照口径说明与当前实现的一致性复核
- 本地缓存 / 文件数据库 / quota 控制的新一轮重复运行验证

### 4. 报告展示与说明文案仍未完全收口

整体还没有完成：

- 是否展示方法二实际采用的历史样本代码
- 是否更明确说明时间衰减加权口径与样本范围
- 是否单独提示“上市公告书未识别，已回退招股文件”
- 报告里的数据源说明是否已经和当前实现完全一致

### 5. 验证资产沉淀仍不够完整

虽然扫描脚本已经补上，但自动化验证资产仍缺：

- 新增黄金样本
- 针对今天修复样本的定向回归脚本
- 后续最小主流程回归脚本或固定回归入口

## 七、基于最新规则的全量复扫结果

在完成 `old_shares`、`comparables`、`business_desc` 的定向修正后，今天已进一步完成基于最新规则的完整全量复扫：

- 输出文件：
  - `输出/pdf_scan_full_20260416_rescan.json`
- 扫描汇总：
  - 总文件数：`44`
  - `old_shares_hits = 25`
  - `comparables_hits = 23`
  - `business_desc_hits = 44`
  - `error_files = 0`

相较于首轮全量扫描：

- `old_shares_hits`
  - 由 `23` 提升至 `25`
- `comparables_hits`
  - 由 `21` 提升至 `23`
- `business_desc_hits`
  - 维持 `44`
  - 但招股类文件中的明显误提已基本消失

### 1. 复扫后的当前结论

- `old_shares`
  - 已达到可接受收口状态
  - 上市公告书漏提样本已消失
  - 此前大值误提样本已修正
- `comparables`
  - 当前仅剩 `920186中科仪招股意向书.pdf` 为零命中
  - 该样本主要为海外 / 非本地 6 位代码可比公司，继续保留为空结果是合理的
  - 另有两个样本仍值得后续关注是否属于部分漏提：
    - `920011晨光电机招股说明书.pdf`
    - `920181赛英电子招股说明书.pdf`
- `business_desc`
  - 按当前复核规则，招股类文件中已无明显误提样本
  - `920180` 已修正为主营描述

### 2. 复扫后新的停点

随着完整复扫已完成，项目当前最值得继续推进的事项已变为：

- 把今天修好的代表性样本补进黄金样本或定向验证脚本
- 选 1 到 2 个样本做最小主流程回归
- 视精力决定是否继续追 `920011` / `920181` 的 `comparables` 部分漏提问题
- 回头检查方法一与报告文案的整体收口情况

## 八、关联文件

本日新增或重点关联文件包括：

- `scan_pdf_samples.py`
- `输出/pdf_scan_smoke_test.json`
- `输出/pdf_scan_full_20260416.json`
- `输出/pdf_scan_full_20260416_summary.md`
- `输出/pdf_scan_full_20260416_rescan.json`
- `validate_pdf_old_shares_goldens.py`
- `validate_pdf_content_goldens.py`
- `20260416_下一步工作清单.md`

## 九、续做补记（验证资产与最小主流程回归）

在完成完整复扫并形成停点后，本轮继续推进了“补验证资产 + 最小主流程回归”两块。

### 1. 验证资产已补齐到当前阶段可用状态

- `validate_pdf_old_shares_goldens.py`
  - 由 `6` 个样本扩充到 `10` 个样本
  - 新增覆盖：
    - `920036`、`920069` 的上市公告书单位修正样本
    - `920086`、`920180` 的上市公告书零老股样本
- `validate_pdf_content_goldens.py`
  - 扩充到 `5` 个 `comparables` 样本、`5` 个 `business_desc` 样本
  - 新增覆盖：
    - `920055`、`920119` 的 `comparables`
    - `920186` 的合理空 `comparables`
    - `920029`、`920119`、`920180` 的 `business_desc`
- 新增：
  - `validate_main_flow_regression.py`
  - 作用：
    - 用真实 PDF 提取链路
    - 配合本地 mock 的 IPO 信息 / 可比快照
    - 做 `提取结果 -> 方法一/方法二 -> 报告输出` 的最小主流程回归
  - 当前覆盖样本：
    - `920180`
    - `920119`

### 2. 本轮验证结果

本轮实际执行并通过：

- `python validate_pdf_old_shares_goldens.py`
  - `Golden sample validation passed: 10 cases`
- `python validate_pdf_content_goldens.py`
  - `PDF content golden validation passed: 5 comparable cases, 5 business cases`
- `python validate_main_flow_regression.py`
  - `Main flow regression passed: 2 cases`

主流程回归产物已落到：

- `输出/回归/920180_爱得科技_估值_20260416.md`
- `输出/回归/920119_美德乐_估值_20260416.md`

### 3. 对当前停点的更新判断

截至本轮续做结束：

- “补验证资产” 这一优先项已完成到当前阶段可接受状态
- “做 1 到 2 个样本的最小主流程回归” 已完成第一版落地
- 当前主流程回归仍是“本地 mock IPO / 可比快照 + 真实 PDF 提取”的离线回归
- 尚未再次做基于真实联网数据源的最小主流程实跑复核

因此当前新的下一接续点应调整为：

- 方法一数据源链路的小样本交叉验证
- `920011` / `920181` 的 `comparables` 部分漏提判断
- 报告展示与说明文案收口

## 十、续做补记（二）：`comparables` 尾项收口与方法一数据链路验证

在完成第一轮验证资产补齐之后，本轮继续推进了两个剩余重点：

- 收口 `920011` / `920181` 的 `comparables` 尾项
- 为方法一数据源链路补独立验证脚本

### 1. `comparables` 尾项样本已完成最终判断与修正

本轮继续围绕 `pdf_parser.py` 做最小修正，重点包括：

- 补强词汇表 / 释义表中的名称-代码识别
  - 扩大 `GLOSSARY_PATTERN` 适配范围
  - 兼容 `挂牌代码`
- 增加“逐条释义 entry”级别的可比公司标签识别
  - 避免把 `供应商` / `客户` 串到下一条“同行业可比公司”标签里
- 修正“选取……作为同行业可比公司”长句里的名称拆分
  - 支持 `选取` 无 `了`
  - 去掉 `国内上市公司` 这类前缀
  - 兼容 `……的星德胜` 这类前置修饰
- 补充 `与同行业可比公司的对比分析` 这类章节锚点
  - 解决 `920180` 依赖 generic 锚点时误选过早章节的问题
- 对可比公司结果统一做目标公司自引用过滤
  - 避免 `920180.BJ` 这类“把发行人自己带进可比列表”的情况

本轮最终确认：

- `920011晨光电机招股说明书.pdf`
  - 当前 `comparables` 提取结果：
    - `['603344.SH', '301226.SZ', '300660.SZ', '002892.SZ', '833450.NQ', '920100.BJ']`
  - 结论：
    - 之前“仅提取 1 家”属于规则漏提，现已修正
- `920181赛英电子招股说明书.pdf`
  - 当前 `comparables` 提取结果：
    - `['301581.SZ', '688103.SH', '873913.NQ']`
  - 结论：
    - 之前“仅提取 1 家”属于规则漏提，现已修正
    - 供应商 `601609.SH` 已被排除

因此，上一轮日志中提到的 `920011` / `920181` 两个 `comparables` 尾项，本轮已完成收口，不再作为当前未解决主项。

### 2. 方法一数据源链路已新增独立验证脚本

本轮新增：

- `validate_method1_data_pipeline.py`

脚本目标：

- 不依赖真实联网结果
- 用本地临时文件数据库 + mock 数据源
- 固化验证以下关键状态：
  - `disabled` 下东方财富补位
  - 新鲜 Wind 快照命中缓存并执行本地计算
  - Wind 刷新后执行东方财富交叉验证并落盘
  - quota 耗尽时沿用旧快照并正确打标

本轮实际执行结果：

- `python validate_method1_data_pipeline.py`
  - `Method1 data pipeline validation passed: 4 cases`

### 3. 本轮回归与验证结果汇总

本轮实际执行并通过：

- `python validate_pdf_old_shares_goldens.py`
  - `Golden sample validation passed: 10 cases`
- `python validate_pdf_content_goldens.py`
  - `PDF content golden validation passed: 7 comparable cases, 5 business cases`
- `python validate_main_flow_regression.py`
  - `Main flow regression passed: 2 cases`
- `python validate_method1_data_pipeline.py`
  - `Method1 data pipeline validation passed: 4 cases`

### 4. 当前阶段新的接续点

随着 `920011` / `920181` 的 `comparables` 已收口、方法一数据链路也已有离线验证脚本，当前更值得继续推进的事项已更新为：

- 报告展示与说明文案收口
- 方法一链路的“真实缓存 / 真实口径”小样本复核
- 性能与稳定性观察（单票耗时、重复解析、缓存调用链）

## 十一、续做补记（三）：`920177` 真实主流程实跑与报告文案问题定位

在完成上一轮离线回归与验证脚本补齐后，本轮开始切到真实样本实跑，先用当日新股 `920177恒道科技` 做了一次完整报告生成检查。

### 1. `920177` 已完成一次真实报告实跑

本轮实际执行：

- `python bse_ipo_valuation.py 920177`

执行过程说明：

- 首次在默认沙箱下运行时，东方财富请求失败，报错为：
  - `WinError 10061`
  - 结论：属于联网访问被拒，非业务逻辑报错
- 随后在放开联网后重跑成功

已生成产物：

- `输出/920177_恒道科技_估值_20260416.md`

### 2. 首次实跑后发现的主要问题

报告主体成功生成，但在阅读生成稿时发现两个需要继续修的点：

- 行业分类未命中
  - `920177` 当时未录入 `industry_mapping.py`
  - 结果导致报告中行业显示为 `未分类`
  - 连带使方法二退回到 `全市场` 口径，而不是落到机械设备相关样本
- 数据来源文案重复
  - 顶部“数据来源”一行出现了 `东方财富` 重复表述
  - 形如：
    - `东方财富，PDF（可选），东方财富（可比快照 ...）`
  - 这属于报告展示层的拼文案问题，不影响估值主计算

另外，当次实跑观察到的提取结果中：

- `comparables` 为 `['874616.NQ', '002859.SZ', '000651.SZ']`
- `old_shares` 为 `0.00`
- `business_desc` 文案主体可用

说明当前真实样本下，PDF 提取链路本身没有暴露新的明显阻塞问题，暴露出来的主要是行业映射与报告展示文案收口问题。

### 3. 针对 `920177` 已完成的代码修补

本轮已直接修改：

- `industry_mapping.py`
  - 新增：
    - `\"920177\": (\"高端装备\", \"机械设备\")`
- `report_generator.py`
  - 调整 Wind 未启用分支的数据来源文案
  - 将原先容易造成重复的 `东方财富（可比快照 ...）` 改为：
    - `可比快照（...）`
  - 无有效可比快照时改为：
    - `可比快照（未形成有效可比公司数据）`

本轮已做的轻量校验：

- `python -m py_compile industry_mapping.py report_generator.py`
  - 通过

### 4. 当前停点与下一接续点

截至当前收工前，`920177` 这一轮的真实样本推进状态是：

- 已完成首次真实报告实跑
- 已定位并修补两个直接暴露的问题：
  - 行业映射缺失
  - 数据来源文案重复
- 尚未在修补后再次联网重跑 `920177` 做最终确认

因此，下一个最直接的接续动作应是：

- 重新联网实跑 `920177`
- 重点核对：
  - 行业是否从 `未分类` 修正为 `高端装备 / 机械设备`
  - 方法二是否不再退回 `全市场`
  - 顶部数据来源文案是否去掉重复 `东方财富`

## 十二、续做补记（四）：`920177` 复跑确认与报告展示增强

在按上一轮停点重新实跑 `920177恒道科技` 后，本轮完成了真实样本确认与一轮报告展示增强。

### 1. `920177` 修补后已完成二次实跑确认

本轮实际执行：

- `python bse_ipo_valuation.py 920177`

复核结果：

- 行业已由 `未分类` 修正为 `高端装备 / 机械设备`
- 方法二已不再退回 `全市场`，而是落到 `二级行业` 样本
- 顶部数据来源文案已稳定为：
  - `东方财富，PDF（可选），可比快照（3 只，实时抓取 0 只，缓存命中 3 只）`
- 方法二当前实际纳入样本代码为：
  - `920011`、`920188`、`920187`、`920086`

补充观察：

- 单票再次实跑耗时约 `41` 秒
- 本次复跑未再暴露新的 PDF 提取链路阻塞问题

### 2. 报告展示层已补充两类显式信息

本轮直接修改：

- `report_generator.py`
  - 在方法二段新增：
    - `方法二样本范围 = 历史候选 X 只，实际纳入 Y 只`
    - `方法二实际样本代码 = ...`
- `bse_ipo_valuation.py`
  - 为 `old_shares_meta` 补充：
    - `listing_pdf_found`
    - `prospectus_pdf_found`
    - `selected_source_label`
    - `fallback_used`
- `valuation_engine.py`
  - 当首日流通老股未从上市公告书提取到有效结果、实际回退到招股文件时，在“关注提示”中新增显式说明

这意味着此前“评估是否展示方法二实际采用的历史样本代码”和“是否单独提示已回退招股文件”两项，当前都已经具备可见输出能力。

### 3. 本轮校验结果

本轮实际执行并通过：

- `python -m py_compile bse_ipo_valuation.py valuation_engine.py report_generator.py validate_main_flow_regression.py`
- `python validate_main_flow_regression.py`
  - `Main flow regression passed: 2 cases`
- `python bse_ipo_valuation.py 920177`
  - 已重新生成：
    - `输出/920177_恒道科技_估值_20260416.md`

### 4. 当前新的停点

截至本轮续做收尾，原先围绕 `920177` 的三个直接问题已经完成收口：

- 行业映射缺失
- 顶部数据来源文案重复
- 方法二实际样本范围 / 样本代码不可见

当前更值得继续推进的事项更新为：

- 方法一“真实缓存 / 真实口径”小样本复核
- 性能与稳定性观察（重复运行耗时、是否重复完整解析 PDF、缓存调用链是否稳定）
- 如后续再做报告展示层收口，重点转向是否还需要补更细的样本来源解释，而不是继续修 `920177` 这一票本身

## 十三、续做补记（五）：用户入口批处理与交互提示落地

按当前优先级调整，本轮暂不继续做 Wind / 万得链路复核，先把面向最终用户的本地入口补齐。

### 1. 用户入口已改为“批处理启动 + Python 交互提示”

本轮直接修改：

- `运行.bat`
  - 改为只负责：
    - 切到项目目录
    - 启动 `python bse_ipo_valuation.py`
    - 保留窗口，便于用户查看结果
  - 不再在 `.bat` 内部承载中文输入逻辑，避免后续因为 `cmd` 编码导致提示乱码
- `bse_ipo_valuation.py`
  - 新增 `6` 位数字代码校验
  - 无命令行参数时，进入交互模式：
    - 提示 `请输入 6 位新股代码（例如 920177，直接回车可退出）：`
    - 输入非 `6` 位数字时，提示 `输入有误，请输入 6 位数字代码。`
    - 直接回车时，提示 `已取消，本次未生成报告。`
  - 生成成功后，明确打印：
    - 报告完整路径
    - 报告所在目录
    - 到 `输出` 文件夹查看的提示

这意味着当前用户双击 `运行.bat` 后，已经可以按“输入 6 位代码 -> 生成报告 -> 看到保存位置”的最短路径使用。

### 2. 本轮补充的用户提示词

当前实际落地的核心提示词包括：

- 输入提示：
  - `请输入 6 位新股代码（例如 920177，直接回车可退出）：`
- 输入错误提示：
  - `输入有误，请输入 6 位数字代码。`
- 取消提示：
  - `已取消，本次未生成报告。`
- 成功提示：
  - `报告已生成：...`
  - `报告所在目录：...`
  - `可直接打开上面的完整路径，或到“输出”文件夹中查看生成的 Markdown 报告。`

### 3. 本轮验证情况

本轮已验证：

- `python -m py_compile bse_ipo_valuation.py`
- 非法代码拦截：
  - `python bse_ipo_valuation.py 123`
  - 已正确返回 `请输入 6 位数字代码，例如 920177。`
- 交互模式离线模拟：
  - 先输入错误代码，再输入 `920177`
  - 已正确输出报告完整路径与目录提示
- `运行.bat` 空输入退出：
  - 已确认可正常显示“已取消，本次未生成报告。”

补充说明：

- 尝试做一次基于真实数据源的在线生成确认时，当前数据源请求出现超时
- 该问题不影响本轮“用户入口 / 提示词 / 输出路径提示”本身的落地判断
- 考虑到后续本就计划切换数据源，本轮不继续沿当前数据源深挖

### 4. 当前新的接续点

围绕用户入口这一块，当前已经完成：

- 批处理入口
- 6 位代码校验
- 报告生成路径提示
- 基本交互提示词

因此，后续与入口相关的下一步更适合调整为：

- 等新数据源方案确定后，把 `bse_ipo_valuation.py` 的实际拉数链路切换过去
- 视需要再补：
  - “生成中，请稍候” 提示
  - 失败原因更细的用户化文案
  - 生成成功后自动打开 `输出` 文件夹或报告文件

## 十四、续做补记（六）：综合估值展示增强与最终输出切换为 PDF

本轮继续围绕最终用户可见结果做了两项直接改动：

### 1. 报告中的“综合估值”已显式展示公式、权重和区间宽度

本轮修改后，报告在“综合估值”段会额外展示：

- `权重设置 = 方法一 XX% + 方法二 XX%`
- `综合估值公式 = 方法一目标价 × XX% + 方法二目标价 × XX%`
- `代入结果 = ...`
- `区间宽度 = ±XX%`

例如在当前样例里，已可直接看到：

- `权重设置 = 方法一 50% + 方法二 50%`
- `综合估值公式 = 方法一目标价 × 50% + 方法二目标价 × 50%`
- `代入结果 = 48.95 × 50% + 42.12 × 50% = 45.54`
- `区间宽度 = ±10%`

这解决了此前“报告里只有综合结果，没有把怎么算讲清楚”的问题。

### 2. 最终用户输出已由 Markdown 切换为 PDF，并带秒级时间戳

本轮修改：

- `report_generator.py`
  - 新增：
    - 报告内容文本构建
    - HTML 渲染
    - 通过本机 `Microsoft Edge` 无头导出 PDF
  - 最终输出文件名改为：
    - `输出/{代码}_{简称}_估值_{YYYYMMDD_HHMMSS}.pdf`
- `bse_ipo_valuation.py`
  - 成功提示改为引导用户查看 `PDF 报告`
- `说明文档.md`
  - 输出格式与交互示例同步更新为 `PDF + 时间戳`

补充说明：

- 若本机 PDF 导出失败，当前会自动保留同名 `.html` 调试文件，便于快速排查
- 回归脚本仍保留文本版校验能力，不会因为最终输出改成 PDF 而失去内容验证入口

### 3. 本轮验证结果

本轮已验证并通过：

- `python -m py_compile bse_ipo_valuation.py report_generator.py validate_main_flow_regression.py`
- `python validate_main_flow_regression.py`
  - `Main flow regression passed: 2 cases`
- 本机 PDF smoke test（使用本地假数据）
  - 已成功生成：
    - `输出/pdf_smoke/920177_恒道科技_估值_20260416_165853.pdf`
  - 并确认 PDF 文本中已包含：
    - `综合估值公式`
    - `权重设置`
    - `区间宽度`

### 4. 当前新的停点

截至本轮，面向最终用户的输出层已完成以下收口：

- 综合估值解释可见
- 最终输出格式切为 PDF
- 文件名支持秒级时间戳区分版本

因此后续若继续沿用户侧打磨，更适合优先考虑：

- 等新数据源接入后，验证真实拉数场景下 PDF 导出耗时与稳定性
- 视需要增加“生成成功后自动打开 PDF / 输出目录”
- 再决定是否补更多展示细节，而不是继续堆大段说明文字

## 十五、续做补记（七）：下一阶段规划确认（Tushare 替换 + 参数训练系统）

本轮未继续直接编码，而是把下一阶段的两条主线正式定了下来：

- 主线一：把方法一当前依赖的 `Wind` 可比公司估值快照链路切换为 `Tushare`
- 主线二：补一个离线调参系统，用历史样本的“估值结果 vs 上市首日实际表现”去训练更合理的参数

### 1. 已确认的总顺序

下一阶段按以下顺序推进：

- 先替换数据源
- 再做参数训练

原因：

- 调参依赖稳定的数据口径
- 如果先按旧 `Wind` 口径训练，后面再切 `Tushare`，大概率需要重训一遍

### 2. 已确认的第一阶段范围：只替换方法一可比估值快照层

本轮已明确：

- 当前 `IPO 基础信息` 与 `近期新股样本` 继续沿用 `data_fetcher.py` 的东方财富链路
- 第一阶段只替换：
  - `bse_ipo_valuation.py -> wind_helper.py -> comparable_data`
  - 也就是“方法一可比公司估值快照”这一层

对应的第一阶段工作拆解如下：

- 先抽一层中性市场数据接口，避免主流程继续写死 `Wind` 命名
- 新建 `tushare_helper.py`，负责：
  - 代码转换
  - 最近交易日定位
  - 批量拉数
  - 字段统一
  - 本地缓存
  - 限频 / 重试
- 调整参数文件与 `config_loader.py`
  - 新增 `tushare_token`
  - 新增缓存目录、批量大小、请求间隔、重试次数等参数
  - 逐步去掉或兼容旧 `wind_*` 命名
- 调整报告中的数据来源文案
  - 从 `Wind` 专名改成更通用的“估值快照数据源说明”
- 补最小回归
  - `Tushare mock`
  - 缓存命中
  - 接口失败降级

### 3. 已确认的第二阶段范围：离线参数训练系统

参数训练系统不直接塞进用户入口，而是作为离线工具单独实现。

第二阶段工作拆解如下：

- 构建历史样本回放数据集
  - 每只历史新股只使用其上市当时可见的信息
  - 避免“偷看未来数据”
- 新增调参脚本
  - 例如 `tools/tune_params.py`
- 按阶段调参数
  - 先调大参数：
    - `bse_discount_factor`
    - `weight_comparable`
    - `weight_industry_momentum`
    - `price_range_width`
  - 再调方法二阈值：
    - `small_cap_premium`
    - `float_size_threshold`
    - `pe_low_threshold`
    - `pe_discount_boost`
    - `pe_high_threshold`
    - `pe_premium_drag`
    - `trend_*`
    - `sample_decay_half_life_days`
  - 最后调：
    - `industry_trend_weight`
    - `market_sentiment_weight`
    - `wsi_weight_*`
- 采用时间切分而不是随机切分
  - 避免过拟合
- 最终输出：
  - 一份推荐参数
  - 一份调参结果报告

### 4. 已确认的评估口径

后续调参优先使用以下指标：

- 主指标：首日涨幅预测误差 `MAE`
- 辅指标：
  - `RMSE`
  - 区间命中率
  - 方向命中率
  - 综合估值可用率

### 5. 已确认的开工前置条件

下一轮预计在 `2026-04-17` 开工，但前置条件是：

- 先购买 `Tushare`
- 获取并确认可用 `token`
- 确认可用接口权限是否满足首期接入范围
- 首期按 `T-1 收盘口径` 接入，不在这一轮扩展到盘中实时口径

### 6. 当前停点

截至 `2026-04-16` 收工，主流程和用户入口已基本收口；下一轮真正开工时，建议直接从以下顺序开始：

- 先做数据源抽象层
- 再落 `Tushare` 适配器
- 再补回归
- 最后再开离线调参系统
