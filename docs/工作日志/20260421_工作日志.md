# 北交所新股估值项目工作日志
日期：2026-04-21  
记录人：Codex  
目的：继续定向观察 `quick_method2_core_only`，补充边界流通盘样本，并修正观察工具对不可用样本的展示口径。

## 一、今日主线完成情况

今天继续沿着 `quick_method2_core_only` 这条线往下推进，目标不是再开一轮大范围调参，而是把“当前这组参数到底值不值得正式固化”拆成更清晰的几个判断：

- 继续补看 `1500 ~ 2000` 万股流通盘的边界样本
- 继续补看 `< 1500` 万股真小盘样本，确认 `small_cap_premium = 0.15` 是否稳定
- 补一个 `> 2000` 万股的不触发对照组，确认 `price_range_width = 0.12` 的单独作用
- 修正 `tools/observe_quick_method2_core_only.py`，把“结果不可用”与“明确为否”区分开

## 二、今日观察执行情况

今天补跑的样本为：

- `920036`
- `920119`
- `920159`
- `920050`
- `920011`
- `920086`

执行命令：

- `python tools\observe_quick_method2_core_only.py --codes 920036,920119,920159,920050,920011,920086 --skip-pdf`

输出文件：

- `输出/观察期/observe_qm2_core_only_prw_0p12_fst_1500_scp_0p15_20260421_094325.json`
- `输出/观察期/observe_qm2_core_only_prw_0p12_fst_1500_scp_0p15_20260421_094325.md`

本轮结果：

- 样本总数：`6`
- 有效可比样本数：`5`
- baseline 平均绝对误差：`16.7107`
- 候选平均绝对误差：`13.6722`
- baseline 区间命中率：`0.2000`
- 候选区间命中率：`0.2000`

不可用样本：

- `920086`
  - 最终估值不可用：`缺少可用的估值结果，无法给出综合定价。`
  - 方法二不可用：`不存在早于标的上市日的历史样本，无法计算方法二。`

## 三、分组观察结论

### 1. `1500 ~ 2000` 万股边界样本

本轮连同昨天已观察样本一起合并后，边界组共有 `5` 只有效样本：

- `920069`
- `920119`
- `920159`
- `920055`
- `920050`

合并结果：

- baseline 平均绝对误差：`14.5477`
- 候选平均绝对误差：`10.7603`
- baseline 区间命中率：`0.4000`
- 候选区间命中率：`0.6000`
- 候选优于 baseline：`4/5`
- 候选劣于 baseline：`1/5`

当前可以写出的结论是：

- `float_size_threshold = 1500` 在边界流通盘样本上的支持度正在明显增强
- baseline 在 `2000` 阈值下更容易触发不必要的小盘溢价
- 当前这条线已经不只是单一样本偶然受益，而是出现了更成体系的边界改善

### 2. `< 1500` 万股真小盘样本

当前已观察的真小盘样本共有 `4` 只：

- `920181`
- `920177`
- `920036`
- `920012`

合并结果：

- baseline 平均绝对误差：`7.3919`
- 候选平均绝对误差：`7.4273`
- baseline 区间命中率：`0.2500`
- 候选区间命中率：`0.5000`
- 候选优于 baseline：`2/4`
- 候选劣于 baseline：`2/4`

这说明：

- `small_cap_premium = 0.15` 还没有形成足够稳定的点估值增益
- 它并不是明显错误，但也还不足以单独拿出来宣布“已经验证通过”
- 当前真小盘组里更稳定的改善，仍主要体现在 `price_range_width = 0.12` 对区间覆盖的帮助

### 3. `>= 2000` 万股不触发对照组

当前对照组共有 `3` 只：

- `920011`
- `920188`
- `920180`

合并结果：

- baseline 平均绝对误差：`3.9945`
- 候选平均绝对误差：`3.9945`
- baseline 区间命中率：`0.0000`
- 候选区间命中率：`0.3333`
- 候选优于 baseline：`0/3`
- 候选劣于 baseline：`0/3`
- 候选持平：`3/3`

这组结果说明：

- 当小盘溢价不触发时，`quick_method2_core_only` 不会把点估值系统性推偏
- `price_range_width = 0.12` 的作用主要体现在区间命中改善，而不是强行抬高点估值

## 四、截至今天的合并判断

把 2026-04-20 与 2026-04-21 两轮观察合并后，当前共有 `12` 只有效可比样本：

- baseline 平均绝对误差：`9.5241`
- 候选平均绝对误差：`7.9578`
- baseline 区间命中率：`0.2500`
- 候选区间命中率：`0.5000`
- 候选优于 baseline：`6/12`
- 候选劣于 baseline：`3/12`
- 候选持平：`3/12`

截至今天，最稳妥的阶段性判断更新为：

- `quick_method2_core_only` 整组参数继续保留，当前没有回退理由
- `price_range_width = 0.12` 的区间命中改善信号更稳
- `float_size_threshold = 1500` 在边界样本上的支持度已经明显增强
- `small_cap_premium = 0.15` 仍然偏“可保留继续观察”，但还不到单独正式固化的证据强度

因此今天不建议直接把整组结论写成“全部验证完成”，而更适合写成：

- 整组参数继续保留
- 其中 `price_range_width = 0.12` 与 `float_size_threshold = 1500` 更接近正式固化
- 后续若继续推进，下一步应优先拆分验证 `small_cap_premium = 0.15`

## 五、观察工具口径修正

今天顺手修正了：

- `tools/observe_quick_method2_core_only.py`

修正点：

- 把 `None` 与 `False` 的展示口径区分开，避免缺结果样本被误显示成“否”
- 摘要中新增“不可用样本”说明
- 对不可用样本显式写出 `final` / `method2` 的不可用原因
- 汇总统计仅基于有效可比样本计算

这次修正后，`920086` 这类“没有可比结果”的样本不再污染观察结论。

## 六、`small_cap_premium = 0.15` 拆分验证结果

在完成上面的边界样本补观察后，今天继续把 `small_cap_premium = 0.15` 从整组 `quick_method2_core_only` 参数里单独拆出来验证。

验证口径：

- baseline：`price_range_width = 0.12`，`float_size_threshold = 1500`，`small_cap_premium = 0.10`
- 候选：`price_range_width = 0.12`，`float_size_threshold = 1500`，`small_cap_premium = 0.15`
- 样本：`920181`、`920183`、`920177`、`920036`、`920012`

执行命令：

- `python tools\observe_quick_method2_core_only.py --codes 920181,920183,920177,920036,920012 --baseline-price-range-width 0.12 --baseline-float-size-threshold 1500 --baseline-small-cap-premium 0.10 --candidate-price-range-width 0.12 --candidate-float-size-threshold 1500 --candidate-small-cap-premium 0.15 --skip-pdf`

输出文件：

- `输出/观察期/observe_qm2_core_only_prw_0p12_fst_1500_scp_0p15_20260421_095713.json`
- `输出/观察期/observe_qm2_core_only_prw_0p12_fst_1500_scp_0p15_20260421_095713.md`

拆分结果：

- baseline 平均绝对误差：`5.9803`
- 候选平均绝对误差：`6.1774`
- baseline 区间命中率：`0.4000`
- 候选区间命中率：`0.6000`
- 候选优于 baseline：`2/5`
- 候选劣于 baseline：`3/5`

逐样本上看：

- `920181`
  - `0.15` 优于 `0.10`
- `920183`
  - `0.15` 劣于 `0.10`
- `920177`
  - `0.15` 劣于 `0.10`
- `920036`
  - `0.15` 劣于 `0.10`
- `920012`
  - `0.15` 优于 `0.10`

这一步把结论进一步缩小到了一个更明确的权衡：

- `small_cap_premium = 0.15` 能提升一部分真小盘样本的区间命中
- 但在当前这组真小盘样本上，它并没有形成更稳的点估值改善
- 从“平均绝对误差”角度看，`0.10` 反而更稳
- 从“区间命中率”角度看，`0.15` 仍有边际帮助

进一步把这次拆分结果回填到当前 `quick_method2_core_only` 已观察样本池后，可以得到一个近似推断：

- 在原 `12` 只有效样本口径下
  - 当前组合 `0.12 / 1500 / 0.15` 的候选平均绝对误差：`7.9578`
  - 若局部回退为 `0.12 / 1500 / 0.10`，候选平均绝对误差约为：`7.9460`
  - 但区间命中率会从 `0.5000` 降到约 `0.4167`

因此截至今天，更稳妥的判断更新为：

- `small_cap_premium = 0.15` 不再适合作为“默认应保留”的强候选
- 它更像是一个“用区间命中换取部分点估值偏高风险”的激进选项
- 如果当前更重视目标价误差，`small_cap_premium = 0.10` 更安全
- 如果当前更重视区间覆盖，`0.15` 仍有保留理由

## 七、今日一句话结论

今天把 `small_cap_premium = 0.15` 单独拆出来后，结论已经比上午更清楚了：`price_range_width = 0.12` 和 `float_size_threshold = 1500` 仍然站得住，但 `small_cap_premium = 0.15` 更像是偏激进的区间增强项，而不是已经验证完毕的默认保留项。

## 八、保存默认参数与剩余优化空间判断

基于上面的拆分验证，今天已将默认参数正式回退为：

- `策略参数.txt`
  - `small_cap_premium = 0.10`
- `README.md`
  - 当前默认参数状态同步更新为 `0.10`

回归验证：

- `python tests\validate_main_flow_regression.py`
- 结果：`Main flow regression passed: 2 cases`

在“还有没有其他值得继续优化的参数”这个问题上，今天结合现有 quick_method2 离线调参与候选复核结果，又补做了一轮交叉判断。

当前已经能明确的事实是：

- Top 5 离线候选全部共同保留：
  - `price_range_width = 0.12`
  - `small_cap_premium = 0.15`
  - `float_size_threshold = 1500`
  - `pe_discount_boost = 0.10`
- Top 5 里真正变化的只有：
  - `pe_low_threshold = 0.25 / 0.30`
  - `pe_high_threshold = 0.55 / 0.60`
  - `pe_premium_drag = -0.05 / -0.10`
- 但这 5 组候选在验证集上的 `MAE / RMSE / 区间命中率` 完全一致

进一步看最优候选的实际触发情况：

- 训练集里，`pe_factor != 1.0` 的样本只有 `2` 个
- 验证集里，`pe_factor != 1.0` 的样本只有 `1` 个
- 当前观察到的 PE 调整全部是“上调”触发
- 还没有观察到验证集中出现 `pe_factor < 1.0` 的“高 PE 压制”样本

因此截至今天，可以把“剩余优化空间”写得更具体：

- 当前唯一还值得继续看的，是 PE 相关参数组
- 但它们还不具备足够强的真实样本证据
- 其中更接近值得继续观察的是：
  - `pe_low_threshold`
  - `pe_discount_boost`
- 相对不值得现在优先动的是：
  - `pe_high_threshold`
  - `pe_premium_drag`
  - 因为当前连有效触发样本都还不够

所以今天收口后的稳妥判断是：

- 默认参数先定为：
  - `price_range_width = 0.12`
  - `float_size_threshold = 1500`
  - `small_cap_premium = 0.10`
- 如果后面还要继续优化，下一条主线不该再是大范围重调，而应是：
  - 定向补 PE 因子会真实触发的样本
  - 再单独观察 `pe_low_threshold / pe_discount_boost`

## 九、PE 参数组定向优化结果

在完成上面的剩余优化空间判断后，今天继续把 PE 参数组往前推进了一步，但策略不是“整组一起改”，而是先固定已经收口的核心参数，只观察 PE 组本身。

### 1. 新增定向调参与观察工具

今天新增与调整如下：

- `code/param_tuning.py`
  - 新增 `quick_method2_pe_focus` 调参阶段
  - 固定：
    - `price_range_width = 0.12`
    - `small_cap_premium = 0.10`
    - `float_size_threshold = 1500`
  - 仅搜索：
    - `pe_low_threshold`
    - `pe_discount_boost`
    - `pe_high_threshold`
    - `pe_premium_drag`
- `tools/observe_quick_method2_pe_candidate.py`
  - 新增 replay 口径 PE 参数观察工具
  - 直接基于 `replay_dataset` 对比 baseline 与候选，避免实时口径与历史回放口径不一致
- `data/offline_tuning/candidate_sets/quick_method2_pe_candidate_set_v1.json`
- `data/offline_tuning/candidate_sets/quick_method2_pe_candidate_set_v1.md`

### 2. PE 定向调参与候选复核

执行命令：

- `python tools\tune_params.py --stage quick_method2_pe_focus --top-n 8 --train-ratio 0.7 --min-train-samples 8`
- `python tools\review_candidate_params.py --candidate-file data\offline_tuning\candidate_sets\quick_method2_pe_candidate_set_v1.json --review-name quick_method2_pe_candidate_set_v1`

输出文件：

- `输出/调参/tune_params_quick_method2_pe_focus_20260421_102535.json`
- `输出/调参/tune_params_quick_method2_pe_focus_20260421_102535.md`
- `输出/调参/review_candidate_quick_method2_pe_candidate_set_v1_20260421_111647.json`
- `输出/调参/review_candidate_quick_method2_pe_candidate_set_v1_20260421_111647.md`

离线结论：

- PE-focus 最优候选为：
  - `pe_low_threshold = 0.20`
  - `pe_discount_boost = 0.10`
  - `pe_high_threshold = 0.60`
  - `pe_premium_drag = -0.10`
- 相对当前 baseline：
  - 验证集 `MAE(涨幅)`：`52.7245 -> 50.9540`
  - 验证集 `RMSE(涨幅)`：`57.8645 -> 56.7096`
  - 验证集区间命中率：`0.1667 -> 0.3333`

候选复核也支持同一结论：

- `quick_method2_pe_low_0p20_keep_current` 排名第一
- `quick_method2_pe_low_0p20_mild_boost` 与其离线同分，但没有提供额外证据优势
- `quick_method2_pe_low_0p25_keep_current` 与 baseline 持平，不构成有效改进

### 3. replay 观察结果

为了只看真正受 PE 阈值变化影响的样本，今天进一步筛出了当前 replay 口径中 `pe_factor` 会变化的样本：

- `920012`
- `920036`
- `920183`

执行命令：

- `python tools\observe_quick_method2_pe_candidate.py --codes 920012,920036,920183 --pe-low-threshold 0.20 --pe-discount-boost 0.10 --pe-high-threshold 0.60 --pe-premium-drag -0.10 --skip-pdf`

输出文件：

- `输出/观察期/observe_qm2_pe_plt_0p2_pdb_0p1_pht_0p6_ppd_m0p1_20260421_111524.json`
- `输出/观察期/observe_qm2_pe_plt_0p2_pdb_0p1_pht_0p6_ppd_m0p1_20260421_111524.md`

观察结果：

- `920012`
  - `pe_factor: 1.10 -> 1.00`
  - 误差：`8.3568 -> 6.2769`
  - 区间命中：`否 -> 是`
- `920036`
  - `pe_factor: 1.10 -> 1.00`
  - 误差：`73.7884 -> 70.6256`
- `920183`
  - `pe_factor: 1.10 -> 1.00`
  - 误差：`11.6389 -> 8.9785`

合并结果：

- baseline 平均绝对误差：`31.2614`
- 候选平均绝对误差：`28.6270`
- baseline 区间命中率：`0.0000`
- 候选区间命中率：`0.3333`

这一步给出的信号很明确：

- 当前真正有效的 PE 优化，不是“增强 PE 因子”
- 而是把低 PE 加成的触发门槛收紧，减少不必要的上调
- 高 PE 压制相关参数当前仍缺乏足够触发样本，不宜同步吸收

### 4. 正式吸收结果

基于“PE-focus 定向调参 + 候选复核 + replay 观察”三层证据，今天已将默认参数正式更新为：

- `策略参数.txt`
  - `pe_low_threshold = 0.20`
- 其余 PE 参数保持不变：
  - `pe_discount_boost = 0.10`
  - `pe_high_threshold = 0.60`
  - `pe_premium_drag = -0.10`
- `README.md`
  - 当前默认参数状态同步更新

验证情况：

- `python tests\validate_param_tuning.py`
  - 结果：`Param tuning validation passed: 11 cases`
- `python tests\validate_main_flow_regression.py`
  - 结果：`Main flow regression passed: 2 cases`

## 十、今日最终结论

今天这条主线已经从“优化 `quick_method2_core_only`”推进到了更完整的状态：`price_range_width = 0.12`、`float_size_threshold = 1500`、`small_cap_premium = 0.10` 和 `pe_low_threshold = 0.20` 现在都已经有较清楚的证据支撑；剩余未收口的，只剩高 PE 压制那一侧，而且当前优先级已经明显低于其他新任务。

## 十一、主程序公告探测链路收口

今天后半段把主程序侧“招股说明书 / 上市公告书自动探测与下载”这条链路也一起收口了，重点完成了三件事：

- 主程序收到代码后，先检查本地招股说明书和上市公告书；缺任意一个，就启动官网探测
- 官网探测结束后，若仍无招股说明书，则直接终止本次报告生成；若已有招股说明书，则无论上市公告书是否缺失，报告都允许继续生成
- 上市公告书若未命中或下载失败，只提示用户可手动补充，不再额外走其他兜底链路

联调上，`920011` 这类“本地已有招股说明书、但没有上市公告书”的样本已经确认会先进入公告书探测，再继续后续报告流程。也就是说，主程序侧目前最核心的文件依赖判断顺序、探测触发条件和失败出口，已经和今天确定的新口径一致。

基于这个状态，明天主线任务不再建议继续深挖主程序细节，而是切到调参模块，补一个面向用户的手动调参出入口。
