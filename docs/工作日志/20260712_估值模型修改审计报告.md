# 2026-07-11 估值模型修改审计报告

审计日期：2026-07-12

审计范围：2026-07-11 完成并于 2026-07-12 00:23 提交的估值学习流水线、雪球语料与本地学习、自动调参重排、申购账户池修复、三方法职责重构、可比公司解析和公告同步修改。

## 〇、最终收口状态（2026-07-12 22时后）

本节为当前有效结论；后续章节保留开工审计原貌和逐项关闭轨迹，不再代表尚未完成。

- replay解析器版本、行业派生签名、PDF文件签名和统一增量同步均已收口；42只行业分类审计为未分类0、映射未同步0、主营冲突0。
- 两套历史红灯已修复；行业分类与方法二置信度落地后，旧local-center黄金值也已同步到当前正式模型。
- 最终26项验证全部通过，Python全量编译检查通过；当前不存在测试红灯。
- 方法二置信度正式采用20%/20%/35%/40%，中枢按MAD清洗后样本、置信度按清洗前原始样本；仅方法二可用时不再重新归一化到100%。
- 正式参数、replay、盘中指导、proxy、regime-break、统一shadow报告及latest shadow上下文均以当前参数签名重新生成。
- 自动调参正式写回受全样本安全门槛和三折时间切片门槛双重约束；今天未通过时间切片的候选均未写回。
- PDF可比审计45份文件、44个代码，13个原始标记全部处置，未关闭0；920177、920186确认原文无可比公司。
- 920117尚未公布正式上市日期，暂缺待估值文章是正常外部条件，不计为代码或数据漏洞。

完成上述收尾后，剩余事项均为结构性边界，留待下一工作日：

1. 动态宽度/regime仍处于影子层，正式估值保持固定±10%；二级评分与正式部署目标的长期统一仍是架构选择。
2. account-pool分布是公告总量约束下结合历史形状的反推，不是公告直接披露的逐金额账户数。
3. 方法二样本仍少，20%/20%/35%/40%档位需随新增上市样本继续做时间切片验证。
4. 雪球隐式PE折算与显式报价继续同权，为用户已接受口径；如未来降权，应作为模型政策变更而非漏洞修复。

## 一、总体结论

- 昨日代码已集中提交并同步，审计开始时工作树干净。
- 雪球语料、Skill、作者验证、本地 proxy、regime-break、盘中指导、账户池修复、三方法重构和公告双阶段下载均已形成代码与验证资产。
- 当前不能视为完全收口。最严重的问题是可比公司解析器已经修复，但离线 replay 数据集和单样本缓存仍停留在 2026-07-09 的旧解析结果，导致方法一及综合估值回测与正式主流程分叉。
- 21 个验证脚本中 19 个通过，2 个失败；Python 全量编译检查通过。

## 二、逐项完成度

### 1. 雪球语料、Skill 与手工导入

- 已形成 184 篇文章、182 篇可读语料。
- 42 个本地样本中 41 个已形成上市前预测覆盖。
- 未完成项：920117 龙鑫智能仍无可用上市前预测证据。

### 2. 作者区间验证与融合

- 显式/隐式区间抽取、author-rule score 和作者/本地模型融合脚本均已完成。
- 当前结果属于样本内研究上限，没有独立留出集，不能作为前向命中率。
- 隐式 PE 区间虽然在验证输出中带有 `forecast_kind`，但 author-rule score 没有继续传递该字段，折算区间仍会与原文显式报价同权进入评分。

### 3. 本地 proxy、动态宽度、regime-break 与盘中指导

- 研究脚本、影子流水线与 BAT 入口已经形成。
- proxy、动态宽度、regime-break 尚未进入正式估值推理，只进入自动调参二级重排评分。
- 最新影子流水线生成于 2026-07-11 20:15，早于 22 点后的三方法职责和正式参数重构，现有影子结论已过期。

### 4. 自动调参二级重排

- 两级重排已能替换核心最优并写回正式参数。
- 存在选择目标与部署目标不一致：二级评分主要依赖尚未正式部署的动态区间和兜底策略，选出的参数最终仍由固定 ±10% 正式模型运行。
- 自动调参和二级重排都在同一近期样本集合上选择并汇报，没有外层时间留出验证。

### 5. 申购账户池修复

- 已移除人工金额梯度与压缩桶账户数直接拼接的问题。
- 公告账户数/手数方程、单调性、逐样本累计快照和未来函数隔离测试通过。
- 剩余风险属于模型风险：账户曲线仍是公告总量约束下结合历史形状先验的反推值，不是公告直接披露的逐金额账户分布。

### 6. 三方法职责重构

- 方法一已接入可比 PE、发行 PE/行业 PE 和流通盘因子。
- 方法二已改为独立的当年同二级行业稳健统计。
- 方法三已改为短周期市场情绪线。
- 主要残余漏洞：方法二没有最低样本数和小样本置信度缩减，一个同行业样本也可以承担正式 80% 权重。

### 7. 可比公司解析与意向书/说明书同步

- 920072 意向书和说明书均已同步。
- 当前解析器 30 个可比公司黄金样本通过，非法代码数为 0。
- 最新审计仍有 13 个代码带人工复核标记，部分为审计器误报或旧代码迁移差异，尚未逐项关闭。

## 三、严重半截工作：replay 未吸收解析修复

### 现状

- `data/offline_tuning/replay_dataset.json` 最后更新时间为 2026-07-09 23:59。
- `data/offline_tuning/replay_items/*.json` 最后更新时间为 2026-07-09 23:33。
- 可比解析器于 2026-07-11 升级至 `comparable_companies=22`。
- replay 缓存版本仍为 7，PDF 签名只包含文件本身，没有包含解析器版本。

### 对比结果

将 replay 中的 `comparable_codes` 与 2026-07-12 00:20 最新解析审计比较，重叠历史样本中有 20 个代码不一致。典型样本：

- 920076：replay 为空，当前解析为 5 家。
- 920183：replay 为空，当前解析为 3 家。
- 920028：replay 为 `002130.SZ`，当前为 `300980.SZ、300920.SZ`。
- 920078：replay 的两家公司与当前四家公司完全不同。
- 920188：replay 为旧代码 `871694.BJ`，当前为三家现行 `920xxx.BJ`。
- 920218：replay 仍包含无效裸代码 `345013`。

### 影响

- 正式主流程使用新解析器，但离线调参和回放仍使用旧 `comparable_codes/comparable_data`。
- 2026-07-11 的三方法回放中，方法一和综合估值指标不能作为最终结论。
- 需要升级 replay 缓存版本、让缓存签名跟踪解析器版本，并强制重建数据集后重新验证。

## 四、测试状态

### 通过

- 19/21 个验证脚本通过。
- 三方法职责、自动调参、本地重排、账户池、可比公司黄金样本、官网公告选择、Tushare 旧北交所代码迁移等关键测试通过。
- `python -X utf8 -m compileall -q code tools tests` 通过。

### 失败

1. `validate_main_flow_regression.py`
   - 两个端到端报告均生成成功。
   - 失败集中在旧关注提示文案和旧报告章节断言，属于测试口径未同步。

2. `validate_tushare_ipo_data_pipeline.py`
   - 7 条断言失败。
   - 测试写死 2026-04-10/16 样本，同时读取真实 `date.today()` 和 90 天窗口；测试未冻结时钟，已随日期漂移变红。

## 五、收口优先级

1. 升级 replay 缓存版本并强制重建全部历史样本。
2. 核对 20 个可比集合差异是否进入新 replay，并重新取得历史可比 PE 快照。
3. 重跑三方法回放、自动调参和完整影子流水线。
4. 解决自动重排评分与正式部署输出不一致的问题。
5. 增加外层时间留出验证。
6. 给方法二增加最低样本数、置信度缩减和小样本权重上限。
7. 全链路保留 `forecast_kind`，隐式 PE 折算证据单独统计或降权。
8. 修复两套红灯测试，逐项关闭可比解析审计的 13 个复核标记。

## 六、第一优先项执行结果（2026-07-12）

- `REPLAY_ITEM_CACHE_VERSION` 已从 7 升级为 8。
- replay PDF 签名已加入完整 `pdf_parser_versions`；以后 `comparable_companies` 等解析器版本变化会自动使旧单样本缓存失效。
- 已强制重建 `data/offline_tuning/replay_dataset.json`：42 个样本全部重建，版本 8，`evaluation_scope=composite`。
- 已将重建后的 42 个样本全部物化到 `data/offline_tuning/replay_items/`，缓存版本均为 8。
- 当前 replay 的 `comparable_codes` 与最新解析审计重新比较，原 20 个差异已全部归零，`mismatch_count=0`。
- 新 replay 的 `method1_ready_count` 为 42/42；其中没有历史可比 PE 的样本会按正式参数使用行业 PE 低置信度兜底，不代表 42 只都取得了直接上市可比 PE。
- 920081 的本地首日分时均价首次进入 `listing_average_prices.json`，因此本次重建同时更新了该样本的实际均价缓存。

验证结果：

- `validate_param_tuning.py`：通过。
- `validate_sync_offline_tuning_dataset.py`：通过。
- `validate_valuation_method_roles.py`：4 个用例通过。
- `validate_pdf_content_goldens.py`：30 个可比公司、5 个业务描述黄金样本通过。

## 七、前两项入口漏洞收口（2026-07-12）

### 1. 时间切片门槛接入正式写回

- `tools/tune_params.py` 在全样本正式门槛之后强制执行三折时间切片复核。
- 核心路径/雪球两级路径按本轮实际执行状态分别校验；指标失败、参数漂移或复核执行异常均拒绝写入。
- 显式应急绕过会记录 `bypassed_by_explicit_flag`，不存在静默跳过。
- 端到端实测：`float_size_threshold=800` 通过全样本门槛，但时间切片失败，入口返回拒绝且没有写入正式参数。

### 2. latest影子上下文与报告刷新

- 上下文升级为 `valuation_auto_shadow_context_v2`，latest已更新为2026-07-12 20:54的 `rejected_time_slice`。
- 被拒候选保留供审计，但 `top_candidates` 回退为正式参数；模型、数据、参数和源码签名已写入。
- 修复上下文区间命中长期显示0/0的问题，当前正式参数近期样本为8/21。
- 已重新生成本地proxy、regime-break、盘中指导和统一shadow报告，统一报告时间为2026-07-12 20:54。

### 3. 方法二小样本雪球作者参考

- 原统计使用了去极值后的 `sample_count`，因此“没有达到4只”不能用于置信度判断。置信度改按去极值前 `raw_sample_count` 理解，中枢仍按去极值后的样本计算。
- 补齐朗信电气、永励精密汽配映射后：朗信电气为原始4只/清洗后3只，乔路铭和永励精密均为原始5只/清洗后3只。
- 本地雪球作者通常把单只近期同类IPO作为交叉验证；主锚仍来自高质量可比公司或行业PE，市场平均涨幅单列为情绪层。
- 建议方法二按1/2/3/4只样本采用25%/50%/75%/100%置信度，将80%名义权重上限缩减为20%/40%/60%/80%；本轮只记录方案，不修改正式参数。
- 专项记录：`docs/工作日志/20260712_方法二小样本权重雪球作者处理参考.md`。

## 八、剩余审计项与行业分类审计（2026-07-12）

- 两个红灯测试均已修复：主流程回归断言已同步双估值区间和最新方法职责；Tushare 管道测试冻结到固定日期，不再随真实日期窗口漂移。
- `forecast_kind` 已进入雪球作者证据记录；按用户决定，显式报价与隐式 PE 折算目前继续同权，但保留可审计的类型和权重乘数，便于以后单独降权。
- 可比解析覆盖审计已完成：45份招股文件、44个代码、13个原始启发式标记全部完成人工处置，未关闭标记0个；920177、920186确认原文确实无可比公司，其余11个为审计器误报。
- 920117尚未公布正式上市日期，没有明确待估值预测文章属于正常状态，保留后续人工补文入口。
- 确认朗信电气、永励精密原来均漏分行业，现已归为“高端装备 / 汽车零部件”。
- replay 刷新契约新增行业派生签名、签名字段集和 PDF 解析器版本；同时统一入口会检查已缓存 replay 的记录签名与 PDF 文件签名。实际只重建上述2只，复用其余40只；申购 history重建2只，account-pool从最早变化样本向后重算。
- 行业分类审计覆盖42只：17只未分类，另有5只现有分类需要语义复核；没有发现映射代码已改但 replay 未同步的样本。
- 5只已分类待复核：920076国亮新材（耐火材料却归金属新材料）、920119美德乐（智能制造装备却归仪器仪表）、920188悦龙科技（高分子软管却归机械设备）、920012创达新材（热固性复合材料却归半导体制造）、920159农大科技（复合肥的农业链/化工口径边界）。
- 17只未分类均已在报告中给出基于主营而非下游应用的建议；920081欧伦电气主营摘要混入可比公司文字，需优先回看原文；920012、920159、920186也建议人工确定边界后再批量写入映射。
- 报告：`outputs/industry_classification_audit_latest.md/json`；工具：`tools/audit_industry_classification.py`。
- 收口回归：主流程2例、Tushare管道7例、统一同步、参数调优、行业映射验证均通过；Python编译检查通过。

### 用户确认的四只边界样本

- 920186中科仪：`信息技术 / 半导体制造`。
- 920081欧伦电气：主营除湿机/空调，按现有家用电器统一映射落为`消费服务 / 消费电子`。
- 920012创达新材：半导体封装材料，保留`信息技术 / 半导体制造`。
- 920159农大科技：化肥，改为`化工新材 / 化学制品`。
- 增量同步仅重建实际发生映射变化的920081、920159、920186三只；920012原映射已正确。行业审计现剩18只待复核，其中15只未分类、3只已分类语义待复核。

### 行业分类最终收口与模型影响

- 用户接受剩余审计建议：国亮新材、瑞尔竞达统一为耐火材料/非金属材料；美德乐归机械设备；悦龙科技按高分子软管归橡塑制品；鸿仕达归半导体链；其余未分类样本按主营建议落库。
- 欧伦电气主营除湿机/空调，用户明确要求传统家电与带AI/高科技加成的消费电子拆分；正式映射及全局参数均改为`消费服务 / 家用电器`。
- 最终42只行业审计全部通过：未分类0、映射未同步0、主营冲突0、人工待复核0。
- 方法二可用标的增至31只；原始同行历史样本分布为1只×9、2只×7、3只×5、4只×4、5只×3、6/7/8只各1。新激活样本多数仍处于低置信度区间。
- 使用同一正式参数和模型、仅切换本轮最终行业分类进行模拟：分类前区间命中27.5%、MAE128.29、P90 318.34；分类后区间命中12.5%、MAE135.39、P90 313.10。
- 该变化不应通过回滚正确行业分类处理；它是方法二小样本80%名义权重漏洞的新增实证，应在后续实现“中枢按清洗后样本、置信度按去极值前样本”的缩权方案。

### 方法二置信度漏洞关闭

- 已实现“中枢按MAD清洗后样本、置信度按去极值前原始样本”的分离口径，并将置信度写入方法二结果和正式报告。
- 仅方法二可用时不再把低置信度重新归一化到100%，剩余权重使用发行价保守锚。
- 1371组单调档位扫描后正式采用20%/20%/35%/40%；相对不降权，命中保持12.5%，MAE 135.39→130.88，P90 313.10→305.32，最差误差651.52→626.92。
- 后22只锚定验证区间的20只可评价样本中，命中保持10%，MAE 168.47→159.63，P90 355.29→340.30。
- 该组参数已写入正式`策略参数.txt`并纳入composite自动调参参数集；专项记录见`20260712_方法二小样本置信度降权.md`。
