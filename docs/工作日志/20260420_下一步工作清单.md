# 北交所新股估值项目下一步工作清单
创建日期：2026-04-20  
适用场景：在综合权重已正式吸收、`WSI` 扩组已形成结论、`trend_balance` 两轮真实观察均未支持吸收后，作为下一轮工作的执行清单。

## 一、当前阶段统一判断

截至 2026-04-20 当前时点，已经完成：

- 综合回放数据集正式切到 `composite`
- 方法一历史快照已能进入离线回放
- 综合权重首轮真实调参与候选复核完成
- `0.20 / 0.80` 已通过真实样本观察并正式吸收为默认参数
- `WSI` 换手权重扩组已完成，并得到“当前无稳定增益”的阶段性结论
- `trend_balance` 首轮真实调参与候选复核完成
- `trend_balance` 两轮真实样本观察完成

当前已经形成的明确结论是：

- 默认综合权重维持 `weight_comparable = 0.20`
- 默认综合权重维持 `weight_industry_momentum = 0.80`
- `WSI` 当前不再是第一优先级主线
- `trend_balance` 当前不建议继续吸收，也不建议继续作为主攻线

## 二、第一优先级：继续定向观察 quick_method2_core_only

当前最自然的下一步，仍然是围绕 `quick_method2_core_only` 做更有针对性的观察。

### 原因

- 综合权重已经正式吸收，不需要继续回头调
- `WSI` 扩组已经形成“当前无稳定增益”的阶段性结论
- `trend_balance` 虽然离线回放更优，但两轮共 `7` 只真实样本里 `0` 只优于 baseline，暂时不值得继续硬推
- `quick_method2_core_only` 两轮共 `7` 只真实样本里整体优于旧 baseline，值得继续保留并做更聚焦的补观察

### 建议做法

1. 优先补 `1500 ~ 2000` 万股流通盘的边界样本  
   重点判断 `float_size_threshold = 1500` 是否比 `2000` 更合理
2. 补少量明确小盘但容易上抬过头的样本  
   重点看 `small_cap_premium = 0.15` 是否在个别高端装备样本上偏激进
3. 继续对比 baseline 与 `core_only` 候选的目标价、区间和真实收盘误差  
   特别关注区间命中改善是否稳定保留
4. 给出阶段性判断  
   明确写成“继续保留 / 继续观察 / 局部回退”

### 完成标准

- 能判断这组 `core_only` 参数是否可以从“继续保留”升级成“正式固化”

## 三、第二优先级：trend_balance 暂停吸收，先不继续补同方向观察

当前这条线已经具备足够明确的真实样本证据：

- 两轮合计 `7` 只样本
- 候选优于 baseline：`0/7`
- 候选劣于 baseline：`4/7`
- 候选持平：`3/7`

因此后续处理原则应调整为：

- 先不继续补第三轮相同方向观察
- 先不把 `0.70 / 0.30` 当成待吸收候选
- 如后续要重开这条线，应以“新假设”而不是“继续硬推当前候选”为前提

## 四、第三优先级：WSI 继续低优先级保留

`WSI` 换手权重这条线今天已经补到足够清楚：

- 正向换手权重扩组已跑完
- baseline 仍优于所有当前候选

因此后续处理原则维持不变：

- 先不继续把 `WSI` 作为第一主线
- 先保留维度，不删除
- 只有等其他主线收口后，再决定是否开下一批更异质的候选

## 五、当前不建议优先做的事

- 不要刚吸收完综合权重，又立刻重新开一轮综合权重搜索
- 不要继续为 `920028 / 920188` 的方法一缺口停住主线
- 不要把 `WSI` 扩组继续作为当前第一优先级
- 不要现在就把 `industry_trend_weight = 0.70` 直接写入 `策略参数.txt`
- 不要因为 `trend_balance` 离线更优，就继续补第三轮同方向观察

## 六、一句话结论

下一步真正该做的，不是继续硬推 `trend_balance`，而是围绕 `quick_method2_core_only` 补“边界流通盘样本”这类更有判别力的观察，把这条线从“继续保留”推进到“能否正式固化”的结论。
