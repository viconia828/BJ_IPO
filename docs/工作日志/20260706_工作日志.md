# 2026-07-06 工作日志

## 申购冻资高估复核

- 复核 920079 乔路铭申购规模估算，确认此前约 2.3 万亿元冻资来自历史倍数模型外推过高，进而把 `1+0` 门槛推高到不合理水平。
- 对比 BAT 正式生成结果与离线公告解析样本，确认 `11959.47` 亿元不是 920079 自身缓存错误，而是正式流程历史样本仍在使用接口粗口径倍数，未优先采用公告解析出的真实有效申购股数。

## 公告真实申购数据接入

- 在主流程 `build_analysis_data()` 中新增申购历史样本覆盖层：正式估值进入申购预测前，使用 `data/offline_tuning/subscription_history_sample.csv` 覆盖 `recent_ipos` 中的 `ONLINE_VA_SHARES`、`FROZEN_FUNDS_YI`、`ONLINE_ES_MULTIPLE`、发行日期、顶格金额等字段。
- 覆盖摘要写入 `ipo_data_summary["subscription_history_overlay"]`，便于后续排查正式估值是否命中了公告解析历史样本。
- 新增主流程回归测试，构造接口粗倍数与公告真实有效申购股数冲突的样本，确认正式流程优先使用公告解析真实口径。

## 历史冻资上限

- 在申购预测器中新增历史冻资上限机制：估算冻资超过近期公告真实冻资分布上沿时，按最近样本上限进行压制，避免发行规模外推导致冻资明显偏高。
- 在 `策略参数.txt` 显式增加 `subscription_prediction_frozen_funds_cap_*` 参数，当前默认口径为最近真实冻资最大值上浮 10%。
- 报告明细中新增“冻结资金上限”行，展示上限是否生效及对应上限值。

## 920079 复算结果

- 正式流程复算后，920079 冻资为 `12636.57` 亿元，`1+0` 门槛为 `280.88` 万元。
- 本次上限锚点来自公告真实历史样本最大冻资 `11487.79` 亿元，乘以 `1.10` 安全垫后得到 `12636.57` 亿元。
- 申购历史覆盖命中 13 条近期样本，已从接口粗倍数口径切换到公告解析真实有效申购股数口径。

## 验证

- 已通过 `python -X utf8 tests\validate_subscription_predictor.py`。
- 已通过 `python -X utf8 tests\validate_main_flow_regression.py`。
- 已通过 `python -X utf8 -m py_compile code\bse_ipo_valuation.py code\subscription_predictor.py tests\validate_main_flow_regression.py tests\validate_subscription_predictor.py`。

## 离线样本缓存与户数表重构

- 复核 920189 康美特估值报告中的申购规模门槛问题，确认正股线 `446.8046` 万元与实际 `447` 万元基本一致，原先主要问题在碎股线估算口径。
- 梳理并统一 replay 样本新增来源：首日分时样本与手工梯度表。模式 2 更新样本缓存时检查这两个入口，补齐公告解析缓存，并由公告缓存继续派生申购 history、户数表和估值调参样本。
- 调整手工梯度表与 replay dataset 的关系：手工梯度表内新增但 replay dataset 尚未覆盖的代码，也会在更新缓存时进入公告下载/解析流程；公告未出齐时记录跳过状态，避免当日重复重试。
- 将样本缓存定位为“调参估值共用的补全样本集”，分时走势仅作为发现入口之一，不再把申购 history、户数表和调参输入分散维护。

## 新户数表生成逻辑

- 重写 `tools/build_account_pool_history.py` 的户数表构建方式：按申购日期顺序处理样本，只把新样本真实提供的切点加入表，不再强制生成固定的 100 万到 5000 万插值网格。
- 每获得一个新样本，就用它的观测切点校准当前状态；旧点与新点冲突时按新点约束旧点，保证“门槛越高，户数不应越多”的单调关系。
- `account_pool_history_thresholds.csv` 现在保存观测快照状态，调用预测器时再对请求金额做运行时插值，插值结果不写回户数表本身。
- `code/subscription_predictor.py` 增加小数切点列名解析和最新观测快照读取逻辑，兼容 `accounts_ge_446p8w_estimate` 这类小数门槛列。

## 920189 模型-only 复核

- 在不使用 920189 自身实际梯度/碎股标签的口径下，保留其实际有效申购量和顶格金额来固定配售比例，使用新户数表估算碎股线。
- 严格事前口径（仅用 2026-06-29 前户数快照）得到碎股线 `612.942` 万元。
- 重建户数表并彻底排除 920189 样本、但允许使用其他后续样本时，碎股线仍为 `612.942` 万元。
- 仅删除当前表中的 920189 本行但不重建时会得到 `631.0942` 万元；该口径会被后续快照 carry forward 的 920189 切点污染，仅作为反例参考。
- 当前模型-only 碎股线约 `613` 万元，较实际 `631` 万元低约 `18` 万元，在可接受偏差内。

## 本轮验证

- 已通过 `python -X utf8 -m py_compile code\param_tuning.py code\subscription_ladder_labels.py code\subscription_predictor.py tools\build_account_pool_history.py tools\build_subscription_history.py tools\sync_offline_tuning_dataset.py tools\tune_params.py tools\tune_subscription_prediction.py`。
- 已通过 `python -X utf8 tests\validate_account_pool_history_builder.py`。
- 已通过 `python -X utf8 tests\validate_subscription_predictor.py`。
- 已通过 `python -X utf8 tests\validate_sync_offline_tuning_dataset.py`。
- 已通过 `python -X utf8 tests\validate_param_tuning.py`。
- 已通过 `python -X utf8 tests\validate_subscription_history_builder.py`。
- 提交前复跑 `python -X utf8 tests\validate_subscription_prediction_tuning.py` 时运行时间异常偏长且无输出，已中断；该项此前在本轮代码修改后通过，后续如继续变慢需单独排查候选搜索性能。

