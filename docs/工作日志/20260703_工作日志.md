# 2026-07-03 工作日志

## 远端同步与冲突处理

- 执行远端最新版本同步；因本地数据和参数文件存在改动，先保存本地现场后完成 `git pull --ff-only`。
- 按用户指示处理冲突：策略参数文件完全采用远端版本；数据文件冲突接受远端版本，后续如需调整再由用户单独修改。

## 申购资金户数表更新逻辑

- 梳理申购资金户数表现有链路，确认此前并非完整的“最新发行结果公告推导值优先覆盖历史分布表”。
- 调整离线调参同步流程：在重建申购历史和同步梯度标签后，自动刷新 `account_pool_history_*` 户数分布数据。
- 调整申购历史合并策略：新的 `model_ready` 发行结果公告解析行优先覆盖旧行；只有新解析不可用时，才保留旧的高质量行作为兜底。
- 为户数分布构建摘要补充 `summary_path`，便于同步流程回传和追踪产物位置。

## 测试与验证

- 已通过 `python -m py_compile tools\sync_offline_tuning_dataset.py tools\build_subscription_history.py tools\build_account_pool_history.py tests\validate_sync_offline_tuning_dataset.py tests\validate_account_pool_history_builder.py tests\validate_subscription_history_builder.py`。
- 已通过 `python tests\validate_subscription_history_builder.py`。
- 已通过 `python tests\validate_sync_offline_tuning_dataset.py`。
- 已通过 `python tests\validate_account_pool_history_builder.py`。
- `python tests\validate_subscription_prediction_tuning.py` 本轮未完成，不作为已通过验证项。

## 项目协作规则

- 新增 `agent.md`，写入三条项目级规则：不要随便停止非自己发起的进程；每天工作要边做边记录日志；PowerShell 中花括号、尖括号和引号要按语法规则正确引用和转义。

## 申购分档展示口径修正

- 根据 920117 龙鑫智能案例，修正一览和申购预测表的分档展示口径：`lot_thresholds` 保留完整计算结果，但新增 `display` 标记，展示层只输出每个最终获配手数对应的最低门槛构成。
- 当同一最终手数的正股档和碎股加配档金额相同或碎股加配更优时，优先显示 `+1` 碎股构成，避免把实际会多拿碎股的档位误写成 `2+0`、`3+0`。
- 龙鑫智能这类碎股门槛落在 1-2 手正股之间的场景，展示结果应为 `1+0`、`1+1`、`2+1`、`3+1`，不再输出被覆盖的 `2+0`、`3+0`。
- 对照外部资金分档公式复核后，确认当前逻辑仍遵循“正股按比例取整、剩余碎股按申购金额从高到低补一手”的配售规则；本轮调整只影响展示选择，不改变正股门槛和碎股门槛的基础计算。

## 本轮验证

- 已通过 `python tests\validate_subscription_predictor.py`。
- 已通过 `python tests\validate_main_flow_regression.py`。
- 已通过 `python -m py_compile code\subscription_predictor.py code\report_generator.py tests\validate_subscription_predictor.py tests\validate_main_flow_regression.py`。

## 离线调参数据集补充提交

- 按项目约定，补充提交本轮已刷新的申购调参训练表和账户资金池参数集产物：`subscription_history_sample.csv`、`account_pool_history_points.csv`、`account_pool_history_thresholds.csv`、`account_pool_history_summary.json`、`sample_manifest.json`、`deferred_listing_data_codes.json`。
- 本批数据变化主要来自统一同步流程刷新：补入 920081 欧伦电气手工分档到账户池长表/阈值表，修正 920125 等样本的网上发行数量口径，并刷新 manifest、retry marker 和账户池摘要统计。
