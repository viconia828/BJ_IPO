# 2026-07-30 工作日志

## 暂时停用估值自动调参三折时间切片写回门槛

- 用户确认旧版 `20只训练 + 7只验证 + 7只验证 + 剩余验证` 属于固定样本切分，后续拟改为自然季度 walk-forward。
- 四个季度的数据可形成三个“历史训练前缀 → 下一季度验证”步进；当前只有三个季度数据，只能形成两折，因此本次不直接修改折边界算法。
- `tools/tune_params.py` 默认暂时不执行三折时间切片写回门槛；自动调参仍必须通过全样本命中率、MAE、P90 和可用率安全门槛。
- 停用状态结构化记录为 `enforced=false`、`bypassed=true`、`status=disabled_pending_calendar_walk_forward`，调参记录显示“暂时停用”，避免误报为检验通过。
- 保留 `tools/revalidate_valuation_time_slices.py` 独立复核工具；研究性复现旧版固定样本门槛时可显式传入 `--auto-time-slice-gate`。
- 保留 `--no-auto-time-slice-gate` 作为旧命令行兼容参数。

验证结果：

- `python -X utf8 tests\validate_param_tuning.py`：通过；覆盖默认停用、结构化停用状态、自动调参继续写回，以及显式启用旧门槛后的拒绝路径。
- `python -X utf8 tests\validate_valuation_time_slice_stability.py`：通过；独立旧版复核工具未受影响。

## 当日数据与调参产物提交

- 补齐森合高科 `920038` 发行结果数据并清空相应待重试标记，刷新申购 history、账户池历史点、阈值快照、摘要和样本 manifest。
- 纳入 `920065` 首日分时走势 CSV。
- 保存当天申购资金自动调参正式参数与完整接受记录；最新一轮 44 只评估样本的 MAE 从 40.1462 万元降至 36.2473 万元，MAPE 从 7.15% 降至 6.51%。
- 提交前补充验证：
  - `python -X utf8 tests\validate_subscription_prediction_tuning.py`：通过。
  - `python -X utf8 tests\validate_subscription_predictor.py`：通过。
  - `python -X utf8 tests\validate_subscription_history_builder.py`：通过。
  - `python -X utf8 tests\validate_account_pool_history_builder.py`：通过。
  - `git diff --check`：通过；仅有仓库既有换行符转换提示。
- `git fetch origin`：完成；提交前本地 `main` 与 `origin/main` 均位于 `261023a`。
