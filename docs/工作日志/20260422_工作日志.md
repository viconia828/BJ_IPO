# 北交所新股估值项目工作日志
日期：2026-04-22  
记录人：Codex  
目标：完成手动调参入口收尾、统一 replay 报告口径，并做一轮阶段性清理与文档整理。

## 一、今日完成事项

今天的工作重点已经从“继续扩实验”切到“把现有成果收口并整理成可持续使用的状态”，主要完成了以下几件事：

- 完成手动调参批处理入口：
  - 新增根目录 `调参.bat`
  - 新增 `tools/manual_tune_prompt.py`
  - 统一复用 `tools/tune_params.py`
- 完成手动调参统一入口：
  - `tools/tune_params.py` 支持 `search / offline / observe`
  - 支持 `--param-name + --candidate-values`
  - 支持重复 `--candidate`
  - 支持 `--candidate-file`
- 把调参运行参数并入 `策略参数.txt` 的独立分类：
  - `tuning_replay_months`
  - `tuning_page_size`
  - `tuning_train_ratio`
  - `tuning_min_train_samples`
  - `tuning_top_n`
- 补齐调参报告中的“前后对比”和“真实结果对比”：
  - 离线报告新增前后汇总
  - 验证集/全样本逐样本对比更直观
- 优化 replay 观察报告展示：
  - 默认只展示调参前后发生变化的样本
  - 保留全量原始结果到 JSON
- 修正 replay 统计口径表达：
  - 报告新增“不可比较但前后相同样本”
  - 解决“无变化样本”和“已省略未变化样本”不易直接对齐的问题
- 完成手动权重调参自动归一化：
  - 二因子组只输入一个时，自动补足到 1
  - 多因子组只输入一个时，其余因子按当前参数比例自动缩放

## 二、阶段性清理

今天确认并清理了几类已经不再需要保留的内容：

- 删除早期按单一主题拆分的专用观察脚本：
  - `tools/observe_composite_weight_candidate.py`
  - `tools/observe_trend_balance_candidate.py`
  - `tools/observe_quick_method2_core_only.py`
  - `tools/observe_quick_method2_pe_candidate.py`
- 删除已被统一入口替代的数据集升级脚本：
  - `tools/upgrade_replay_dataset_composite.py`
- 清理测试临时目录：
  - 删除 `tests/_tmp/`
  - 将 `tests/_tmp/` 加入 `.gitignore`

本轮清理后的原则是：

- 保留“用户能直接用”的入口
- 保留“当前仍有明确职责”的工具
- 删除“已经被统一入口覆盖”的重复脚本
- 不清理用户真实使用中的本地数据目录与报告目录

## 三、README 重写

今天把 README 从偏“仓库结构说明”改成偏“使用说明”：

- 把“怎么用”放到最前面
- 把 `运行.bat / 调参.bat / 添加新股首日走势.bat` 作为主入口
- 明确输出目录、常见命令和调参报告口径
- 保留少量高级说明，但不再要求使用者先理解内部模块结构

现在 README 更适合作为交付给最终使用者的首页说明。

## 四、当前项目状态

截至今天，项目已经基本进入“阶段性可收尾”状态：

- 主程序估值链路已稳定
- 北交所官网公告下载链路已稳定
- 手动调参、离线复核、replay 观察已统一入口
- 调参运行参数已统一进 `策略参数.txt`
- replay 报告口径已明显更易读
- 早期重复脚本已经收缩

可以把当前项目理解为：

- 已经不是“继续快速试错的原型”
- 而是一套“可直接日常使用，并可继续小步迭代”的本地工具

## 五、验证

今天继续用统一回归脚本做了验证：

- `python tests\validate_param_tuning.py`

结果：

- `Param tuning validation passed: 20 cases`

## 六、当前建议

当前不建议再继续做大范围结构调整。

后续如果继续推进，更适合做的是：

- 按新入口做真实参数实验
- 基于新增报告口径筛选高价值样本
- 只针对仍然存在歧义的参数做小范围复核

如果没有新的业务需求，这一阶段已经可以视为完成阶段性收尾。
