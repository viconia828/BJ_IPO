# 北交所新股估值项目下一步工作 List

创建日期：2026-04-16  
适用场景：在本轮 PDF 三条提取链路已做完一轮定向修正后，继续推进全项目收口时直接照单执行

## 一、基于最新规则的全量复扫已完成，当前先补验证资产

当前复扫结论：

- `old_shares_hits = 25`
- `comparables_hits = 23`
- `business_desc_hits = 44`
- 当前剩余显著尾项主要为：
  - `920186` 的 `comparables` 合理空结果
  - `920011` / `920181` 的 `comparables` 可能仍属于部分漏提

因此下一轮优先事项不再是重复复扫，而是：

- 把今天修复过的典型样本补进黄金样本
- 必要时新增定向验证脚本，至少覆盖：
  - `920036` / `920069` / `920086` / `920180` 的 `old_shares`
  - `920055` / `920119` 的 `comparables`
  - `920029` / `920119` / `920180` 的 `business_desc`

建议原则：

- 优先补“之前失败、今天修好、且失败模式具有代表性”的样本
- 先沉淀最小验证，不急着做很大的自动化框架

## 二、做 1 到 2 个样本的最小主流程回归

当复扫确认局部规则基本稳定后，再做主流程最小回归。

优先样本建议：

- 一个以上市公告书 `old_shares` 修复为主的样本
  - 例如 `920180` 或 `920086`
- 一个以招股文件 `comparables + business_desc` 修复为主的样本
  - 例如 `920119`

回归时重点检查：

- 首日流通老股
- 可比公司代码列表
- 可比公司估值表
- 公司简介
- 方法一目标价
- 综合估值
- 提示文案

## 三、方法一数据源链路继续收口

PDF 规则本轮推进较大后，项目整体还需要回头确认方法一链路没有掉队。

下一轮建议完成：

- 再做一轮 `Wind 原料字段 -> 本地计算` 的小样本验证
- 再做一轮东方财富快照口径检查
- 再确认本地文件数据库、缓存优先、请求台账、quota 控制在重复运行时正常

约束：

- 继续坚持“小样本交叉验证优先”
- 不直接扩大为大范围联网扫描
- `PB` 仍不作为当前阻塞项

## 四、是否继续追 `comparables` 的部分漏提

本轮已完成最终判断与修正：

- `920011晨光电机招股说明书.pdf`
  - 已由原先仅 `1` 家修正为 `6` 家
- `920181赛英电子招股说明书.pdf`
  - 已由原先仅 `1` 家修正为 `3` 家
  - 供应商误带入问题已排除

因此这一项已不再作为下一轮主任务，后续仅需在全量复扫或回归时持续观察是否回归破坏。

## 五、报告展示与文案收口

项目整体层面仍未完成的非提取类工作包括：

- 检查报告中的数据源说明是否已和当前真实链路一致
- 评估是否展示方法二实际采用的历史样本代码
- 评估是否更明确展示时间衰减加权口径与样本范围
- 视回归结果决定是否单独提示“上市公告书未识别，已回退招股文件”

## 六、性能与稳定性继续盯住

下一轮推进时继续观察：

- 是否仍存在单票运行超时
- PDF 是否存在重复完整解析
- 文本缓存、文件选择和重复调用链路是否被后续改动破坏

## 七、收工前必须补记的内容

下一轮无论做到哪里，结束前至少补记以下内容到当日日志：

- 新增或更新的黄金样本
- 最小主流程回归结果
- 如继续修 `comparables`，记录 `920011` / `920181` 的最终判断
- 当前仍未完成的大块与下一接续点

## 八、基于本轮续做的状态更新

本轮已完成：

- 已把代表性修复样本补入验证资产
  - `validate_pdf_old_shares_goldens.py` 已扩充到 `10` 个样本
  - `validate_pdf_content_goldens.py` 已扩充到 `7` 个 `comparables` 样本、`5` 个 `business_desc` 样本
- 已新增 `validate_main_flow_regression.py`
  - 当前用真实 PDF 提取 + 本地 mock IPO / 可比快照
  - 已完成 `920180`、`920119` 两个样本的最小主流程回归
- 已新增 `validate_method1_data_pipeline.py`
  - 已覆盖 `4` 个方法一数据链路场景：
    - 东方财富补位
    - Wind 缓存命中 + 本地计算
    - Wind 刷新 + 东方财富交叉验证
    - quota 耗尽 + 沿用旧快照
- 已完成 `920011` / `920181` 的 `comparables` 尾项收口

因此下一轮的真实优先级顺序建议更新为：

### 1. 方法一数据源链路继续收口

- 在现有离线验证脚本基础上，再做一轮“真实缓存 / 真实口径”的小样本复核
- 再检查东方财富快照口径说明与报告文案是否一致
- 继续确认本地文件数据库 / 缓存 / quota 控制在重复运行时稳定

### 2. 报告展示与说明文案收口

- 检查数据源说明是否和当前真实链路完全一致
- 评估是否展示方法二历史样本代码
- 评估是否更明确展示时间衰减加权口径与样本范围
- 结合真实主流程回归结果，决定是否单独提示“上市公告书未识别，已回退招股文件”

### 3. 如有精力，再补一轮真实联网最小主流程实跑

- 当前已具备离线回归入口
- 后续若要进一步收口，可再补 `1` 到 `2` 个基于真实数据源的最小主流程实跑样本

## 九、基于 `920177` 实跑后的即时接续顺序

本轮已经用当日新股 `920177恒道科技` 做了一次真实主流程实跑，并已生成：

- `输出/920177_恒道科技_估值_20260416.md`

首次实跑暴露出的新增问题是：

- `920177` 未录入行业映射，导致报告显示 `未分类`，方法二退回 `全市场`
- 顶部“数据来源”文案出现 `东方财富` 重复表述

这两个问题本轮都已经先做了代码修补，因此下一轮的最优先顺序应调整为：

### 1. 先重跑并复核 `920177`

- 重新联网执行：
  - `python bse_ipo_valuation.py 920177`
- 重点检查：
  - 行业是否已变为 `高端装备 / 机械设备`
  - 方法二是否不再退回 `全市场`
  - 数据来源文案是否已去掉重复 `东方财富`
- 若仍有展示或口径问题，继续沿 `920177` 这个真实样本当场修完

### 2. 在 `920177` 修补确认后，继续做报告文案收口

- 继续检查数据来源说明是否和真实链路完全一致
- 评估是否需要更明确展示方法二样本范围与历史样本代码
- 评估是否需要单独提示“上市公告书未识别，已回退招股文件”

### 3. 然后再回到方法一“真实缓存 / 真实口径”小样本复核

- 复核真实缓存命中时的口径说明
- 复核东方财富快照与本地缓存 / quota 控制的配合
- 持续观察重复运行稳定性与单票耗时

## 十、基于 `920177` 复跑确认后的最新接续顺序

本轮已完成并确认：

- `920177` 已再次联网实跑通过
  - 行业已稳定显示为 `高端装备 / 机械设备`
  - 方法二已不再回退 `全市场`
  - 顶部数据来源文案已去掉重复 `东方财富`
- 报告展示层已新增：
  - `方法二样本范围`
  - `方法二实际样本代码`
- 首日流通老股在“上市公告书未提取到有效结果 -> 回退招股文件”场景下，已具备显式提示能力

因此，`920177` 这一轮真实样本已不再是下一轮主任务；下一轮优先级建议进一步更新为：

### 1. 方法一“真实缓存 / 真实口径”小样本复核

- 选 `1` 到 `2` 只真实样本，连续重复运行
- 重点核对：
  - 可比快照说明是否准确区分 `实时抓取` / `缓存命中`
  - 重复运行时是否稳定命中本地缓存
  - quota / 旧快照 / 东方财富补位说明是否与真实行为一致

### 2. 性能与稳定性观察

- 当前 `920177` 单票再次实跑耗时约 `40` 秒，后续需要继续观察：
  - 是否存在重复完整解析 PDF
  - 是否存在可避免的重复数据抓取
  - 重复运行时单票耗时是否继续下降或保持稳定

### 3. 报告展示层后续只做“高价值增量”

当前已能显式展示方法二样本范围与样本代码，因此后续若继续动报告展示，建议只考虑以下剩余问题：

- 是否还需要补更细的样本来源解释，而不是继续堆更多字段
- 是否需要在真实回退样本中观察“招股文件回退提示”的可读性
- 是否需要把方法二样本代码与下方“近期新股列表”的关系讲得更明确

## 十一、基于本轮入口开发后的最新优先级

本轮已完成：

- `运行.bat` 已可作为最终用户入口
- 用户无命令行参数启动时，已支持：
  - 输入 `6` 位数字代码
  - 非法输入拦截
  - 直接回车退出
- 生成成功后，已明确提示：
  - 报告完整路径
  - 报告所在目录
  - 去 `输出` 文件夹查看

因此，接下来暂不围绕旧数据源继续深挖，入口相关的下一步优先级建议更新为：

### 1. 等新数据源方案确定后，再切主流程拉数链路

- 当前入口层已经足够支撑后续替换数据源
- 后续重点应放在：
  - `fetch_ipo_info`
  - 近期样本拉取
  - 可比公司估值数据源

### 2. 入口层如继续补，只做用户体验增强

如要继续打磨入口，建议只补以下高价值小项：

- 增加“生成中，请稍候”提示
- 失败提示改成更偏用户语言，而不是直接暴露底层异常类别
- 视需要增加“生成成功后自动打开输出目录”能力

### 3. 文档与交接维持当前最小闭环

当前已经有：

- 批处理入口
- 代码内提示词
- `说明文档.md` 中的使用示例

因此这一块暂不需要再扩写更多文档，除非后续数据源切换导致交互方式变化

## 十二、基于本轮 PDF 输出改造后的最新优先级

本轮已完成：

- 报告“综合估值”段已显式展示：
  - 权重设置
  - 综合估值公式
  - 代入结果
  - 区间宽度
- 最终输出已由 `Markdown` 切换为 `PDF`
- 文件名已带秒级时间戳：
  - `输出/{代码}_{简称}_估值_{YYYYMMDD_HHMMSS}.pdf`
- 本机 PDF smoke test 已通过

因此，后续与输出层相关的下一步优先级建议更新为：

### 1. 等新数据源切入后，优先看真实场景下的 PDF 稳定性

- 重点观察：
  - 单票生成耗时
  - 是否存在偶发 PDF 导出失败
  - Edge 无头导出在连续运行时是否稳定

### 2. 如继续打磨用户体验，只补“小而值钱”的动作

- 生成成功后自动打开 PDF 或输出目录
- 失败时给出更偏用户语言的提示，而不是只展示底层异常
- 必要时补一个“保留 HTML 调试文件”的用户侧说明

### 3. 报告展示层暂不再扩写大段说明

当前已把用户最关心的三件事补齐：

- 综合估值怎么算
- 最终文件在哪里
- 重复生成时怎么区分版本

因此这一层后续除非出现真实样本误解，否则先不继续膨胀字段和解释

## 十三、下一阶段主任务：Tushare 替换与参数训练系统

本轮已确认下一阶段按“两步走”推进：

- 第一步：先替换方法一的可比公司估值数据源
- 第二步：再做离线参数训练系统

### 1. `2026-04-17` 开工前置

明天正式开工前，先确认以下条件：

- 已购买 `Tushare`
- 已拿到并可用 `token`
- 已确认首期需要的接口权限可用
- 首期口径固定为 `T-1 收盘`，暂不做盘中实时

### 2. 第一阶段：方法一数据源替换

第一阶段只动“方法一可比公司估值快照层”，暂不改：

- `IPO 基础信息`
- `近期新股样本`

第一阶段拆解为：

- 抽一层中性数据接口，避免主流程继续写死 `Wind`
- 新建 `tushare_helper.py`
- 统一输出结构，保持 `comparable_data` 口径兼容当前主流程
- 调整参数文件与 `config_loader.py`
- 调整报告中的数据来源文案
- 补最小回归验证

### 3. 第二阶段：离线参数训练系统

第二阶段单独做离线工具，不进入用户入口主流程。

第二阶段拆解为：

- 构建历史样本回放数据集
- 新增调参脚本
- 先调大参数，再调方法二阈值，最后调走势权重
- 使用时间切分验证，避免过拟合
- 输出推荐参数文件与调参报告

### 4. 调参时优先盯住的指标

- 主指标：首日涨幅预测误差 `MAE`
- 辅指标：
  - `RMSE`
  - 区间命中率
  - 方向命中率
  - 综合估值可用率

### 5. 明天开工时的建议顺序

- 先做数据源抽象层
- 再接 `Tushare` 适配器
- 再补回归
- 再开始离线调参系统

### 6. 本轮明确暂不做

- 暂不一起替换 `data_fetcher.py` 的东方财富 IPO / 样本链路
- 暂不做盘中实时估值快照
- 暂不把调参系统塞进最终用户入口
