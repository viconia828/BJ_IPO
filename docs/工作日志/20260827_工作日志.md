# 2026-08-27 工作日志

## 全员碎股输出分支

- 开始调整申购预测输出：针对“顶格仍达不到 100 股正股门槛”的全员碎股场景，单独输出 `0+1`、顶格以上插值户数、碎股名额缓冲户数和分户情景判断。
- 用户确认：此场景“正股抢时间”固定显示“无正股”；“碎股抢时间”须按顶格户数是否覆盖碎股名额及缓冲大小，区分“可能”和“必须”。
- 当前工作树已有多项离线调参数据改动，本轮仅修改预测/报告代码、验证脚本和本日志，不覆盖现有数据改动。

### 实现

- `code/subscription_predictor.py`
  - 账户池碎股估算新增顶格以上户数和“碎股名额－顶格户数”缓冲。
  - 新增全员碎股情景：默认以缓冲户数/顶格户数不超过 20% 作为“分户后可能顶格”的阈值；顶格户数已覆盖碎股名额时输出“必须”。
  - 保留静态 `0+1` 插值门槛，同时生成分户修正门槛、抢时间状态及完整备注。
  - 全员碎股场景的正股提示统一为“无正股（顶格仍不足 100 股正股门槛）”。
- `code/report_generator.py`
  - 一览新增条件式“备注”字段。
  - 全员碎股时“正股抢时间”显示“无正股”；“碎股抢时间”根据新状态显示“可能”“必须”或“否”。
- 新增预测器“可能/必须”两档测试，以及一览文本回归测试。

### 验证

- `python -X utf8 -m py_compile code\subscription_predictor.py code\report_generator.py tests\validate_subscription_predictor.py tests\validate_main_flow_regression.py`：通过。
- `python -X utf8 tests\validate_subscription_predictor.py`：通过。
- `python -X utf8 tests\validate_main_flow_regression.py`：通过。
- `python -X utf8 tests\validate_report_dual_valuation_ranges.py`：通过。
- `python -X utf8 tests\validate_pdf_content_goldens.py`：通过（30 个可比案例、5 个业务案例）。
- 使用百迈科 `920268` 真实数据复核一览：静态 `0+1=576.10 万元`；顶格以上约 8.36 万户；94,850 手碎股名额尚余 11,247 户缓冲；分户所需新增账户约为当前顶格户数的 13.5%，输出“正股抢时间 无正股”“碎股抢时间 可能”，并提示门槛可能上移至顶格 810.88 万元。
- 首次正式报告试生成时出现 Edge 进程先退出、PDF 随后完成落盘的竞态，程序误报导出失败；在 `report_generator.py` 增加进程退出后的短暂 PDF 完成检测窗口。
- 修正后重新生成百迈科报告成功：`输出/920268_百迈科_估值_20260827_212630.pdf` 与对应 `_一览.txt` 均正常产出；一览内容逐项符合本轮规则。

### 提交范围

- 准备将本轮全员碎股输出、PDF 导出竞态修正、对应回归测试与工作日志统一提交。
- 按当前工作树整体交付，同批纳入已更新的离线调参数据、`920269`/`920289` 回放样本、策略参数及自动调参记录。
- 提交前已执行 `git diff --check`，除既有行尾格式提示外未发现空白错误。
